# Project Checkpoint - 2026-05-03 18:29

## Project

- Root: `/Users/kyylian/flowline`
- Product direction: Flowline should feel like a local-first developer workflow layer emerging from the physical MacBook notch.

## Key Decision

Use Boring Notch as interaction inspiration, not as product surface copy. Flowline's `Hardware notch` mode should read as the real black camera housing expanding downward, not as a separate floating widget.

## Implemented Since Previous Checkpoint

- Added `ui-ux-pro-max`-driven design notes in `design-system/MASTER.md`.
- Added shared UI tokens in `Sources/FlowlineApp/Views/FlowlineDesign.swift`.
- Reworked Settings into a cleaner custom compact layout.
- Fixed Accessibility permission refresh and app signing:
  - `script/build_and_run.sh` now signs `dist/Flowline.app` after writing `Info.plist`.
  - Code signing identifier verifies as `dev.kyylian.flowline`.
- Renamed position modes:
  - `Floating pill`
  - `Hardware notch`
- Reworked notch collapsed state:
  - `214x36`
  - black body
  - no collapsed border/shadow
  - no title text
  - minimal status dots + small icon
- Reworked notch expanded state:
  - `644x184`
  - content starts lower
  - animated panel resize
- Fixed notch placement root cause:
  - Do not center on `screen.frame.midX`.
  - Use gap between `NSScreen.auxiliaryTopLeftArea` and `NSScreen.auxiliaryTopRightArea`.
  - Fall back to `frame.midX` only if no camera housing data exists.

## Important Files

- `Sources/FlowlineApp/App/OverlayController.swift`: panel sizing, top-edge frame, hardware notch center using auxiliary top areas.
- `Sources/FlowlineApp/Views/Overlay/OverlayRootView.swift`: notch background, border/shadow behavior, content inset.
- `Sources/FlowlineApp/Views/Overlay/CollapsedPillView.swift`: minimal collapsed notch signals.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`: compact custom settings layout.
- `Sources/FlowlineApp/Models/PositionMode.swift`: user-facing mode names.
- `script/build_and_run.sh`: bundle staging, signing, launch.

## Verification

Latest successful commands:

```bash
rtk swift test
rtk ./script/build_and_run.sh --verify
```

## Next Steps

1. Ask for/inspect a fresh screenshot after the auxiliary-area alignment fix.
2. If alignment is still slightly off, add a `notchOffsetX` calibration preference; do not revert to `screen.midX`.
3. Add staged expansion: grow black body first, then fade/slide `Context / Agent / Shelf` content after 80-120ms.
4. Consider hover-to-expand only after placement and visual illusion are correct.

## Resume Prompt

Continue from this checkpoint. The highest-priority design principle is physical notch illusion: top edge fixed, black tone continuous, minimal collapsed state, content appearing only after expansion. For placement, use `NSScreen.auxiliaryTopLeftArea` / `auxiliaryTopRightArea`; if visually off by a few pixels, add calibration offset.
