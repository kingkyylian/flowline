# Project Checkpoint - 2026-05-24 18:10 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, synchronized with `origin/main`
- Head: `5047b15b413d49e51d71d27fb495456b438b87a3`
- Context: Release-candidate execution was pursued seriously from the previous checkpoint. Source-side gates are freshly verified; notarized release execution remains blocked by missing external Apple Developer ID/notary credentials and missing GitHub Actions secrets.

## Done This Session

- Re-read the latest checkpoint and release workflow/docs.
- Confirmed current git state:
  - `git status --short --branch`: `## main...origin/main`
  - `git log --oneline -5`: latest commit is `5047b15 Clarify release blocker checkpoint`.
- Checked live GitHub workflow state:
  - Workflows are active: `CI`, `Release Candidate Verify`, `Release Candidate`.
  - Latest `main` CI run `26335083723` for `5047b15` completed with `success`.
- Re-checked release credential state:
  - GitHub Actions repository secrets list returned no configured secret names.
  - GitHub repository variables list returned no configured variables.
  - Local Keychain codesigning identities still contain only `Apple Development: bwib3927@outlook.com (G953CU4G2L)`.
  - No local `Developer ID Application` identity is installed.
  - Current shell environment has none of the required release secret values present.
- Ran source-side release validation:
  - `script/publish_preflight.sh` passed with network access after the sandbox-only run failed to resolve `github.com`.
  - `git diff --check` passed.
  - `rtk swift test` passed with 279 tests.
  - `rtk swift build -c release` passed.
  - `script/package_release.sh --preflight` failed as expected because `FLOWLINE_DEVELOPER_ID_IDENTITY` is not set.
- Checked tag readiness:
  - No local `v*` tags are present.
  - `rtk git ls-remote --exit-code --tags origin refs/tags/v0.1.0` exited with no tag output, so `v0.1.0` is not currently present on origin.
- Did not trigger the `Release Candidate` workflow because the missing secrets make a failed run certain and would not move the release forward.

## Current State

- Source tree was clean and pushed before this checkpoint edit.
- Current working tree now contains only this uncommitted checkpoint update:
  - `docs/checkpoints/LATEST.md`
  - `docs/checkpoints/2026-05-24-1810.md`
- Source-side release gates are passing.
- CI is green for current `main`.
- The release-candidate workflows exist and are active.
- A real notarized release candidate still cannot be produced until the external credentials are supplied.

## Important Files / Artifacts

- `.github/workflows/release-candidate.yml`: release candidate workflow; validates secrets, imports Developer ID certificate, stores notary credentials, runs `script/package_release.sh --notarize`, and uploads the archive plus manifest.
- `.github/workflows/release-candidate-verify.yml`: verifies the uploaded release candidate artifact provenance and publish preflight.
- `docs/RELEASE.md`: release checklist and exact secret list.
- `script/package_release.sh`: release packaging and notarization implementation; currently stops before build when Developer ID identity is absent.
- `script/publish_preflight.sh`: publish preflight; passed after network access was available.
- `docs/checkpoints/2026-05-24-1810.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: updated to this checkpoint.

## Verification

- Command: `rtk gh api repos/kingkyylian/flowline/actions/secrets --jq '.secrets[].name'`
  - Result: passed with no output; no repository Actions secrets are listed.
- Command: `rtk gh variable list --repo kingkyylian/flowline`
  - Result: passed with no output; no repository variables are listed.
- Command: `rtk security find-identity -v -p codesigning`
  - Result: only `Apple Development: bwib3927@outlook.com (G953CU4G2L)` is available.
- Command: shell presence check for required release env vars
  - Result: all missing: `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`, `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`, `FLOWLINE_DEVELOPER_ID_IDENTITY`, `FLOWLINE_KEYCHAIN_PASSWORD`, `APPLE_ID`, `APPLE_TEAM_ID`, `APPLE_APP_SPECIFIC_PASSWORD`.
- Command: `script/publish_preflight.sh`
  - Result: first sandbox run failed at GitHub DNS resolution; rerun with network access passed: `Secret scan passed` and `Publish preflight passed for kingkyylian/flowline`.
- Command: `git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed; `Test run with 279 tests in 0 suites passed after 15.363 seconds`.
- Command: `rtk swift build -c release`
  - Result: passed; release build complete.
- Command: `script/package_release.sh --preflight`
  - Result: failed as expected: `FLOWLINE_DEVELOPER_ID_IDENTITY is required for release packaging`.
- Command: `rtk gh run view 26335083723 --repo kingkyylian/flowline --json status,conclusion,headSha,displayTitle,workflowName,createdAt,updatedAt`
  - Result: `CI` run for `5047b15` is `completed` / `success`.
- Command: `rtk git ls-remote --exit-code --tags origin refs/tags/v0.1.0`
  - Result: exit 2 with no tag output; `v0.1.0` is not present on origin.
- Command: `git status --short --branch` after writing this checkpoint
  - Result: branch still synchronized with `origin/main`; only `docs/checkpoints/LATEST.md` and `docs/checkpoints/2026-05-24-1810.md` are changed/untracked.

## Open Questions / Risks

- The release-candidate workflow still requires these repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- Local notarized packaging still requires an installed `Developer ID Application` identity, or CI must import it from the certificate secret.
- `Release Candidate` was not run in this session because the missing secrets would fail the workflow at `Validate release secrets`.

## Next Steps

1. Obtain/export the Developer ID Application `.p12`, base64-encode it, and configure all required GitHub Actions secrets.
2. Run `Release Candidate` from `main` with the intended tag, for example `v0.1.0`.
3. Let `Release Candidate Verify` complete, then run/preserve the publish preflight evidence against the uploaded notarized artifact before creating or pushing the public release tag.

## Project Rules For Resume

- Use `rtk` for noisy shell commands.
- Keep Turkish communication short and concrete.
- Protect dirty worktree state.
- Prefer TDD for behavior changes.
- Do not claim completion without fresh verification.
- No local `AGENTS.md` file exists in the repo; active instructions come from the conversation and user `.codex` files.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short --branch`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, and GitHub Actions secrets/identity state. Do not run the `Release Candidate` workflow unless the required secrets are present. Once credentials are configured, run the workflow for the intended tag and verify the uploaded notarized artifact with `Release Candidate Verify` / `script/publish_preflight.sh --require-ci --require-artifact`.
