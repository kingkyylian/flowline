# Project Checkpoint - 2026-05-20 16:52 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, source HEAD `b094af6 Remove embedded Gemini OAuth client secret`
- Remote: `origin -> https://github.com/kingkyylian/flowline.git`
- Context: Publish blocker was eliminated: the GitHub repo exists, `origin` is configured, and rewritten `main` has been pushed after removing a push-protection secret from history.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and verified the worktree was clean before making changes.
- Resolved the owner/repo ambiguity:
  - `kyylian` exists on GitHub as a separate user.
  - Authenticated GitHub account is `kingkyylian`.
  - `kyylian/flowline` and `kingkyylian/flowline` were both initially absent.
  - Chose `kingkyylian/flowline` because it matches the authenticated account.
- Updated app metadata to the canonical repo:
  - `SettingsPresentation.githubURL`
  - `SettingsPresentation.releasesURL`
  - `SettingsPresentation.licenseURL`
  - Matching settings tests and URL detector sample.
- Created public GitHub repo:
  - `https://github.com/kingkyylian/flowline`
- Added `origin`:
  - `https://github.com/kingkyylian/flowline.git`
- First push was blocked by GitHub push protection because historical commit `cce232e...` contained Google OAuth client values in `AIUsageDirectClients.swift`.
- Removed embedded Gemini OAuth client credentials from HEAD:
  - `GeminiUsageClient` now requires `GEMINI_OAUTH_CLIENT_ID` and `GEMINI_OAUTH_CLIENT_SECRET` to refresh expired Gemini credentials.
  - Without those env vars, it returns no Gemini usage and makes no token-refresh request.
- Added tests for the new behavior:
  - `geminiUsageClientRefreshesExpiredCredentialsAndFetchesQuota`
  - `geminiUsageClientDoesNotRefreshExpiredCredentialsWithoutOAuthClientConfig`
- Rewrote local git history with `git filter-branch` to replace historical Google OAuth literals with placeholders.
- Deleted `refs/original`, expired reflogs, and ran `git gc --prune=now`.
- Pushed rewritten `main` successfully to `origin`.

## Current State

- Source worktree was clean after `b094af6`.
- This checkpoint update is the only pending doc change at checkpoint-write time.
- `main` tracks `origin/main`.
- GitHub default branch is `main`.
- Remote repo is public: `kingkyylian/flowline`.
- `dist/Flowline.app` remains running from prior debug verify, PID `37811`.
- Keychain codesigning identities still show only:
  - `Apple Development: bwib3927@outlook.com (G953CU4G2L)`
- No `Developer ID Application` identity is currently available, so release archive/notarization remains blocked until that certificate exists.

## Important Files / Artifacts

- `Sources/FlowlineApp/Services/AIUsageDirectClients.swift`: Gemini token refresh no longer embeds OAuth client credentials.
- `Tests/FlowlineAppTests/AIUsageDirectClientTests.swift`: covers env-provided Gemini OAuth client config and no-refresh behavior when config is absent.
- `Sources/FlowlineApp/Support/SettingsPresentation.swift`: About URLs now point to `kingkyylian/flowline`.
- `script/publish_preflight.sh`: publish readiness script. Note: under the Codex sandbox, the internal `gh` network check can false-negative even when top-level `rtk gh repo view` succeeds.
- `script/package_release.sh`: release packaging preflight for Developer ID identity.
- `docs/checkpoints/2026-05-20-1652.md`: current checkpoint.

## Verification

- Command: `rtk swift test --filter settingsAboutInfoReadsReleaseMetadataAndGithubURL`
  - Result: first failed against old `kyylian/flowline`, then passed after metadata update.
- Command: `rtk swift test --filter SettingsPresentationTests`
  - Result: passed, 8 tests.
- Command: `rtk swift test --filter URLDetectorsTests`
  - Result: passed, 3 tests.
- Command: `rtk swift test --filter geminiUsageClientDoesNotRefreshExpiredCredentialsWithoutOAuthClientConfig`
  - Result: first failed because hardcoded credentials still refreshed, then passed after implementation.
- Command: `rtk swift test --filter geminiUsageClientRefreshesExpiredCredentialsAndFetchesQuota`
  - Result: passed with env-provided fake OAuth client config.
- Command: `rtk rg <known Google OAuth client markers> -n Sources Tests README.md docs script`
  - Result: no current-tree matches.
- Command: `rtk git log --all -S <known Google OAuth client secret marker> --oneline`
  - Result: no history matches.
- Command: `rtk git log --all -S <known Google OAuth client ID marker> --oneline`
  - Result: no history matches.
- Command: `rtk swift test`
  - Result: passed, 214 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk gh repo view kingkyylian/flowline --json nameWithOwner,url,defaultBranchRef,isPrivate`
  - Result: repo exists, public, default branch `main`.
- Command: `rtk git ls-remote --heads origin main`
  - Result: remote `main` exists at `b094af6`.
- Command: `rtk git push -u origin main`
  - Result: passed; `main` pushed and tracking `origin/main`.

## Open Questions / Risks

- The old Google OAuth client secret was committed historically before this cleanup. It has been removed from this repository’s reachable history and the GitHub push was accepted, but the credential should still be treated as exposed and rotated/revoked in Google Cloud if it was real.
- `script/publish_preflight.sh` can false-negative inside the Codex sandbox because nested `gh` network calls fail; top-level `rtk gh repo view` verified the repo during this session.
- Release archive/notarization remains blocked until a Developer ID Application certificate is installed and `FLOWLINE_DEVELOPER_ID_IDENTITY` is set to that exact installed identity.
- Notarization still requires `FLOWLINE_NOTARY_PROFILE` or Apple ID credentials.

## Next Steps

1. Commit and push this checkpoint update.
2. Rotate/revoke the exposed Google OAuth client secret if it was a real credential.
3. Install/select a Developer ID Application identity, run `script/package_release.sh --preflight`, then run `--archive` or `--notarize`.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -8`, `rtk git remote -v`, `rtk gh repo view kingkyylian/flowline --json nameWithOwner,url,defaultBranchRef,isPrivate`, `rtk security find-identity -v -p codesigning`, and `docs/checkpoints/LATEST.md`. The publish blocker is resolved; the remaining release blocker is Developer ID/notary configuration, plus credential rotation outside this repo if the historical Google OAuth values were real.
