# Project Checkpoint - 2026-05-12 19:34 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with active Flowline notch/HOLD/LIMITS/module changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: HOLD is now the primary polished side-slot surface; latest sweep stabilized screenshot copy/drag, empty state alignment, inline previews, and right-slot behavior.

## Done This Session

- Reworked HOLD text/code hover behavior:
  - removed the floating peek card that covered `COPY/NEXT/DEL`
  - hover now swaps the tray content into an inline 4-line preview inside the tray
  - long native tooltip text was reduced to a compact title help string
- Polished empty HOLD state:
  - enlarged the drop arrow
  - placed the arrow beside `Drop here`
  - centered the arrow/title group over `ss - text - code`
- Changed screenshot copy behavior:
  - screenshot primary action label is now `IMG`
  - clicking `IMG` copies actual image data instead of a shell path
  - ordinary file items still copy URL/path payloads
  - OCR copy is only exposed through the explicit `TEXT` action
  - copied HOLD payloads are marked as handled so HOLD does not re-import its own clipboard write
- Improved screenshot drag-out behavior:
  - screenshot/file drag provider now includes file URL, shell-safe plain text path, legacy string type, and `NSFilenamesPboardType`
  - user physically tested screenshot drag to Terminal and confirmed it is working now
- Found and fixed a review issue in right-slot HOLD:
  - when Workspace owns the left slot and Hold moves right, the app previously rendered an older non-interactive hold summary
  - right-slot Hold now renders the same functional `NotchHoldColumn` with tray/actions/drag provider
  - expanded HOLD drop target now follows Hold placement and accepts right-slot drops when Hold is on the right
  - removed unused old right-side hold row/summary code from the notch overlay file
- Prevented plain window drags from opening the collapsed notch:
  - global mouse-drag expansion now requires a HOLD payload signal and the controller passes `false` for ordinary mouse/window drags
  - real file/text/image drops still expand through the AppKit `NSDraggingInfo` pasteboard path

## Current State

- Flowline has been rebuilt, signed, relaunched, and verified running after the latest sweep.
- Worktree is still dirty and uncommitted.
- Tracked diff currently includes 19 modified files, plus many new untracked support/test files from the HOLD/LIMITS/module batch.
- `docs/checkpoints/LATEST.md` points to this checkpoint after this save.

## Important Files / Artifacts

- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`
  - HOLD tray UI, inline preview, empty state, action row, shared left/right Hold rendering.
- `Sources/FlowlineApp/App/OverlayHostingView.swift`
  - AppKit drag destination bridge; expanded drop target now uses module preferences.
- `Sources/FlowlineApp/Support/OverlayHoldDropTarget.swift`
  - left/right Hold drop frames based on current module slot layout.
- `Sources/FlowlineApp/Support/HoldItemTransfer.swift`
  - screenshot image clipboard copy and Terminal-compatible drag payloads.
- `Sources/FlowlineApp/Support/HoldActionPresentation.swift`
  - screenshot primary action changed from `PATH` to `IMG`; OCR stays explicit as `TEXT`.
- `Sources/FlowlineApp/Services/ShelfService.swift`
  - screenshot stash lifecycle, OCR, clipboard import, and `markPasteboardWriteHandled`.
- `Sources/FlowlineApp/Stores/AppState.swift`
  - HOLD copy/export/cycle/remove action routing.
- `Sources/FlowlineApp/Support/NotchHoldTrayLayout.swift`
  - tray, empty-state, inline-preview, and feedback constants.
- `Tests/FlowlineAppTests/HoldItemTransferTests.swift`
  - verifies screenshot image copy, file path copy, OCR copy, and drag provider types.
- `Tests/FlowlineAppTests/ShelfServiceTests.swift`
  - verifies screenshot stash, OCR, cleanup, export preservation, and self-copy import guard.
- `Tests/FlowlineAppTests/OverlayHoldDropTargetTests.swift`
  - verifies left/right HOLD drop target placement and disabled Hold rejection.
- `Tests/FlowlineAppTests/NotchHoldTrayLayoutTests.swift`
  - verifies empty state alignment, inline preview fit, and action row fit.

## Verification

- Command: `rtk swift test --filter HoldActionPresentationTests`
  - Result: passed.
- Command: `rtk swift test --filter HoldItemTransferTests`
  - Result: passed.
- Command: `rtk swift test --filter ShelfServiceTests`
  - Result: passed.
- Command: `rtk swift test --filter OverlayHoldDropTargetTests`
  - Result: passed.
- Command: `rtk swift test --filter NotchModuleSlotLayoutTests`
  - Result: passed.
- Command: `rtk swift test --filter OverlayDragActivationTests`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed; 139 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.
- Physical validation:
  - user tested screenshot drag/drop/export flow and reported it is working well.

## Open Questions / Risks

- Physical validation was confirmed for the main screenshot flow, but right-slot HOLD should be tested once with Workspace enabled and Music disabled.
- Calendar remains a counted module, not a fully designed notch product surface.
- The worktree is large; before commit, stage related files carefully and avoid accidentally mixing unrelated future polish.

## Next Steps

1. Physically test right-slot Hold by enabling Workspace + Hold and disabling Music, then drop a screenshot onto the right-side Hold tray.
2. Do one last quick pass over settings module labels/count and LIMITS rows in the running app.
3. Commit the stabilized HOLD/LIMITS/module batch.

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For behavior changes, prefer test-first and keep changes scoped.
- Do not revert unrelated user changes in the dirty worktree.

## Resume Prompt

Continue from this checkpoint. First read this file and the project instructions, then verify right-slot Hold with Workspace enabled, Music disabled, and Hold enabled before committing the batch.
