# Project Checkpoint - 2026-05-21 13:56 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit before this checkpoint:
  - `89339c6 Require release archive for tag preflight`
- Context: Continued autonomous release hardening by tying release tag preflight to a concrete archive artifact.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed `main` was clean.
- Selected the next checkpoint target:
  - Prevent release tag creation without a matching release archive/version.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `publishPreflightRequiresReleaseArchiveWhenTagIsProvided`
  - `publishPreflightRejectsReleaseArchiveThatDoesNotMatchTag`
  - `publishPreflightRejectsEmptyReleaseArchive`
  - `publishPreflightAcceptsMatchingReleaseArchiveForReleaseTag`
  - `publishPreflightRejectsReleaseArchiveWithoutReleaseTag`
- Updated existing local/origin tag collision tests to pass a valid archive so they still exercise tag collision behavior.
- Updated `script/publish_preflight.sh`:
  - Accepts `--archive PATH`.
  - Requires `--archive` whenever `--tag vX.Y.Z` is used.
  - Requires archive basename `Flowline-X.Y.Z.zip` to match the tag version.
  - Rejects missing or empty archive files.
  - Rejects `--archive` without `--tag`.
- Updated release docs:
  - `README.md`
  - `docs/RELEASE.md`
- Pushed `89339c6`; GitHub Actions run `26221578104` passed.

## Current State

- Working tree was clean after pushing `89339c6` and before writing this checkpoint.
- `main` and `origin/main` were aligned at `89339c68362f376c2c67bb1c3385c811bbc08a29`.
- GitHub Actions is green on current implementation head:
  - Run: `26221578104`
  - Head: `89339c68362f376c2c67bb1c3385c811bbc08a29`
  - Result: `success`
  - Job: `Swift tests`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.

## Important Files / Artifacts

- `script/publish_preflight.sh`: release tag preflight now requires a matching non-empty archive.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers required, mismatched, empty, missing-tag, positive archive, and tag collision paths.
- `docs/RELEASE.md`: documents tag preflight with matching archive.
- `README.md`: documents release tag preflight with matching archive.
- `/private/tmp/Flowline-0.1.0.zip`: temporary non-empty archive used to verify the real preflight path; not a release artifact.

## Verification

- Command: `rtk swift test --filter publishPreflight`
  - Result: first failed with expected RED issues, then passed with 11 publish preflight tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 35 tests.
- Command: `rtk swift test`
  - Result: passed, 244 tests.
- Command: `rtk ./script/publish_preflight.sh --tag v0.1.0 --archive /private/tmp/Flowline-0.1.0.zip`
  - Result: passed after push.
- Command: `rtk gh run watch 26221578104 --repo kingkyylian/flowline --exit-status`
  - Result: passed.

## Open Questions / Risks

- Historical Google OAuth credential still needs external rotation/revocation if it was real.
- Real release packaging/notarization still requires external Apple credentials:
  - Developer ID Application certificate.
  - `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.
- The real notarization service and Gatekeeper assessment still need one end-to-end run with real credentials before a public release.
- The archive preflight proves presence/name/non-empty artifact, not that the archive was produced by the release packaging script.

## Next Steps

1. Add a stronger archive provenance/version check, such as verifying release metadata inside the archive or producing a manifest during packaging.
2. Run one real notarized release when Apple Developer ID and notary credentials are available.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `script/publish_preflight.sh`, `script/package_release.sh`, `docs/RELEASE.md`, `README.md`, `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`, and the latest GitHub Actions run for `main`. CI is green on `89339c6`; publish preflight now supports `--tag vX.Y.Z --archive Flowline-X.Y.Z.zip` and rejects missing, mismatched, empty, missing-tag, invalid, local-existing, and origin-existing release tag/archive states. Follow project rules from `AGENTS.md`: use `rtk`, preserve unrelated changes, and verify before claiming completion.
