# Project Checkpoint - 2026-05-20 16:40 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, source HEAD `a96bef2 Fix publish preflight file modes`
- Context: Local QA and release preflight are covered; publish readiness now has an explicit preflight path that catches missing or unreachable GitHub remotes before push work.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and verified the worktree was clean before making changes.
- Investigated the publish blocker:
  - `git remote -v` was empty.
  - App metadata points to `https://github.com/kyylian/flowline`.
  - `gh repo view kyylian/flowline` failed with repository not found.
  - GitHub auth is active as `kingkyylian`; `kingkyylian` repo list does not include `flowline`.
- Decided not to add an `origin` remote because the target repository is not currently reachable/verified.
- Added `script/publish_preflight.sh`:
  - Checks that the current directory is a git worktree.
  - Fails if the worktree has uncommitted changes.
  - Fails if `origin` is missing.
  - Parses HTTPS, SSH, and `ssh://` GitHub remote URLs.
  - Uses `gh repo view` to verify the remote repository is reachable through the authenticated GitHub account.
- Updated `README.md` with the publish preflight command.
- Added publish preflight tests in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `publishPreflightFailsWhenOriginRemoteIsMissing`
  - `publishPreflightFailsWhenOriginRepositoryIsNotReachableOnGitHub`
- Fixed file modes so only `script/publish_preflight.sh` is executable; README and tests remain `100644`.
- Committed the source/test/doc changes:
  - `4717bfb Add publish preflight`
  - `a96bef2 Fix publish preflight file modes`

## Current State

- Source worktree was clean after `a96bef2`.
- This checkpoint update is the only pending doc change at checkpoint-write time.
- `script/publish_preflight.sh` currently fails on this repo with `git remote origin is not configured`, which is the correct current blocker.
- `git remote -v` remains empty.
- GitHub auth is active as `kingkyylian`.
- `kyylian/flowline` is not reachable through `gh repo view`.
- `kingkyylian` account has visible repos, but not `kingkyylian/flowline`.
- Keychain codesigning identities still only show Apple Development; no Developer ID Application identity is available.

## Important Files / Artifacts

- `script/publish_preflight.sh`: publish readiness preflight for clean worktree, GitHub origin, and reachable repo.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers release and publish preflight failure paths.
- `README.md`: documents `script/publish_preflight.sh`.
- `script/package_release.sh`: release packaging preflight for Developer ID identity.
- `docs/checkpoints/2026-05-20-1640.md`: current checkpoint.

## Verification

- Command: `rtk swift test --filter publishPreflightFailsWhenOriginRemoteIsMissing`
  - Result: first failed because `script/publish_preflight.sh` did not exist, then passed after implementation.
- Command: `rtk swift test --filter publishPreflightFailsWhenOriginRepositoryIsNotReachableOnGitHub`
  - Result: passed.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 5 tests.
- Command: `rtk bash -n script/publish_preflight.sh`
  - Result: passed.
- Command: `rtk ./script/publish_preflight.sh --help`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 213 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/publish_preflight.sh`
  - Result: failed as expected with `git remote origin is not configured`.
- Command: `rtk git ls-files --stage README.md Tests/FlowlineAppTests/PermissionSurfaceTests.swift script/publish_preflight.sh`
  - Result: README and tests are `100644`; publish preflight script is `100755`.

## Open Questions / Risks

- Publishing remains blocked until the intended GitHub repository exists and `origin` is configured to that verified repository.
- There is an owner mismatch to resolve before adding remote: project links use `kyylian/flowline`, while authenticated GitHub account is `kingkyylian`.
- Release archive/notarization remains blocked until a Developer ID Application certificate is installed and `FLOWLINE_DEVELOPER_ID_IDENTITY` is set to that exact installed identity.
- Notarization still requires `FLOWLINE_NOTARY_PROFILE` or Apple ID credentials.

## Next Steps

1. Commit this checkpoint update.
2. Decide whether the canonical repository should be `kyylian/flowline` or `kingkyylian/flowline`.
3. Create/verify the GitHub repo, add `origin`, then run `script/publish_preflight.sh`.
4. Install/select a Developer ID Application identity, run `script/package_release.sh --preflight`, then run `--archive` or `--notarize`.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status --short`, `rtk git log --oneline -8`, `rtk git remote -v`, `rtk gh auth status`, `rtk gh repo view kyylian/flowline --json nameWithOwner,url`, and `docs/checkpoints/LATEST.md`. Do not add an origin remote until the intended GitHub owner/repository is verified.
