# Project Checkpoint - 2026-05-15 17:13

## Project

- Root: `/Users/kyylian/flowline`
- Current directory: `/Users/kyylian/flowline/Sources/FlowlineApp`
- Git: `main`, dirty working tree with broad Flowline app/core/test changes.
- Recent commit: `c2fc8f9 Record notch UI checkpoint`
- Context: Flowline notch, HOLD, settings, module layout, AI usage, screenshot/OCR, and packaging work are in an active uncommitted state.

## Done This Session

- No product code was changed during this checkpoint turn.
- Captured the current project state after the 2026-05-14 notch/HOLD checkpoint.
- Confirmed there is no repo `AGENTS.md` on disk; active instructions are still the user-provided rules in this conversation.
- Ran a lightweight patch hygiene check with `rtk git diff --check`.

## Current State

- The working tree is intentionally dirty. Do not reset, checkout, or revert broad files without explicit user direction.
- Modified tracked files are concentrated in:
  - App lifecycle and overlay hosting/controller code.
  - Notch overlay rendering and settings UI.
  - AI usage, calendar, shelf/HOLD, and window-title services.
  - FlowlineCore module preferences, shelf item/store, and context summary support.
  - Core tests and packaging scripts.
- Untracked files add new app support/presentation/layout types and many focused FlowlineApp/FlowlineCore tests.
- Existing checkpoint files from 2026-05-11 through 2026-05-14 remain untracked in git.

## Important Files / Artifacts

- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`: major notch module composition changes; now routes left/right slots and includes HOLD/calendar/agent/music rendering work.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`: redesigned settings categories for general, HOLD, modules, shortcuts, and about.
- `Sources/FlowlineApp/Stores/AppState.swift`: central state wiring for new settings, module, HOLD, shelf, and permission behavior.
- `Sources/FlowlineApp/Services/ShelfService.swift`: large shelf/HOLD service expansion.
- `Sources/FlowlineCore/Services/ShelfStore.swift`: persistence changes for shelf/HOLD data.
- `Sources/FlowlineCore/Models/FlowlineModulePreferences.swift`: new/expanded module preference model.
- `Sources/FlowlineApp/Support/NotchHoldTrayLayout.swift`: untracked layout metrics for HOLD tray and action row.
- `Sources/FlowlineApp/Support/NotchModuleSlotLayout.swift`: untracked layout logic for module placement.
- `Sources/FlowlineApp/Support/SettingsPresentation.swift`: untracked presentation model for settings panels.
- `Sources/FlowlineApp/Services/AIUsageDirectClients.swift`: untracked direct AI usage client support.
- `Sources/FlowlineCore/Services/ScreenshotFileScanner.swift`: untracked screenshot scanner support.
- `Tests/FlowlineAppTests/*`: many untracked focused tests for new presentation, layout, HOLD, settings, permission, shelf, and AI usage behavior.
- `docs/checkpoints/2026-05-15-1713.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: updated to this checkpoint.

## Verification

- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: not run during this checkpoint turn.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: not run during this checkpoint turn.
- Previous full verification in `docs/checkpoints/2026-05-14-1723.md`: `rtk swift test` passed with 186 tests and `rtk ./script/build_and_run.sh --verify` passed at that time.

## Project Rules That Affect Resume

- Use `rtk` for shell commands.
- Communicate in Turkish, concise and direct.
- Preserve unrelated dirty worktree changes.
- Prefer `apply_patch` for manual file edits.
- Before claiming completion after code changes, run fresh relevant verification.

## Open Questions / Risks

- The worktree is broad enough that the next change should inspect overlapping diffs before editing.
- Current full test/build status is unknown because only `diff --check` ran in this checkpoint turn.
- New untracked tests are not yet part of a committed baseline; make sure they compile before using them as regression coverage.
- UI-heavy notch/settings changes still need live visual review if the next task is product polish.

## Next Steps

1. Run `rtk swift test` from `/Users/kyylian/flowline` to refresh the baseline across the broad dirty tree.
2. If tests pass, run `rtk ./script/build_and_run.sh --verify` before any visual or packaging claim.
3. Split or review the dirty worktree by area before committing: notch/modules, HOLD/shelf, settings/about, AI usage, screenshot/OCR, tests, and scripts.

## Resume Prompt

Continue from this checkpoint. First read this file and the active user instructions from the conversation, then inspect the current git status and the key changed files above before making changes. Do not revert unrelated worktree changes.
