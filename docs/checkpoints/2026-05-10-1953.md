# Project Checkpoint - 2026-05-10 19:53 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`
- Worktree: clean at checkpoint time.
- Context: Expanded notch layout polish, hover behavior fixes, optional Workspace module, and AI limits display improvements.

## Done Since Previous Checkpoint

- Stabilized expanded notch column layout:
  - `f34fc99 Stabilize expanded notch column layout`
  - Removed expanded-content upward transition offset.
  - Replaced vertical `offset` layout for center/right columns with layout-aware frame/padding.
- Tuned MUSIC column:
  - `4c7925e Align notch music controls`
  - `8590dcc Nudge notch music column left`
  - Music controls are centered to the timeline width.
  - Music column was moved up and then nudged left.
- Reworked three-column positioning around the physical notch:
  - `ba06c49 Make notch columns center-aware`
  - `a31a63e Give notch limits column breathing room`
  - Columns are no longer simple HStack flow; positions are explicit:
    - left column in the left safe area
    - LIMITS near notch center but shifted left for spacing
    - MUSIC in the right safe area
- Made Workspace optional:
  - `5c37d28 Make workspace column optional`
  - Workspace is default off.
  - Settings -> Modules has a Workspace toggle.
  - Default left column is now `ACTIONS`.
- Iterated notch hover behavior:
  - `093ce25 Restrict collapsed hover to notch`
  - `ed97511 Limit notch hover tracking area`
  - `62668ce Sync notch hover tracking state`
  - `c8c474d Drive notch hover from mouse position`
  - Current model: collapsed notch hover is driven by global mouse position against a physical-notch screen rect in `OverlayController`, not SwiftUI `.onHover` or `NSTrackingArea`.
- Improved LIMITS provider display:
  - `bb1d5c8 Show all AI usage providers`
  - LIMITS always shows Codex, Claude, and Gemini.
  - Missing data displays as `--`.
  - Sort priority: provider with the lowest known remaining percent comes first; unknown providers are last; ties fall back to Codex -> Claude -> Gemini.

## Current State

- Worktree is clean.
- Flowline is running from the latest dev build.
- Latest UI snapshot discussed:
  - `ACTIONS` column currently shows `Ready`, `Copy or refresh`, copy context, and refresh limits.
  - User is considering replacing or supplementing that area with more minimal ambient information.

## Verification

- Command: `rtk git diff --check`
- Result: passed.
- Command: `rtk swift test`
- Result: passed, 42 tests after AI usage display tests were added.
- Command: `rtk ./script/build_and_run.sh --verify`
- Result: passed; Flowline rebuilt, signed for development, relaunched, and running.

## Open Design Thread

User asked whether to add minimalist useful elements to the bar edges instead of overworking the main three columns.

Recommended next design direction:

- Add ambient date/time at the bottom corners of the expanded notch.
- Keep it low-emphasis and non-interactive:
  - left lower corner: `SUN 10 MAY` or `MAY 10`
  - right lower corner: `19:53`
  - small monospaced font, low opacity around 0.35-0.45
- Keep main layout as `ACTIONS / LIMITS / MUSIC`.
- Optionally evolve `ACTIONS` into `NOW` later:
  - title: active app/repo/meeting
  - detail: branch/status/next event
  - only show action icons when real actions exist.

## Files Most Relevant For Resume

- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`
  - three-column expanded notch layout
  - ACTIONS / Workspace / LIMITS column views
- `Sources/FlowlineApp/Views/Overlay/MusicOverlayViews.swift`
  - compact music player, timeline, transport controls
- `Sources/FlowlineApp/Models/NotchMetrics.swift`
  - notch geometry constants, column widths/shifts/drops
- `Sources/FlowlineApp/App/OverlayController.swift`
  - panel positioning and current collapsed hover logic
- `Sources/FlowlineApp/App/OverlayHostingView.swift`
  - click hit-test only for notch mode
- `Sources/FlowlineCore/Models/CodexUsageSnapshot.swift`
  - AI provider models and `AIUsageDisplayRows`
- `Tests/FlowlineCoreTests/AIUsageDisplayTests.swift`
  - ordering and missing-provider display behavior

## Open Risks / Things To Check

- Hover behavior should be manually checked on the real notch:
  - should open only when cursor enters the physical notch rect
  - should not open when cursor is above Settings/other windows but below the notch
  - should not intermittently fail after relaunch or screen changes
- Ambient corner text should not collide with the three columns or the physical notch.
- If ACTIONS remains weak visually, consider replacing it with `NOW` instead of adding more controls.

## Resume Prompt

Continue from this checkpoint. First run `git status`. If the goal is the next UI pass, add low-opacity date/time corner text to the expanded notch without changing the core `ACTIONS / LIMITS / MUSIC` layout. Preserve the physical notch safe area and verify with `rtk swift test`, `rtk git diff --check`, and `rtk ./script/build_and_run.sh --verify`.
