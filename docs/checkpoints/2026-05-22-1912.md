# Project Checkpoint - 2026-05-22 19:12 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commits:
  - `0c2b068 Fix hold layer label`
  - `327fe71 Fix about links opening`
  - `b4d1643 Redesign Flowline app and menu bar mark`
- Context: Logo refresh, About project links, and Settings left-layer label fixes are implemented, pushed, and CI-green.

## Done This Session

- Replaced the old app/menu bar mark direction with a Flowline ribbon mark.
  - Updated `FlowlineMarkView`, `FlowlineMarkImage`, and `script/generate_app_icon.swift`.
  - Regenerated `Resources/Flowline.icns`.
  - Added/updated Flowline mark renderer tests.
  - Pushed `b4d1643 Redesign Flowline app and menu bar mark`.
- Wrote a checkpoint for the logo work.
  - Pushed `f856a89 Record logo redesign checkpoint`.
- Fixed About page project buttons not opening links.
  - Root cause: the About project rows used a custom `Button("OPEN") { open(url) }` path.
  - Fix: `AboutLinkRow` now uses SwiftUI `Link("OPEN", destination: url)` for system URL opening.
  - Added regression coverage in `SettingsPresentationTests`.
  - Pushed `327fe71 Fix about links opening`.
- Fixed Settings Modules left-layer copy.
  - User expected `Hold` under `Left layer` to say `Left only`.
  - Root cause: `SettingsPresentation.moduleLayerSections()` still hard-coded `Hold` detail as `Left first`.
  - Fix: changed the presentation data and test expectation to `Left only`.
  - Pushed `0c2b068 Fix hold layer label`.
- Resolved the apparent stale UI after the Hold label fix.
  - Evidence: source and `.build/debug/Flowline` no longer contained `Left first`, but running `dist/Flowline.app/Contents/MacOS/Flowline` still did.
  - Ran `rtk ./script/build_and_run.sh --verify`.
  - New `dist/Flowline.app` was built, signed, launched, and verified running.
  - Post-rebuild binary scan showed `Left first` removed from the running app bundle.

## Current State

- `main` is clean and synchronized with `origin/main`.
- Latest code commit is `0c2b068603e11b908269ceeb8e09bed61dd20ade`.
- Latest CI for `0c2b068` passed:
  - GitHub Actions run `26293803821`
  - Workflow `CI`
  - Conclusion `success`
- `dist/Flowline.app` has been rebuilt from current source and relaunched.
- Running Flowline process observed from:
  - `/Users/kyylian/flowline/dist/Flowline.app/Contents/MacOS/Flowline`

## Important Files / Artifacts

- `Sources/FlowlineApp/Support/SettingsPresentation.swift`: source of Settings module layer labels and About metadata URLs.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`: About project rows now use SwiftUI `Link`.
- `Tests/FlowlineAppTests/SettingsPresentationTests.swift`: regression coverage for About links and module layer labels.
- `Sources/FlowlineApp/Views/FlowlineMarkView.swift`: SwiftUI Flowline ribbon mark.
- `Sources/FlowlineApp/Views/FlowlineMarkImage.swift`: AppKit menu bar template mark.
- `script/generate_app_icon.swift`: deterministic app icon renderer.
- `script/generate_app_icon.sh`: regenerates `Resources/Flowline.icns`.
- `script/build_and_run.sh`: rebuilds and relaunches `dist/Flowline.app`.
- `Resources/Flowline.icns`: regenerated macOS app icon asset.
- `dist/Flowline.app`: local runnable app bundle, rebuilt after the latest label fix.

## Verification

- Command: `rtk swift test --filter FlowlineMark`
  - Result: passed after the logo redesign.
- Command: `rtk swift -frontend -parse script/generate_app_icon.swift`
  - Result: passed.
- Command: `rtk ./script/generate_app_icon.sh`
  - Result: passed; regenerated `Resources/Flowline.icns`.
- Command: `rtk file Resources/Flowline.icns`
  - Result: passed; reported `Mac OS X icon`.
- Command: `rtk swift test --filter settingsAboutProjectRowsUseSystemLinksForExternalURLs`
  - Result: RED before implementation, passed after switching About rows to `Link`.
- Command: `rtk swift test --filter settingsModuleLayersSeparateLeftAndRightRows`
  - Result: RED before implementation, passed after changing `Hold` detail to `Left only`.
- Command: `rtk swift test --filter Settings`
  - Result: passed, 10 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 280 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26293803821 --repo kingkyylian/flowline --exit-status`
  - Result: passed for `0c2b068`.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; `Flowline is running`.
- Command: `rtk rg -a -n "Left first|Left only" dist/Flowline.app/Contents/MacOS/Flowline`
  - Result: current app bundle contains `Left only`; stale `Left first` is gone.

## Open Questions / Risks

- User should visually re-check the live Settings Modules screen after the forced rebuild/relaunch.
- About link buttons should be tested once in the live app if Safari/browser behavior is still in question.
- Logo direction is implemented and CI-green, but visual taste tuning may continue after seeing it at native Dock/menu bar sizes.
- Release candidate workflow still requires repository secrets before notarized release execution.
- No local `AGENTS.md` file is present in the repo, but the session supplied AGENTS context points at user rules in `/Users/kyylian/.codex/RTK.md`, `/Users/kyylian/.codex/CLAUDE-MEM.md`, `/Users/kyylian/.codex/KYYLIAN-PREFS.md`, and `/Users/kyylian/.codex/CODEX-WORKFLOW.md`.
- Continue using `rtk` command prefixes, protect dirty worktree state, prefer TDD for behavior changes, and verify before completion claims.

## Next Steps

1. Open the live Settings Modules screen and confirm `Hold` now reads `Left only`.
2. Click the About page GitHub, Releases, and License `OPEN` controls in the live app.
3. Continue any remaining UI polish or release-candidate setup from the clean `main` state.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `Sources/FlowlineApp/Support/SettingsPresentation.swift`, `Sources/FlowlineApp/Views/Settings/SettingsView.swift`, `Tests/FlowlineAppTests/SettingsPresentationTests.swift`, `Sources/FlowlineApp/Views/FlowlineMarkView.swift`, `Sources/FlowlineApp/Views/FlowlineMarkImage.swift`, `script/generate_app_icon.swift`, and `Resources/Flowline.icns`. The latest code commit is `0c2b068`, CI run `26293803821` is green, and `dist/Flowline.app` has already been rebuilt and relaunched from current source.
