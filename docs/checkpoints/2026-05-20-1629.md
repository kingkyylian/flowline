# Project Checkpoint - 2026-05-20 16:29 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, source HEAD `b740e80 Cover overlay Hold drop smoke`
- Context: Hold drag/drop QA now has importer-level regression coverage plus deterministic overlay lifecycle smoke coverage for text and Finder file URL drops.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and verified the worktree was clean before making changes.
- Chose the next local target from the checkpoint: replace the missing physical Hold drag/drop evidence with a reliable AppKit smoke harness.
- Added `Tests/FlowlineAppTests/OverlayHostingViewHoldDropTests.swift`:
  - `overlayHostingViewImportsExpandedHoldTextDropAndMarksLanding`
  - `overlayHostingViewImportsExpandedHoldFinderFileDropAndMarksLanding`
- The new tests instantiate `OverlayHostingView`, drive `draggingEntered` and `performDragOperation` with an `NSDraggingInfo` test double, and verify:
  - Hold drop targeting is accepted inside the expanded Hold drop column.
  - Text drops are imported into Hold snapshot state.
  - Finder-style file URL drops are imported into Hold snapshot state.
  - Successful drops clear targeted state and increment `holdDropLandingTick`.
- Preserved/restored module `UserDefaults` around the overlay smoke harness.
- Committed the source/test change:
  - `b740e80 Cover overlay Hold drop smoke`

## Current State

- Source worktree was clean after `b740e80`.
- This checkpoint update is the only pending doc change at checkpoint-write time.
- `dist/Flowline.app` was rebuilt and is running.
- UserDefaults remain restored:
  - `module.workspace.enabled = 0`
  - `module.music.enabled = 1`
  - `module.calendar.enabled = 0`
  - `module.shelf.enabled = 1`
  - `hold.autoCapture.clipboardText = 1`
  - `hold.autoCapture.screenshots` key absent, so it uses fallback `true`.

## Important Files / Artifacts

- `Tests/FlowlineAppTests/OverlayHostingViewHoldDropTests.swift`: deterministic AppKit smoke harness for overlay Hold drop lifecycle and Finder file drops.
- `Sources/FlowlineApp/Support/HoldDropImporter.swift`: testable Hold drop importer for file promise, file URL, web URL, image data, and text pasteboards.
- `Sources/FlowlineApp/App/OverlayHostingView.swift`: delegates drop imports through `HoldDropImporter` and marks landing feedback after successful imports.
- `Tests/FlowlineAppTests/HoldDropImporterTests.swift`: importer-level text, web URL, and Finder file URL coverage.
- `/private/tmp/flowline-settings-modules-window-qa.png`: prior Settings Modules screenshot showing module toggle states.
- `/private/tmp/flowline-secret-expanded-window-qa.png`: prior overlay evidence showing `Sensitive clip SECRET` without countdown.
- `docs/checkpoints/2026-05-20-1629.md`: current checkpoint.

## Verification

- Command: `rtk swift test --filter OverlayHostingViewHoldDropTests`
  - Result: passed, 2 overlay lifecycle smoke tests.
- Command: `rtk swift test --filter HoldDropImporterTests`
  - Result: passed, 3 HoldDropImporter tests.
- Command: `rtk swift test --filter HoldDropTypesTests`
  - Result: passed, 5 HoldDropTypes tests.
- Command: `rtk swift test --filter HoldDropImportPlanTests`
  - Result: passed, 3 HoldDropImportPlan tests.
- Command: `rtk swift test`
  - Result: passed, 210 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; debug app was built, signed with the Apple Development identity, and Flowline is running.
- Command: `rtk defaults read dev.kyylian.flowline`
  - Result: module/Hold defaults remained restored after tests and app verify.

## Open Questions / Risks

- A literal manual Finder drag gesture into the live overlay was still not performed; the missing evidence is now covered by a deterministic `OverlayHostingView` AppKit drag lifecycle smoke harness using Finder-style file URL pasteboards.
- Push remains blocked until a git remote is configured.
- Release packaging remains blocked until a Developer ID Application certificate is available and `FLOWLINE_DEVELOPER_ID_IDENTITY` is set.

## Next Steps

1. Commit this checkpoint update.
2. Configure a git remote when ready to publish the completed QA commits.
3. Configure Developer ID Application signing identity when ready to produce a release package.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status --short`, `rtk git log --oneline -5`, and `docs/checkpoints/LATEST.md`. Local Hold QA now includes importer-level and overlay lifecycle smoke coverage; remaining non-local release work is blocked by missing git remote and Developer ID identity.
