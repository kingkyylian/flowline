# Project Checkpoint - 2026-05-22 15:36 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit:
  - `5fa5efa Verify release workflow run attempt`
- Context: Continued autonomous release hardening by requiring release manifests to match the actual GitHub Actions run attempt.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current `main` and CI were clean.
- Selected the next secret-free release hardening target:
  - `script/package_release.sh` records `github_run_attempt=$GITHUB_RUN_ATTEMPT` in release manifests.
  - `script/publish_preflight.sh --require-ci` verified run ID, head SHA, status, conclusion, and workflow metadata.
  - It did not verify manifest `github_run_attempt` against the actual GitHub Actions run attempt.
  - That left the run attempt value self-attested in release provenance.
- Confirmed `gh run view` exposes the `attempt` field.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `publishPreflightRejectsCIManifestWhenWorkflowRunAttemptDoesNotMatch`
  - The test first failed because manifest attempt `2` passed against actual run attempt `1`.
- Updated the fake GitHub run fixture:
  - Supports an `attempt` field.
  - Returns the attempt only when the script asks for it, preserving older metadata call behavior.
- Updated `script/publish_preflight.sh`:
  - Requests `attempt` from `gh run view`.
  - Parses the fifth run metadata field.
  - Requires manifest `github_run_attempt` to be present and match the actual run attempt whenever `--require-ci` is used.
- Updated `README.md` and `docs/RELEASE.md` to document that `--require-ci` verifies the successful GitHub Actions run attempt.
- Pushed implementation commit `5fa5efa`.
- GitHub Actions run `26287978320` passed for `5fa5efa`.

## Current State

- The release hardening implementation is pushed at `5fa5efa753106387602c633486a62e5a1d079b71`.
- Working tree was clean before writing this checkpoint.
- Release identity/provenance checks now cover:
  - exact candidate artifact names;
  - artifact version to archive/tag binding;
  - manifest GitHub artifact name to release tag binding;
  - manifest workflow binding to `Release Candidate`;
  - actual GitHub run workflow binding to `Release Candidate`;
  - manifest GitHub run attempt binding to the actual run attempt;
  - manifest notarization binding to `notarized=true`;
  - downloaded artifact ZIP hash matching the local archive;
  - downloaded artifact manifest content matching the local manifest;
  - ambiguous downloaded archives and manifests;
  - CI run head/status matching;
  - release manifest archive name, version, checksum, size, and git commit.

## Important Files / Artifacts

- `script/publish_preflight.sh`: now verifies actual GitHub run attempt metadata for `--require-ci`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers mismatched manifest run attempts.
- `README.md`: documents run attempt verification.
- `docs/RELEASE.md`: documents run attempt verification.
- `.github/workflows/release-candidate.yml`: source of release manifests that include `github_run_attempt`.
- `.github/workflows/release-candidate-verify.yml`: runs publish preflight with `--require-ci --require-artifact`.

## Verification

- Command: `rtk gh run view 26287373080 --repo kingkyylian/flowline --json databaseId,attempt,headSha,status,conclusion,workflowName --jq 'to_entries | map(.key + "=" + (.value|tostring)) | .[]'`
  - Result: passed; confirmed `attempt=1` is available from `gh run view`.
- Command: `rtk swift test --filter WorkflowRunAttemptDoesNotMatch`
  - Result: first failed against the old script because mismatched run attempts passed, then passed after enforcing attempt metadata.
- Command: `rtk swift test --filter 'publishPreflight|releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 67 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 276 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26287978320 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `5fa5efa`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `5fa5efa`; publish preflight now requires manifest `github_run_attempt` to match actual `gh run view` `attempt` when `--require-ci` is used.
