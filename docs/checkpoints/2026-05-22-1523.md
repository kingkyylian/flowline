# Project Checkpoint - 2026-05-22 15:23 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit:
  - `a874d8b Verify release artifact workflow run`
- Context: Continued autonomous release hardening by requiring the actual GitHub Actions workflow run to be `Release Candidate` for artifact-backed publication.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current `main` and CI were clean.
- Selected the next secret-free release hardening target:
  - `script/publish_preflight.sh --require-artifact` already required manifest `github_workflow=Release Candidate`.
  - That value was still self-attested by the manifest.
  - The script fetched GitHub run head/status/conclusion, but did not verify the actual run's `workflowName`.
  - A successful non-release workflow could therefore upload a matching artifact and manifest that claimed `Release Candidate`.
- Confirmed `gh run view` supports `workflowName` in run metadata.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `publishPreflightRejectsCIArtifactWhenWorkflowRunIsNotReleaseCandidate`
  - The test first failed because a run with actual workflow `CI` passed when its manifest claimed `Release Candidate`.
- Updated the fake GitHub run fixture to return `workflowName` only when the script asks for it, matching the real CLI shape and preserving older metadata call behavior.
- Updated `script/publish_preflight.sh`:
  - Requests `workflowName` from `gh run view`.
  - Parses the fourth run metadata field.
  - Requires actual run workflow `Release Candidate` whenever `--require-artifact` is used.
- Updated `README.md` and `docs/RELEASE.md` to document that both manifest and GitHub run metadata must identify the `Release Candidate` workflow.
- Pushed implementation commit `a874d8b`.
- GitHub Actions run `26287373080` passed for `a874d8b`.

## Current State

- The release hardening implementation is pushed at `a874d8bf399117b4b97f3b9a348ccd16ca93bd2d`.
- Working tree was clean before writing this checkpoint.
- Release identity/provenance checks now cover:
  - exact candidate artifact names;
  - artifact version to archive/tag binding;
  - manifest GitHub artifact name to release tag binding;
  - manifest workflow binding to `Release Candidate`;
  - actual GitHub run workflow binding to `Release Candidate`;
  - manifest notarization binding to `notarized=true`;
  - downloaded artifact ZIP hash matching the local archive;
  - downloaded artifact manifest content matching the local manifest;
  - ambiguous downloaded archives and manifests;
  - CI run head/status matching;
  - release manifest archive name, version, checksum, size, and git commit.

## Important Files / Artifacts

- `script/publish_preflight.sh`: now verifies actual GitHub run workflow metadata for `--require-artifact`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers a non-Release-Candidate run whose manifest claims Release Candidate.
- `README.md`: documents manifest and run metadata workflow checks.
- `docs/RELEASE.md`: documents manifest and run metadata workflow checks.
- `.github/workflows/release-candidate.yml`: expected source workflow for release artifacts.
- `.github/workflows/release-candidate-verify.yml`: runs publish preflight with `--require-ci --require-artifact`.

## Verification

- Command: `rtk gh run view 26286817944 --repo kingkyylian/flowline --json headSha,status,conclusion,workflowName --jq '.headSha + "\t" + .status + "\t" + (.conclusion // "") + "\t" + .workflowName'`
  - Result: passed; confirmed `workflowName` is available from `gh run view`.
- Command: `rtk swift test --filter WorkflowRunIsNotReleaseCandidate`
  - Result: first failed against the old script because the actual `CI` workflow run passed, then passed after enforcing workflow metadata.
- Command: `rtk swift test --filter 'publishPreflight|releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 66 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 275 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26287373080 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `a874d8b`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `a874d8b`; publish preflight now requires both manifest `github_workflow` and actual `gh run view` `workflowName` to be `Release Candidate` when `--require-artifact` is used.
