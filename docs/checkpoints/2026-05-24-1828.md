# Project Checkpoint - 2026-05-24 18:28 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, synchronized with `origin/main`
- Head: `5047b15b413d49e51d71d27fb495456b438b87a3`
- Context: Release-candidate execution remains blocked by missing external Apple/GitHub credentials, but the repo now has tested helpers for both secret setup and guarded workflow dispatch.

## Done This Session

- Added `script/run_release_candidate.sh`:
  - Requires `--tag vX.Y.Z`.
  - Resolves `--repo` or GitHub `origin`.
  - Refuses non-`main` branches.
  - Refuses existing local or origin release tags.
  - Runs `script/publish_preflight.sh`.
  - Runs `script/configure_release_secrets.sh --check`.
  - Dispatches `release-candidate.yml` only after all guards pass.
  - Supports `--dry-run` using the same guards without dispatching.
- Updated `docs/RELEASE.md` to use `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0`.
- Added release-runner tests to `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - Static guard coverage for publish preflight, secret check, branch/tag gates, and `gh workflow run`.
  - Fake `git`, fake `gh`, and fake sibling scripts proving dispatch happens only after guards pass.
- Proved live safety behavior:
  - `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0 --dry-run` stops before dispatch because the current worktree has uncommitted changes.
  - `script/configure_release_secrets.sh --repo kingkyylian/flowline --check` still reports all seven required GitHub Actions secrets missing.

## Current State

- Source branch is still synchronized with `origin/main`.
- Working tree is intentionally dirty with release-helper, release-runner, docs, tests, and checkpoint work:
  - `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`
  - `docs/RELEASE.md`
  - `docs/checkpoints/LATEST.md`
  - `docs/checkpoints/2026-05-24-1810.md`
  - `docs/checkpoints/2026-05-24-1821.md`
  - `docs/checkpoints/2026-05-24-1828.md`
  - `script/configure_release_secrets.sh`
  - `script/run_release_candidate.sh`
- Source-side release gates are passing.
- A real notarized release candidate still cannot be produced until the external credentials are supplied and configured.

## Important Files / Artifacts

- `script/configure_release_secrets.sh`: helper for validating/configuring/checking release GitHub Actions secrets.
- `script/run_release_candidate.sh`: new guarded workflow dispatch helper.
- `docs/RELEASE.md`: now documents both helper-based secret setup and guarded release-candidate dispatch.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: release helper and dispatch guard test coverage.
- `.github/workflows/release-candidate.yml`: unchanged release candidate workflow consumed by the new dispatch helper.
- `.github/workflows/release-candidate-verify.yml`: unchanged artifact provenance verifier.
- `docs/checkpoints/2026-05-24-1828.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: updated to this checkpoint.

## Verification

- Command: `rtk swift test --filter releaseCandidateRunScript`
  - Result: passed; 2 focused release-runner tests passed.
- Command: `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0 --dry-run`
  - Result: expected failure; stopped at `script/publish_preflight.sh` because the worktree has uncommitted changes.
- Command: `script/secret_scan.sh`
  - Result: passed.
- Command: `git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed; `Test run with 285 tests in 0 suites passed after 25.546 seconds`.
- Command: `rtk swift build -c release`
  - Result: passed; release build complete.
- Command: `script/configure_release_secrets.sh --repo kingkyylian/flowline --check`
  - Result: expected failure; all seven required GitHub Actions secrets are missing.
- Command: `git status --short --branch`
  - Result: branch is synchronized with `origin/main`; release-helper/checkpoint files are uncommitted.

## Open Questions / Risks

- The release-candidate workflow still requires these repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- Local notarized packaging still requires an installed `Developer ID Application` identity, or CI must import it from the configured certificate secret.
- Helper/test/docs/checkpoint changes are not committed or pushed.
- `script/run_release_candidate.sh` will intentionally refuse to dispatch until the helper work is committed or otherwise removed from the worktree and the required secrets exist.

## Next Steps

1. Commit and push the release helper/test/docs/checkpoint changes if they should become part of the release process.
2. Provide the Developer ID `.p12`, certificate password, Developer ID identity string, Apple ID, Team ID, and app-specific password.
3. Run `script/configure_release_secrets.sh --repo kingkyylian/flowline --dry-run`, then run it without `--dry-run`, then run `--check`.
4. Run `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0`.
5. Let `Release Candidate Verify` complete, then preserve the publish preflight evidence against the uploaded notarized artifact before creating or pushing the public release tag.

## Project Rules For Resume

- Use `rtk` for noisy shell commands.
- Keep Turkish communication short and concrete.
- Protect dirty worktree state.
- Prefer TDD for behavior changes.
- Do not claim release completion without fresh verification.
- No local `AGENTS.md` file exists in the repo; active instructions come from the conversation and user `.codex` files.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short --branch`, `docs/checkpoints/LATEST.md`, `script/configure_release_secrets.sh`, `script/run_release_candidate.sh`, `docs/RELEASE.md`, and GitHub Actions secret state. Do not run the `Release Candidate` workflow until `script/configure_release_secrets.sh --repo kingkyylian/flowline --check` passes and the worktree is clean/pushed. Once credentials are configured, run `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0` and verify the uploaded notarized artifact with `Release Candidate Verify` / `script/publish_preflight.sh --require-ci --require-artifact`.
