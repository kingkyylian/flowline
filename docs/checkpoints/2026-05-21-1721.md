# Project Checkpoint - 2026-05-21 17:21 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest implementation commit:
  - `524d449 Bind verified archive version to artifact name`
- Context: Continued autonomous release hardening by binding the verified archive name to the exact release artifact version.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed the working tree was clean on `main`.
- Selected the next secret-free release hardening target:
  - `Release Candidate Verify` already selected exact artifact names such as `flowline-release-v1.2.3`.
  - It still accepted any single `Flowline-*.zip` inside the artifact and derived `RELEASE_TAG` from that zip name.
  - A mismatched artifact/archive pair could therefore pass the artifact-name filter while verifying a different version.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `releaseCandidateVerifyWorkflowBindsArchiveVersionToArtifactName` requires the workflow to derive `ARTIFACT_RELEASE_VERSION` from `artifact_name`.
  - The test requires the archive lookup to use `Flowline-$ARTIFACT_RELEASE_VERSION.zip`.
  - The test prevents reintroducing tag derivation from `Flowline-*.zip`.
- Updated `.github/workflows/release-candidate-verify.yml`:
  - Extracts `artifact_version="${artifact_name#flowline-release-v}"`.
  - Exports `ARTIFACT_RELEASE_VERSION` through `$GITHUB_ENV`.
  - Resolves only `Flowline-$ARTIFACT_RELEASE_VERSION.zip`.
  - Sets `RELEASE_TAG=v$ARTIFACT_RELEASE_VERSION`.
- Updated the ambiguous archive invariant test to expect the exact versioned archive lookup.
- Pushed implementation commit `524d449`.
- GitHub Actions run `26231776319` passed for `524d449`.

## Current State

- The release hardening implementation is pushed at `524d449817a204ad34a2eb4dee02c12165f39d31`.
- Working tree was clean before writing this checkpoint.
- Release Candidate Verify now requires all three release identity surfaces to agree:
  - candidate artifact name: `flowline-release-vX[.Y[.Z]]`;
  - archive name: `Flowline-X[.Y[.Z]].zip`;
  - publish preflight tag: `vX[.Y[.Z]]`.
- Existing release protections remain in place:
  - release candidate workflow runs publish preflight before release secret validation;
  - Developer ID `.p12` import uses scoped keychain access;
  - signing identity is preflighted before notary credential storage;
  - release candidate verification rejects ambiguous artifacts and archives;
  - publish preflight verifies CI and artifact provenance for the produced archive.

## Important Files / Artifacts

- `.github/workflows/release-candidate-verify.yml`: artifact version extraction and exact archive lookup.
- `.github/workflows/release-candidate.yml`: guarded release candidate build and notarization workflow.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers release workflow security invariants.
- `script/publish_preflight.sh`: repository hygiene, release manifest, CI, and artifact provenance checks.

## Verification

- Command: `rtk swift test --filter BindsArchiveVersionToArtifactName`
  - Result: first failed against the old workflow, then passed after binding archive lookup to artifact version.
- Command: `rtk swift test --filter 'releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 59 tests.
- Command: `rtk ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/ci.yml .github/workflows/release-candidate.yml .github/workflows/release-candidate-verify.yml`
  - Result: passed.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 268 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26231776319 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `524d449`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The new workflows are loaded and locally/CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `524d449`; Release Candidate Verify now binds the archive name and release tag to the exact selected artifact version.
