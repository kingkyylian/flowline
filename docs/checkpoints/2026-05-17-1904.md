# Project Checkpoint - 2026-05-17 19:04

## Project

- Root: `/Users/kyylian/flowline`
- Git: `main`, latest commit `f048c39 Fix stale music playback toggle`.
- Context: Active uncommitted work is Flowline HOLD/screenshot/settings polish inside a broad dirty tree.

## Done This Session

- Fixed screenshot auto-capture latency for HOLD:
  - Existing screenshot files are seeded so old files are not imported.
  - Newly discovered screenshots can be captured by discovery time even when modification time is stale.
  - Screenshot watcher is seeded on service start and when screenshot capture is enabled.
- Added/updated screenshot scanner and shelf service tests for the faster capture path.
- Polished HOLD screenshot preview caption spacing so the `SS` label sits lower and does not crowd the preview.
- Reworked HOLD text/code caption summaries:
  - Text/code captions now use metadata instead of showing content excerpts.
  - Short text shows character count; multiline/code can show line count.
- Moved stacked HOLD item quantity out of the caption:
  - Removed inline `xN` beside `TEXT`/`CODE`/`SS`.
  - Added a detached top-right stack badge over the preview card when `itemCount > 1`.
- Reworked Settings > Modules:
  - Layout preview now includes the capacity label, for example `0/2 + Limits`.
  - Module toggles are split into `Left layer` and `Right layer` blocks.
  - Left layer rows: Workspace `Left only`, Hold `Left first`.
  - Right layer rows: Music `Right only`, Calendar `Right first`.
- Updated core module placement policy:
  - Music is now right-only at the selection-policy level.
  - Workspace is now left-only.
  - Hold and Calendar remain flexible according to their existing fallback behavior.

## Current State

- Working tree is intentionally dirty with broad ongoing feature work and many untracked support/test files.
- No files are staged.
- The app was rebuilt, signed, and launched after the latest settings/module changes.
- `docs/checkpoints/2026-05-17-1904.md` is the new checkpoint.
- `docs/checkpoints/LATEST.md` should mirror this checkpoint after save.
- Do not reset, checkout, delete, or revert unrelated dirty files without explicit direction.

## Important Files / Artifacts

- `Sources/FlowlineCore/Models/FlowlineModulePreferences.swift`: module capacity, supported side placements, rejection reasons, and placement policy. Music is right-only here.
- `Sources/FlowlineApp/Support/SettingsPresentation.swift`: settings presentation data for layout preview and left/right module layer sections.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`: Settings > Modules UI now renders layout preview plus left/right layer blocks.
- `Sources/FlowlineCore/Services/ScreenshotFileScanner.swift`: screenshot discovery/seeding behavior for fast HOLD auto-capture.
- `Sources/FlowlineApp/Services/ShelfService.swift`: screenshot auto-capture service wiring and seeding integration.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`: HOLD preview rendering, stack badge, captions, and tray composition.
- `Sources/FlowlineApp/Support/NotchHoldTrayLayout.swift`: HOLD layout metrics including screenshot caption spacing and stack badge sizing.
- `Tests/FlowlineCoreTests/ModulePreferencesTests.swift`: module placement policy coverage, including Music right-only.
- `Tests/FlowlineAppTests/SettingsPresentationTests.swift`: settings preview and left/right layer presentation coverage.
- `Tests/FlowlineCoreTests/ScreenshotFileScannerTests.swift`: screenshot scanner regression coverage.
- `Tests/FlowlineAppTests/ShelfServiceTests.swift`: shelf screenshot auto-capture and seeding coverage.
- `Tests/FlowlineAppTests/HoldPreviewPresentationTests.swift`: HOLD caption summary behavior.
- `Tests/FlowlineAppTests/HoldItemMetadataPresentationTests.swift`: text/code metadata caption behavior.
- `Tests/FlowlineAppTests/NotchHoldTrayLayoutTests.swift`: HOLD preview/caption/badge layout coverage.

## Verification

- Command: `rtk swift test --filter settingsModuleLayersSeparateLeftAndRightRows`
  - Result: passed.
- Command: `rtk swift test --filter musicModuleSupportsOnlyTheRightSide`
  - Result: passed after the policy update.
- Command: `rtk swift test --filter ModulePreferencesTests`
  - Result: passed, 5 tests.
- Command: `rtk swift test --filter NotchModuleSlotLayoutTests`
  - Result: passed, 5 tests.
- Command: `rtk swift test --filter SettingsPresentationTests`
  - Result: passed, 8 tests.
- Command: `rtk swift test`
  - Result: passed, 201 Swift Testing tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; `Flowline is running`.

## Project Rules That Affect Resume

- Use `rtk` for shell commands.
- Communicate with the user in Turkish, concise and direct.
- Preserve unrelated dirty worktree changes.
- Use `apply_patch` for manual edits.
- Before claiming completion after code changes, run fresh relevant verification.
- No `AGENTS.md` file exists on disk at the project root; active AGENTS-style instructions were provided in the conversation and reference local preference files.

## Open Questions / Risks

- The worktree remains broad and dirty across notch modules, HOLD/shelf, settings, AI usage, calendar, scripts, and checkpoint docs.
- Commit boundaries need careful splitting. Likely candidates are screenshot auto-capture, HOLD preview polish, and settings/module placement UX.
- The settings module split is built and tested, but should still get a quick visual check in the running app.
- Several support and test files are untracked as part of the larger feature set; do not delete them during cleanup.

## Next Steps

1. Visually check Settings > Modules in the running app and confirm the left/right layer wording feels clear.
2. Visually check HOLD screenshot/text/code previews one more time, especially caption spacing and the detached stack badge.
3. Split commits carefully by feature area, review `rtk git diff --cached`, and rerun relevant tests before each commit.

## Resume Prompt

Continue from this checkpoint. First read this file and the active user instructions from the conversation, then inspect `rtk git status --short` and `rtk git log --oneline -5`. Do not revert unrelated worktree changes.
