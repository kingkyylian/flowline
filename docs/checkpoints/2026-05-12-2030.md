# Project Checkpoint - 2026-05-12 20:30 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with active Flowline notch/HOLD/LIMITS/module changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: HOLD side-slot is polished; screenshot drag-to-open works; clipboard text capture is now faster and user-controlled.

## Done This Session

- Fixed screenshot drag-to-open regression:
  - `OverlayController` now reads `NSPasteboard.Name.drag` during collapsed drag events
  - panel expands only for real HOLD payloads: file, screenshot, image data, text, file promise
  - ordinary Terminal/window drags still do not open the panel
  - legacy `NSFilenamesPboardType` filename drags are supported
- Made automatic HOLD capture user-controlled:
  - `Clipboard text` default is off
  - `Screenshots` default is on
  - Settings > Behavior has `Hold capture` toggles
  - enabling capture marks the current pasteboard as handled to avoid importing old clipboard contents
- Reduced clipboard text capture latency:
  - pasteboard polling is now a separate fast timer at `0.2s`
  - screenshot directory fallback polling stays at `1.0s` to avoid aggressive directory scanning
  - timers run in `.common` run loop mode
  - added a regression test requiring opt-in clipboard text to land within `0.35s`
- Clarified screenshot auto-capture behavior:
  - auto capture is for new screenshots created while the service is running
  - old desktop screenshots are not pulled into HOLD retroactively, by design

## Current State

- Flowline has been rebuilt, signed, relaunched, and verified running after the fast clipboard polling change.
- Worktree is still dirty and uncommitted.
- `docs/checkpoints/LATEST.md` points to this checkpoint.

## Important Files / Artifacts

- `Sources/FlowlineApp/Services/ShelfService.swift`
  - split fast pasteboard timer from screenshot directory polling; owns auto-capture options.
- `Tests/FlowlineAppTests/ShelfServiceTests.swift`
  - includes fast clipboard capture test plus auto-capture option tests.
- `Sources/FlowlineApp/App/OverlayController.swift`
  - collapsed notch drag expansion uses drag pasteboard detection.
- `Sources/FlowlineApp/App/OverlayHostingView.swift`
  - AppKit drag bridge uses shared HOLD payload detection.
- `Sources/FlowlineApp/Support/HoldDropPayloadDetector.swift`
  - shared pasteboard classifier for HOLD payloads.
- `Sources/FlowlineApp/Support/HoldDropTypes.swift`
  - registered drop pasteboard types, including `NSFilenamesPboardType`.
- `Sources/FlowlineApp/Stores/AppState.swift`
  - persists `hold.autoCapture.screenshots` and `hold.autoCapture.clipboardText`.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`
  - `Hold capture` toggles.

## Verification

- Command: `rtk swift test --filter shelfServiceCapturesClipboardTextQuicklyWhenEnabled`
  - Result: failed first with old 1s polling; passed after fast pasteboard timer, around 0.21s.
- Command: `rtk swift test --filter ShelfServiceTests`
  - Result: passed; 20 tests.
- Command: `rtk swift test`
  - Result: passed; 149 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.
- Physical validation:
  - user confirmed screenshot drag now opens the panel and drop works.

## Open Questions / Risks

- User should physically retest `Clipboard text` enabled: copy text and confirm it lands in HOLD quickly.
- User should verify screenshot auto-capture with a newly created screenshot, not an old desktop file.
- Right-slot HOLD should still be physically tested once with Workspace enabled and Music disabled.
- The worktree is large; stage related files carefully before commit.

## Next Steps

1. Physically test `Clipboard text` toggle on, then `cmd+c`/`ctrl+c` text capture latency.
2. Physically test newly created screenshot auto-capture with `Screenshots` enabled.
3. Commit the stabilized HOLD/LIMITS/module batch after physical validation.

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For behavior changes, prefer test-first and keep changes scoped.
- Do not revert unrelated user changes in the dirty worktree.

## Resume Prompt

Continue from this checkpoint. First read this file and the project instructions, then physically validate fast clipboard text capture and new screenshot auto-capture before committing.
