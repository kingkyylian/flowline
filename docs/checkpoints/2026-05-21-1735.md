# Project Checkpoint - 2026-05-21 17:35 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest implementation commit:
  - `5443e0a Reject ambiguous downloaded release archives`
- Context: Continued autonomous release hardening by making publish preflight reject ambiguous downloaded GitHub Actions release archives.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current head/CI were clean.
- Selected the next secret-free release hardening target:
  - `Release Candidate Verify` already rejected ambiguous artifacts and exact archive mismatches.
  - `script/publish_preflight.sh --require-artifact` still used `find ... -print -quit` after downloading the manifest's GitHub Actions artifact.
  - That allowed an artifact with multiple same-named release archives to pass if the first found archive matched the local archive hash.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `publishPreflightRejectsCIManifestWhenDownloadedArtifactHasAmbiguousArchives` creates a fake downloaded artifact with two `Flowline-1.2.3.zip` files.
  - The old script passed this case; the test now requires a failure.
  - Extended the fake GitHub artifact fixture to write a duplicate archive under a nested directory.
- Updated `script/publish_preflight.sh`:
  - Replaced `find "$download_dir" ... -print -quit` with a sorted full archive list.
  - Counts matching downloaded archives.
  - Keeps the existing missing-archive error for zero matches.
  - Fails when more than one downloaded archive matches the expected release archive name.
- Pushed implementation commit `5443e0a`.
- GitHub Actions run `26232570549` passed for `5443e0a`.

## Current State

- The release hardening implementation is pushed at `5443e0a273cb2d20d94be6571bc75cf7004e4d27`.
- Working tree was clean before writing this checkpoint.
- Release identity/provenance checks now cover:
  - exact candidate artifact names;
  - artifact version to archive/tag binding;
  - ambiguous downloaded archives in both the verify workflow and publish preflight;
  - CI run head/status matching;
  - release manifest archive name, version, checksum, size, and git commit.

## Important Files / Artifacts

- `script/publish_preflight.sh`: now rejects multiple downloaded archives with the expected release archive name.
- `.github/workflows/release-candidate-verify.yml`: downloads the exact candidate artifact and runs publish preflight with `--require-ci --require-artifact`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers release workflow and publish preflight security invariants.
- `.github/workflows/release-candidate.yml`: guarded notarized release candidate builder.

## Verification

- Command: `rtk swift test --filter DownloadedArtifactHasAmbiguousArchives`
  - Result: first failed against the old script because preflight passed the ambiguous artifact, then passed after the script counted matches.
- Command: `rtk swift test --filter 'publishPreflight|releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 60 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 269 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26232570549 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `5443e0a`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The new workflows are loaded and locally/CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `5443e0a`; publish preflight now rejects ambiguous same-named downloaded release archives.
