# Project Checkpoint - 2026-05-24 19:29 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, synchronized with `origin/main`
- Head: `5047b15b413d49e51d71d27fb495456b438b87a3`
- Context: Release-candidate hardening continued. Repo-side guards are stronger, but real release execution is still blocked by missing Apple/GitHub release credentials.

## Done This Session

- Re-audited release workflow, verify workflow, helper scripts, package script, and release docs.
- Tightened `.github/workflows/release-candidate.yml`:
  - `Validate release secrets` now runs `script/configure_release_secrets.sh --dry-run`.
  - The Developer ID `.p12` import step now verifies the decoded certificate file is non-empty before creating/importing into the signing keychain.
- Tightened `.github/workflows/release-candidate-verify.yml`:
  - Release artifact listing now uses `gh api --paginate` before filtering for `flowline-release-vX.Y.Z`.
- Tightened `script/configure_release_secrets.sh`:
  - Validates that `FLOWLINE_DEVELOPER_ID_IDENTITY` includes the same `APPLE_TEAM_ID` in parentheses.
- Tightened `script/package_release.sh`:
  - Local `--notarize` with Apple ID credentials now rejects a Developer ID identity whose Team ID does not match `APPLE_TEAM_ID` before starting `swift build`.
- Updated `docs/RELEASE.md`:
  - Uses consistent `TEAMID1234` placeholders.
  - Documents CI helper validation and the signing/notary Team ID match requirement.
- Expanded release tests:
  - Secret helper: 8 focused tests.
  - Notarization credential guard: 4 focused tests.
  - Release candidate workflow: 6 focused tests.
  - Full suite now has 291 tests.

## Current State

- Working tree is dirty with release-helper, workflow, package, docs, tests, and checkpoint work:
  - `.github/workflows/release-candidate.yml`
  - `.github/workflows/release-candidate-verify.yml`
  - `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`
  - `docs/RELEASE.md`
  - `docs/checkpoints/LATEST.md`
  - `docs/checkpoints/2026-05-24-1810.md`
  - `docs/checkpoints/2026-05-24-1821.md`
  - `docs/checkpoints/2026-05-24-1828.md`
  - `docs/checkpoints/2026-05-24-1836.md`
  - `docs/checkpoints/2026-05-24-1900.md`
  - `docs/checkpoints/2026-05-24-1910.md`
  - `docs/checkpoints/2026-05-24-1919.md`
  - `docs/checkpoints/2026-05-24-1929.md`
  - `script/configure_release_secrets.sh`
  - `script/package_release.sh`
  - `script/run_release_candidate.sh`
- Source-side release gates are passing after a rerun.
- No release-candidate workflow has been dispatched from this dirty/unpushed worktree.
- No notarized release artifact exists yet from this helper flow.

## Important Files / Artifacts

- `script/configure_release_secrets.sh`: validates/configures/checks release GitHub Actions secrets; rejects repo mismatch, paginates secret-name checks, rejects conflicting modes, and validates signing/notary Team ID match.
- `script/package_release.sh`: release packaging/notarization path; now rejects Apple ID Team ID mismatch with the signing identity before build.
- `script/run_release_candidate.sh`: guarded workflow dispatch helper for `release-candidate.yml`.
- `.github/workflows/release-candidate.yml`: notarized candidate workflow; uses the secret helper for CI-side input validation and checks decoded `.p12` is non-empty.
- `.github/workflows/release-candidate-verify.yml`: candidate artifact provenance workflow; paginates artifact listing.
- `docs/RELEASE.md`: documents secret setup, guarded dispatch, CI helper validation, candidate verification, and publish preflight.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: release helper, runner, workflow, verify workflow, package, preflight, and secret-scan coverage.
- `docs/checkpoints/2026-05-24-1929.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: updated to this checkpoint.

## Verification

- Command: `rtk swift test --filter releaseSecretSetup`
  - Result: passed; 8 focused secret-helper tests passed.
- Command: `env ... script/configure_release_secrets.sh --dry-run`
  - Result: passed with dummy non-secret values; workflow-style no-`--repo` dry-run resolved `kingkyylian/flowline` from `origin`.
- Command: `env ... script/configure_release_secrets.sh --repo kingkyylian/flowline --dry-run`
  - Result: expected failure for mismatched identity/team; rejected with `FLOWLINE_DEVELOPER_ID_IDENTITY must include APPLE_TEAM_ID`.
- Command: `rtk swift test --filter notarizeRelease`
  - Result: passed; 4 focused notarization guard tests passed.
- Command: `rtk swift test --filter releaseCandidateWorkflow`
  - Result: passed; 6 release-candidate workflow tests passed.
- Command: `rtk swift test --filter releaseCandidateVerifyWorkflow`
  - Result: passed earlier this session; 4 verify workflow tests passed.
- Command: `rtk swift test`
  - Result: first run had one transient `WaitTimeoutError` in `musicControlTogglesCurrentPlayerStateWithoutTrustingStaleSnapshot`; the test passed alone, then the full suite passed on rerun with `Test run with 291 tests in 0 suites passed after 19.894 seconds`.
- Command: `rtk swift build -c release`
  - Result: passed; release build complete.
- Command: `script/secret_scan.sh`
  - Result: passed.
- Command: `git diff --check`
  - Result: passed.
- Command: `bash -n script/package_release.sh script/configure_release_secrets.sh script/run_release_candidate.sh`
  - Result: passed.
- Command: `script/configure_release_secrets.sh --repo kingkyylian/flowline --check`
  - Result: expected failure; all 7 required GitHub Actions release secret names are missing.
- Command: `rtk security find-identity -v -p codesigning`
  - Result: only `Apple Development: bwib3927@outlook.com (G953CU4G2L)` is available; no `Developer ID Application` identity is installed.

## Open Questions / Risks

- The release-candidate workflow still requires these repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- Local notarized packaging still requires an installed `Developer ID Application` identity, or CI must import it from the configured certificate secret.
- Helper/workflow/package/test/docs/checkpoint changes are not committed or pushed.
- A real release candidate still requires a clean pushed commit, configured secrets, successful `Release Candidate`, successful `Release Candidate Verify`, and publish preflight evidence against the uploaded notarized artifact.
- One unrelated music-control test timed out once in the full suite, then passed alone and in a full rerun; watch for recurrence.

## Next Steps

1. Commit and push the release helper/workflow/package/test/docs/checkpoint changes after explicit approval.
2. Provide the Developer ID `.p12`, certificate password, Developer ID identity string, Apple ID, Team ID, and app-specific password.
3. Run `script/configure_release_secrets.sh --repo kingkyylian/flowline --dry-run`, then run it without `--dry-run`, then run `--check`.
4. Run `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0`.
5. Let `Release Candidate Verify` complete, then preserve the publish preflight evidence against the uploaded notarized artifact before creating or pushing the public release tag.

## Project Rules For Resume

- Use `rtk` for noisy shell commands.
- Keep Turkish communication short and concrete.
- Protect dirty worktree state.
- Do not commit or push without explicit user approval.
- Do not claim release completion without fresh verification against current files, GitHub state, and produced artifacts.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short --branch`, `docs/checkpoints/LATEST.md`, `script/configure_release_secrets.sh`, `script/package_release.sh`, `script/run_release_candidate.sh`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `docs/RELEASE.md`, and GitHub Actions secret state. Do not run the `Release Candidate` workflow until `script/configure_release_secrets.sh --repo kingkyylian/flowline --check` passes and the helper/workflow/package changes are committed/pushed.
