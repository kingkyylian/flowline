# Project Checkpoint - 2026-05-20 17:12 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and tracking `origin/main`
- Latest implementation commit: `efcbbe8 Add publish secret scan`
- Remote: `origin -> https://github.com/kingkyylian/flowline.git`
- Context: Publish is now guarded by a local secret scan and the latest `main` is pushed to GitHub.

## Done This Session

- Restored from the previous checkpoint and inspected current git state.
- Finished the next target after the GitHub push-protection incident:
  - Added `script/secret_scan.sh`.
  - Scans both current project files and reachable git history.
  - Detects high-risk Google OAuth credential signatures before publish.
- Wired the secret scan into `script/publish_preflight.sh`.
- Updated publish preflight remote checking so Codex runs use `rtk git ls-remote` when `rtk` is present.
- Added test coverage for:
  - rejecting OAuth client secret signatures in the worktree,
  - rejecting OAuth client ID signatures in the worktree,
  - rejecting credential signatures present only in reachable git history,
  - keeping existing publish preflight failure behavior for missing/unreachable remotes.
- Updated `README.md` to document the new secret scan script.
- Amended the secret-scan commit and pushed `main` to `origin`.

## Current State

- Working tree is clean.
- `main` is synchronized with `origin/main`.
- Latest implementation commit is `efcbbe8 Add publish secret scan`; this checkpoint records that pushed state.
- Publish preflight now passes when run with network access.
- Release archive/notarization is still blocked by missing Developer ID/notary configuration.

## Important Files / Artifacts

- `script/secret_scan.sh`: standalone pre-publish scan for high-risk credential signatures in worktree and reachable history.
- `script/publish_preflight.sh`: now requires a clean tree, runs the secret scan, and verifies GitHub origin reachability.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: regression tests for publish preflight and secret scan behavior.
- `README.md`: documents `script/secret_scan.sh` under release/publish checks.
- `docs/checkpoints/2026-05-20-1712.md`: this checkpoint.

## Verification

- Command: `rtk bash -n script/secret_scan.sh script/publish_preflight.sh`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 8 tests.
- Command: `rtk swift test`
  - Result: passed, 217 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/publish_preflight.sh`
  - Result: passed with network access; secret scan passed and GitHub origin was reachable.
- Command: `rtk git push origin main`
  - Result: passed; `main` pushed.

## Open Questions / Risks

- The historical OAuth credential should still be treated as exposed if it was real. Rotate or revoke it in Google Cloud outside this repo.
- Developer ID Application certificate is still not installed, so `script/package_release.sh --preflight` cannot pass for release archive/notarization yet.
- Notarization still needs `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.

## Next Steps

1. Rotate/revoke the exposed Google OAuth credential if it was real.
2. Install/select a Developer ID Application identity and set `FLOWLINE_DEVELOPER_ID_IDENTITY`.
3. Run `script/package_release.sh --preflight`, then proceed with archive/notarization once signing prerequisites exist.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -8`, `docs/checkpoints/LATEST.md`, and `script/publish_preflight.sh`. The publish blocker is resolved and guarded; the remaining release blocker is Developer ID/notary configuration plus external credential rotation if the old OAuth credential was real.
