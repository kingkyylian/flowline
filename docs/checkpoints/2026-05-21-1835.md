# Project Checkpoint - 2026-05-21 18:35 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest implementation commit:
  - `51cb1ec Require release candidate workflow provenance`
- Context: Continued autonomous release hardening by requiring release artifact provenance to come from the `Release Candidate` workflow.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current head/CI were clean.
- Selected the next secret-free release hardening target:
  - `script/publish_preflight.sh --require-artifact` already checked artifact name, archive hash, run status, run head, repository, and tag alignment.
  - It still accepted a manifest whose `github_workflow` was not `Release Candidate`.
  - That meant an artifact from another successful workflow on the same commit could satisfy the artifact gate if the artifact name and archive hash matched.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `publishPreflightRejectsCIManifestWhenArtifactWorkflowIsNotReleaseCandidate` first failed because `github_workflow=CI` passed `--require-artifact`.
  - Existing artifact-provenance positive fixtures now use `github_workflow=Release Candidate`.
- Updated `script/publish_preflight.sh`:
  - Reads `github_workflow` from the release manifest.
  - Requires `github_workflow` to equal `Release Candidate` when `--require-artifact` is used.
  - Keeps `--require-ci` alone compatible with generic successful CI provenance.
- Updated `README.md` and `docs/RELEASE.md` to document that the stricter artifact gate requires the `Release Candidate` workflow.
- Pushed implementation commit `51cb1ec`.
- GitHub Actions run `26235933206` passed for `51cb1ec`.

## Current State

- The release hardening implementation is pushed at `51cb1ecd642f6f66589dab0cac72808e76478b44`.
- Working tree was clean before writing this checkpoint.
- Release identity/provenance checks now cover:
  - exact candidate artifact names;
  - artifact version to archive/tag binding;
  - manifest GitHub artifact name to release tag binding;
  - manifest workflow binding to `Release Candidate` for artifact provenance;
  - ambiguous downloaded archives in both the verify workflow and publish preflight;
  - CI run head/status matching;
  - release manifest archive name, version, checksum, size, and git commit.

## Important Files / Artifacts

- `script/publish_preflight.sh`: now requires `github_workflow=Release Candidate` for `--require-artifact`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers the workflow-provenance invariant and release provenance tests.
- `README.md`: documents the stricter artifact gate.
- `docs/RELEASE.md`: documents the stricter release artifact gate.
- `.github/workflows/release-candidate.yml`: produces the expected `Release Candidate` workflow provenance and artifact name.
- `.github/workflows/release-candidate-verify.yml`: runs publish preflight with `--require-ci --require-artifact`.

## Verification

- Command: `rtk swift test --filter ArtifactWorkflowIsNotReleaseCandidate`
  - Result: first failed against the old script because `github_workflow=CI` passed the artifact gate, then passed after enforcing `Release Candidate`.
- Command: `rtk swift test --filter 'publishPreflight|releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 62 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 271 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26235933206 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `51cb1ec`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `51cb1ec`; publish preflight now requires manifest `github_workflow=Release Candidate` when `--require-artifact` is used.
