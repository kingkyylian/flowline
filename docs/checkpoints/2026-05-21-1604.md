# Project Checkpoint - 2026-05-21 16:04 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest implementation commit:
  - `16a6d78 Verify release signing identity before notary`
- Context: Continued autonomous release hardening by making the manual Release Candidate workflow verify the imported Developer ID identity before storing Apple notary credentials.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current head/CI were clean.
- Selected the next secret-free release hardening target:
  - The workflow imported the Developer ID certificate and then immediately stored notary credentials.
  - `script/package_release.sh --notarize` would eventually validate the signing identity, but only after notary credentials had already been stored.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `releaseCandidateWorkflowVerifiesSigningIdentityBeforeUsingNotaryCredentials`
- Updated `.github/workflows/release-candidate.yml`:
  - Added `Verify release signing identity` after `Import Developer ID certificate`.
  - The new step runs `script/package_release.sh --preflight` with `FLOWLINE_DEVELOPER_ID_IDENTITY`.
  - The step runs before `Store notary credentials`.
- Pushed implementation commit `16a6d78`.
- GitHub Actions run `26227541285` passed for `16a6d78`.

## Current State

- The release hardening implementation is pushed at `16a6d78a85aab95f31f4d66a49ebecd292c671db`.
- Working tree was clean before writing this checkpoint.
- Release candidate workflow now has ordered pre-secret and pre-notary gates:
  - release metadata and tag validation;
  - local/origin tag collision check;
  - repository publish preflight before release secret validation;
  - Developer ID certificate import with scoped key access;
  - signing identity preflight before notary credential storage.
- Existing release protections remain in place:
  - release candidate verification rejects ambiguous artifacts and archives;
  - publish preflight verifies CI and artifact provenance for the produced archive.

## Important Files / Artifacts

- `.github/workflows/release-candidate.yml`: verifies imported signing identity before notary credential storage.
- `.github/workflows/release-candidate-verify.yml`: rejects ambiguous release candidate artifacts and archives.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers release workflow security invariants.
- `script/package_release.sh`: `--preflight` validates Developer ID identity, icon, and release metadata without requiring notary credentials.

## Verification

- Command: `rtk swift test --filter SigningIdentityBeforeUsingNotaryCredentials`
  - Result: first failed against the old workflow, then passed after adding the signing preflight step.
- Command: `rtk swift test --filter 'releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 58 tests.
- Command: `rtk ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/ci.yml .github/workflows/release-candidate.yml .github/workflows/release-candidate-verify.yml`
  - Result: passed.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 267 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26227541285 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `16a6d78`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `16a6d78`; release candidate now validates the imported Developer ID signing identity before storing Apple notary credentials.
