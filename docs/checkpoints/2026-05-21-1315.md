# Project Checkpoint - 2026-05-21 13:15 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commits before this checkpoint:
  - `7e822d3 Validate Apple Team ID before notarizing`
  - `6b0860b Cover Apple ID notarization release path`
- Context: Continued autonomous release hardening and release test-suite maintenance.

## Done This Session

- Confirmed `docs/checkpoints/LATEST.md` was stale at `a43d442` while repo HEAD had advanced to `7e822d3`.
- Added Apple Team ID validation coverage and implementation before this checkpoint:
  - `script/package_release.sh` now rejects invalid Apple ID Team IDs before build when `--notarize` uses Apple ID credentials.
  - `Tests/FlowlineAppTests/PermissionSurfaceTests.swift` covers invalid Team IDs and proves fake `swift` is not reached.
- Added positive fake-tool coverage for the Apple ID notarization branch:
  - `notarizedReleaseSubmitsAppleIDCredentialsWhenProfileIsNotConfigured`
  - Verifies `xcrun notarytool submit` receives `--apple-id`, `--team-id`, `--password`, and `--wait`.
  - Verifies staple, Gatekeeper assessment, and final re-zip ordering remains intact.
- Extracted shared release packaging test setup into `ReleasePackagingFixture`:
  - Used by archive metadata/plist coverage.
  - Used by profile-based notarization coverage.
  - Used by Apple ID notarization coverage.
- Investigated a full-suite-only failure in `appStatePublishesAutoCapturedClipboardTextIntoSnapshot`:
  - Isolated test passed quickly.
  - Full suite failed because the prior 0.45s polling window was too tight under parallel load.
  - Aligned it with the existing clipboard timer tolerance by using a 1.0s deadline and 25ms polling.
- Pushed `6b0860b`; GitHub Actions run `26219729261` passed.

## Current State

- Working tree was clean after pushing `6b0860b` and before writing this checkpoint.
- `main` and `origin/main` were aligned at `6b0860b0fdf76867c6e79f69e9c90cb231cd3588`.
- GitHub Actions is green on current implementation head:
  - Run: `26219729261`
  - Head: `6b0860b0fdf76867c6e79f69e9c90cb231cd3588`
  - Result: `success`
  - Job: `Swift tests`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.
- Local full test suite passes with 235 tests.

## Important Files / Artifacts

- `script/package_release.sh`: validates Apple ID Team ID format before build for Apple ID notarization.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: release packaging fixture plus profile and Apple ID notarization coverage.
- `Tests/FlowlineAppTests/AppStateHoldDropFeedbackTests.swift`: less flaky AppState clipboard publish wait window.
- `docs/checkpoints/2026-05-21-1315.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: should match this checkpoint.

## Verification

- Command: `rtk swift test --filter notarizeReleaseRejectsInvalidAppleTeamIDBeforeBuild`
  - Result: first failed correctly before implementation because fake `swift` ran; passed after implementation.
- Command: `rtk swift test --filter notarizedReleaseSubmitsAppleIDCredentialsWhenProfileIsNotConfigured`
  - Result: first exposed a test-fixture newline logging issue; passed after fixture correction.
- Command: `rtk swift test --filter notarizedReleaseAssessesStapledBundleBeforeFinalZip`
  - Result: passed after fixture refactor.
- Command: `rtk swift test --filter releaseArchiveLintsPlistAndBuildsBundleWithMetadata`
  - Result: passed after fixture refactor.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 26 tests.
- Command: `rtk swift test --filter appStatePublishesAutoCapturedClipboardTextIntoSnapshot`
  - Result: passed after diagnosing the full-suite timing failure.
- Command: `rtk swift test`
  - Result: passed, 235 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk ./script/publish_preflight.sh`
  - Result: passed after push; remote HEAD matched `6b0860b`.
- Command: `rtk gh run watch 26219729261 --repo kingkyylian/flowline --exit-status`
  - Result: passed for `6b0860b`.

## Open Questions / Risks

- Historical Google OAuth credential still needs external rotation/revocation if it was real.
- Real release packaging/notarization still requires external Apple credentials:
  - Developer ID Application certificate.
  - `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.
- Fake-tool coverage now covers profile-based notarization, Apple ID notarization, missing credentials, blank Apple ID credentials, invalid Team ID, plist linting, stapling, assessment, and final re-zip.
- The real notarization service and Gatekeeper assessment still need one end-to-end run with real credentials before a public release.

## Next Steps

1. Run one real notarized release when Apple Developer ID and notary credentials are available.
2. Continue reducing older release packaging test duplication only when touching that area again.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `script/package_release.sh`, `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`, and the latest GitHub Actions run for `main`. CI is green on `6b0860b`; Apple ID notary credentials are now validated before build and the Apple ID submit path has fake-tool coverage. Follow project rules from `AGENTS.md`: use `rtk`, preserve unrelated changes, and verify before claiming completion.
