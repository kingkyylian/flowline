# Project Checkpoint - 2026-05-24 19:00 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, synchronized with `origin/main`
- Head: `5047b15b413d49e51d71d27fb495456b438b87a3`
- Context: Release-candidate execution is still blocked by missing external Apple/GitHub credentials, but the guarded dispatch helper now also prevents accidentally dispatching a release candidate workflow to a repo that does not match local `origin`.

## Done This Session

- Reviewed the release helper diff for remaining release-risk gaps.
- Found and fixed a repo mismatch risk in `script/run_release_candidate.sh`:
  - Before: `--repo` could point at a different GitHub repository while tag checks still used local `origin`.
  - Now: explicit `--repo` must match GitHub `origin`, otherwise the script exits before branch/tag/preflight/dispatch.
- Added test coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - Static guard coverage now checks the repo/origin mismatch error is present.
  - New fake `git`/fake `gh` test proves mismatch exits before any `gh workflow run`.
- Re-ran live guard checks:
  - `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0 --dry-run` still refuses to proceed while the worktree is dirty.

## Current State

- Working tree is dirty with release-helper, release-runner, docs, tests, and checkpoint work:
  - `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`
  - `docs/RELEASE.md`
  - `docs/checkpoints/LATEST.md`
  - `docs/checkpoints/2026-05-24-1810.md`
  - `docs/checkpoints/2026-05-24-1821.md`
  - `docs/checkpoints/2026-05-24-1828.md`
  - `docs/checkpoints/2026-05-24-1836.md`
  - `docs/checkpoints/2026-05-24-1900.md`
  - `script/configure_release_secrets.sh`
  - `script/run_release_candidate.sh`
- Source-side release gates are passing.
- A real notarized release candidate still cannot be produced until the external credentials are supplied and configured.

## Important Files / Artifacts

- `script/configure_release_secrets.sh`: validates/configures/checks release GitHub Actions secrets.
- `script/run_release_candidate.sh`: guarded release workflow dispatch helper; now rejects `--repo` mismatch with `origin`.
- `docs/RELEASE.md`: documents secret setup and guarded dispatch.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: tests release helper, dispatch guard behavior, and repo mismatch rejection.
- `docs/checkpoints/2026-05-24-1900.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: updated to this checkpoint.

## Verification

- Command: `rtk swift test --filter releaseCandidateRunScript`
  - Result: passed; 3 focused release-runner tests passed.
- Command: `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0 --dry-run`
  - Result: expected failure; stopped at `script/publish_preflight.sh` because the worktree has uncommitted changes.
- Command: `script/secret_scan.sh`
  - Result: passed.
- Command: `git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed; `Test run with 286 tests in 0 suites passed after 13.746 seconds`.
- Command: `rtk swift build -c release`
  - Result: passed; release build complete.
- Command: `git status --short --branch`
  - Result: branch is synchronized with `origin/main`; helper/test/docs/checkpoint files are uncommitted.

## Open Questions / Risks

- The release-candidate workflow still requires these repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- Local notarized packaging still requires an installed `Developer ID Application` identity, or CI must import it from the configured certificate secret.
- Helper/test/docs/checkpoint changes are not committed or pushed.

## Next Steps

1. Commit and push the release helper/test/docs/checkpoint changes if they should become part of the release process.
2. Provide the Developer ID `.p12`, certificate password, Developer ID identity string, Apple ID, Team ID, and app-specific password.
3. Run `script/configure_release_secrets.sh --repo kingkyylian/flowline --dry-run`, then run it without `--dry-run`, then run `--check`.
4. Run `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0`.
5. Let `Release Candidate Verify` complete, then preserve the publish preflight evidence against the uploaded notarized artifact before creating or pushing the public release tag.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short --branch`, `docs/checkpoints/LATEST.md`, `script/configure_release_secrets.sh`, `script/run_release_candidate.sh`, `docs/RELEASE.md`, and GitHub Actions secret state. Do not run the `Release Candidate` workflow until `script/configure_release_secrets.sh --repo kingkyylian/flowline --check` passes and the worktree is clean/pushed.
