# Project Checkpoint - 2026-05-09 18:59 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, dirty worktree with 22 modified files and 4 untracked files
- Context: Release-readiness pass after notch/settings polish, AI usage parsing, music controls, and macOS packaging audit.

## Done This Session

- Kept collapsed notch Codex usage metrics visible at low opacity when not hovering, stronger on hover.
  - Added `CollapsedPillUsageMetricsStyle`.
  - Added `Tests/FlowlineAppTests/CollapsedPillViewTests.swift`.
- Cleaned Settings UI visual framing:
  - Removed pale card fills/strokes around permission health and settings blocks.
  - Removed selected-sidebar gray rectangle while preserving the left selection indicator.
  - Removed row divider lines in all settings tabs.
  - Removed sidebar/content vertical separator.
- Audited release readiness across code, packaging, signing, docs, and manual QA.
- Found current `dist/Flowline.app` is local-dev runnable but not distribution-ready:
  - `codesign --verify --deep --strict` reported `CSSMERR_TP_NOT_TRUSTED`.
  - Current dev script packages `.build/debug`.
  - Existing dev signing does not prove Developer ID / hardened runtime / notarization readiness.
- Added release packaging workflow:
  - `script/package_release.sh`: release build, Developer ID identity requirement, hardened runtime, timestamp, zip archive, optional notarization/staple.
  - `docs/RELEASE.md`: release blockers, notarization commands, validation commands, manual QA checklist.
  - `README.md`: release section pointing to the release checklist.
- Tightened privacy/lifecycle behavior:
  - `CalendarService` now starts only when Calendar module is enabled.
  - `ShelfService` now starts only when Shelf module is enabled.
  - Disabling Shelf stops clipboard polling and clears session shelf items.
  - `Show over fullscreen` now persists via `UserDefaults`.
- Optimized Codex usage parsing:
  - `AIUsageService` now reads the newest usable Codex rate-limit event by scanning newest session files and lines from the end instead of carrying latest state across all logs.

## Current State

- App builds, tests pass, release build passes, and local dev bundle launches.
- Worktree is intentionally dirty; changes are not committed.
- Release packaging script is added but real notarization has not been run because Developer ID / notary credentials are not available in this session.
- `docs/checkpoints/2026-05-08-1849.md` is still untracked from the prior checkpoint.
- `docs/checkpoints/LATEST.md` points to this checkpoint.

## Important Files / Artifacts

- `script/package_release.sh`: new release packaging/notarization entrypoint.
- `docs/RELEASE.md`: release checklist and manual QA matrix.
- `README.md`: release workflow docs.
- `Sources/FlowlineApp/Stores/AppState.swift`: module lifecycle and persisted fullscreen behavior.
- `Sources/FlowlineApp/Services/AIUsageService.swift`: fresh cache handling and faster Codex session event parsing.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`: stripped-down settings visual treatment.
- `Sources/FlowlineApp/Views/Overlay/CollapsedPillView.swift`: collapsed usage metric visibility behavior.
- `Tests/FlowlineAppTests/CollapsedPillViewTests.swift`: regression test for collapsed metric opacity.
- `Sources/FlowlineApp/Models/NotchMetrics.swift`: current notch geometry constants.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`: notch workspace/limits/music layout.
- `Sources/FlowlineApp/Views/Overlay/MusicOverlayViews.swift`: compact music player layout.

## Verification

- Command: `rtk bash -n script/package_release.sh`
- Result: passed.
- Command: `rtk ./script/package_release.sh --help`
- Result: passed.
- Command: `rtk swift test`
- Result: passed, 40 tests.
- Command: `rtk swift build -c release`
- Result: passed.
- Command: `rtk git diff --check`
- Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
- Result: passed; Flowline was built, signed for local development, and running.
- Command: `rtk codesign --verify --deep --strict --verbose=2 dist/Flowline.app`
- Result: failed before release fixes with `CSSMERR_TP_NOT_TRUSTED`; this confirms the local dev bundle is not release-signed.

## Open Questions / Risks

- Real release blocker: run `script/package_release.sh --notarize` with a valid `Developer ID Application` identity and notary credentials/profile.
- Validate notarized app with:
  - `codesign --verify --deep --strict --verbose=2 dist/release/Flowline.app`
  - `spctl -a -vv dist/release/Flowline.app`
  - `xcrun stapler validate dist/release/Flowline.app`
- Manual QA still required on clean user account or clean machine:
  - Accessibility permission prompt and granted-state refresh.
  - Calendar disabled does not prompt and does not poll.
  - Shelf disabled does not poll clipboard and clears session shelf.
  - Spotify and Apple Music automation prompts and controls.
  - Safari audio is not paused by Flowline controls.
  - Notch mode on notched display and companion mode on non-notched display.
  - Fullscreen behavior with setting off/on.
  - Launch at login from `/Applications/Flowline.app`.
- App icon / DMG polish / Homebrew cask are not implemented.
- Current dirty diff should be reviewed and possibly split before commit.

## Next Steps

1. Obtain/select Developer ID Application identity and notary profile, then run `script/package_release.sh --notarize`.
2. Run the release validation commands in `docs/RELEASE.md` and do clean-user manual QA.
3. Review the dirty diff, split if needed, commit the UX/release-readiness work, then tag a release candidate.

## Project Rules For Resume

- Read `AGENTS.md` and referenced user workflow docs before broad changes.
- Use `rtk` command wrappers for build/test/run flows.
- Prefer `rg` for search.
- Use `apply_patch` for manual file edits.
- Do not revert existing dirty changes unless explicitly requested.
- Before claiming done, run fresh verification.

## Resume Prompt

Continue from this checkpoint. First read this file and project `AGENTS.md`, then inspect the listed artifacts before making changes. Start with Developer ID/notarization setup if release packaging is the goal; otherwise review and split the dirty diff before committing.
