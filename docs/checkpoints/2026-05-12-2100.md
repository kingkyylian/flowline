# Project Checkpoint - 2026-05-12 21:00 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with active Flowline notch/HOLD/LIMITS/module changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: HOLD side-slot is polished; screenshot drag-to-open works; clipboard text capture is faster; Calendar toggle now has visible status.

## Done This Session

- Fixed Calendar module “on/off does nothing” UX:
  - root cause: Calendar only affected `nextEvent` and `Join` action generation; with no upcoming event or visible action row, toggling had no visible overlay difference
  - added `CalendarModulePresentation`
  - Settings module detail now shows real state: `Meeting actions`, `Needs permission`, `No event`, or `Next <time>`
  - center `LIMITS` header now shows a compact calendar chip when Calendar is enabled:
    - `ASK` when permission is needed
    - `IDLE` when enabled with no upcoming event
    - event time when a next event exists
- Fixed screenshot drag-to-open regression earlier in this batch:
  - `OverlayController` reads `NSPasteboard.Name.drag` during collapsed drag events
  - panel expands only for real HOLD payloads
  - ordinary Terminal/window drags do not open the panel
- Made automatic HOLD capture user-controlled:
  - `Clipboard text` default off
  - `Screenshots` default on
  - Settings > Behavior has `Hold capture` toggles
- Reduced clipboard text capture latency:
  - pasteboard polling split to fast `0.2s` timer
  - screenshot directory fallback polling stays at `1.0s`
  - timers run in `.common` run loop mode

## Current State

- Flowline has been rebuilt, signed, relaunched, and verified running after the Calendar UX fix.
- Worktree is still dirty and uncommitted.
- `docs/checkpoints/LATEST.md` points to this checkpoint.

## Important Files / Artifacts

- `Sources/FlowlineApp/Support/CalendarModulePresentation.swift`
  - Calendar settings detail and center chip presentation.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`
  - Calendar row detail now reflects actual state instead of static `Center actions`.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`
  - center `LIMITS` header shows calendar status chip when enabled.
- `Tests/FlowlineAppTests/CalendarModulePresentationTests.swift`
  - covers off, permission, idle, and next-event states.
- `Sources/FlowlineApp/Services/ShelfService.swift`
  - fast pasteboard timer and auto-capture options.
- `Sources/FlowlineApp/App/OverlayController.swift`
  - collapsed notch drag expansion uses drag pasteboard detection.
- `Sources/FlowlineApp/Support/HoldDropPayloadDetector.swift`
  - shared HOLD payload detector for drag/drop.

## Verification

- Command: `rtk swift test --filter CalendarModulePresentationTests`
  - Result: failed first before presentation layer existed; passed after implementation, 4 tests.
- Command: `rtk swift test`
  - Result: passed; 153 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.
- Physical validation:
  - user confirmed screenshot drag now opens the panel and drop works.

## Open Questions / Risks

- User should physically retest Calendar toggle: expected center chip appears/disappears.
- User should physically retest `Clipboard text` enabled: copied text should land quickly.
- User should verify screenshot auto-capture with a newly created screenshot.
- The worktree is large; stage related files carefully before commit.

## Next Steps

1. Physically test Calendar on/off and observe `ASK`/`IDLE`/time chip in the center header.
2. Physically test fast clipboard text capture with `Clipboard text` enabled.
3. Commit the stabilized HOLD/LIMITS/module batch after physical validation.

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For behavior changes, prefer test-first and keep changes scoped.
- Do not revert unrelated user changes in the dirty worktree.

## Resume Prompt

Continue from this checkpoint. First read this file and the project instructions, then physically validate Calendar toggle visibility, fast clipboard text capture, and new screenshot auto-capture before committing.
