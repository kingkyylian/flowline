# Project Checkpoint - 2026-05-08 18:49 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, dirty worktree with 18 modified files
- Context: Expanded notch UX pass focused on settings cleanup, music controls, usage limits, and precise notch-safe layout.

## Done This Session

- Simplified settings direction to focus on Permissions and Behavior, without the older yellow-accent visual direction.
- Fixed Accessibility permission display path so granted access is rechecked instead of staying stuck on `NEEDS ACCESS`.
- Reworked expanded notch content into three focused areas:
  - `WORKSPACE`: app/repo context and readiness.
  - `LIMITS`: Codex/Claude/Gemini usage.
  - `MUSIC`: track metadata, timeline, elapsed/duration, and transport controls.
- Rebuilt music UI toward the Boring Notch-inspired compact player:
  - Wider music area.
  - Track title and artist.
  - Live elapsed counter and duration.
  - Progress bar.
  - Previous / play-pause / next controls.
- Fixed music elapsed time so it advances while playing and stays stable while paused.
- Changed music controls to target Spotify / Apple Music style app controllers instead of broad global media-key behavior.
  - Removed the global media key fallback that could pause Safari audio.
  - Switched play/pause commands to state-aware AppleScript actions.
- Removed most visible light gray framing/backgrounds from expanded notch:
  - Notch base is black.
  - Controls/text stay white or gray.
  - Button backgrounds are clear in notch mode.
- Fixed stale AI limit readings:
  - Old web cache entries are ignored after a freshness window.
  - Codex usage is read from local session JSONL `rate_limits` events.
  - Codex session data overrides stale cache data.
- Added repository name support for workspace display.
- Iterated expanded notch geometry:
  - `Workspace` column widened.
  - `Limits` compressed and dropped below the physical notch safe area.
  - `Music` shifted right/down and timeline shortened to avoid physical notch overlap.

## Current State

- App builds and runs.
- Tests pass.
- Worktree is intentionally dirty; changes have not been committed.
- Latest notch layout constants are in `Sources/FlowlineApp/Models/NotchMetrics.swift`.
- Latest visual issue addressed: `MUSIC` header was still near the physical notch, so the utility/music column is shifted right and down.

## Important Files / Artifacts

- `Sources/FlowlineApp/Models/NotchMetrics.swift`: central notch sizing, column widths, offsets, and music timeline width.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`: expanded notch layout, Workspace/Limits/Music columns.
- `Sources/FlowlineApp/Views/Overlay/MusicOverlayViews.swift`: compact music player, progress/timer row, transport controls.
- `Sources/FlowlineApp/Views/Overlay/OverlayRootView.swift`: expanded notch top/bottom content insets and overlay shell.
- `Sources/FlowlineApp/Services/MusicControlService.swift`: state-aware app-specific music control.
- `Sources/FlowlineApp/Services/MusicPlaybackRefreshWorker.swift`: playback refresh and command refresh behavior.
- `Sources/FlowlineApp/Services/AIUsageService.swift`: fresh cache handling and Codex session usage parsing.
- `Sources/FlowlineCore/Models/MusicPlaybackSnapshot.swift`: live playback progress/timing helpers.
- `Sources/FlowlineCore/Models/CodexUsageSnapshot.swift`: Codex usage parsing/model support.
- `Tests/FlowlineAppTests/MusicPlaybackRefreshWorkerTests.swift`: music command/refresh behavior coverage.
- `Tests/FlowlineCoreTests/CodexUsageParserTests.swift`: Codex limit parsing coverage.
- `Tests/FlowlineCoreTests/MusicPlaybackSnapshotTests.swift`: elapsed/duration/progress coverage.

## Verification

- Command: `rtk swift test`
- Result: passed, 39 tests.
- Command: `rtk git diff --check`
- Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
- Result: passed; Flowline was built, signed, and running.

## Open Questions / Risks

- Final visual tuning still depends on real screenshots against the physical notch; current offsets may need one more pass.
- Music controls should be manually checked with Spotify and Apple Music separately.
- AI usage values should be checked after more live Codex sessions to confirm the cache/session precedence feels correct.
- Current changes are uncommitted; avoid reverting unrelated edits.

## Next Steps

1. Inspect the latest expanded notch screenshot and fine-tune `utilityColumnShiftX`, `utilityColumnDrop`, `centerColumnDrop`, and `musicTimelineWidth` if needed.
2. Manually verify music controls with Spotify playing, Safari audio playing, and Apple Music if available.
3. Review the dirty diff, split any oversized changes if needed, then commit once UI and behavior are approved.

## Project Rules For Resume

- Read `AGENTS.md` and the referenced user workflow docs before making broad changes.
- Use `rtk` command wrappers for build/test/run flows.
- Prefer `rg` for search.
- Use `apply_patch` for manual file edits.
- Do not revert user or existing dirty worktree changes unless explicitly requested.

## Resume Prompt

Continue from this checkpoint. First read this file and project `AGENTS.md`, then inspect the listed artifacts before making changes. Start by checking the current notch screenshot and adjusting only the relevant layout constants unless a behavior bug is reported.
