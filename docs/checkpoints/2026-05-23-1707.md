# Project Checkpoint - 2026-05-23 17:07 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, synchronized with `origin/main` before this checkpoint update
- Latest code commit:
  - `ce96dc1 Keep Hold left only`
- Latest checkpoint commit before this update:
  - `e09eefe Record hold left-only checkpoint`
- Context: Hold is now a true left-only module; live Settings and About QA have been verified after the code/CI pass.

## Done This Session

- Re-read the active checkpoint and current repo state.
- Verified the live Settings > Modules behavior through Accessibility automation:
  - Started from all module toggles off.
  - Pressed `MODULES`.
  - Enabled Workspace through the live toggle.
  - Observed toggle values change to `On,Unavailable,Off,Off`.
  - Observed row text: `WORKSPACE | LEFT | LEFT ONLY | HOLD | SLOT | LEFT ONLY`.
  - Restored Workspace back off afterward; toggles returned to `Off,Off,Off,Off`.
- Verified the live About page link controls:
  - The About page exposes 3 `AXLink` controls.
  - Their live `AXURL` values are:
    - `https://github.com/kingkyylian/flowline`
    - `https://github.com/kingkyylian/flowline/releases`
    - `https://github.com/kingkyylian/flowline/blob/main/LICENSE`
  - Clicking each link through System Events opened the expected URL in Safari.

## Current State

- Working tree was clean before this checkpoint update.
- Flowline is running from `dist/Flowline.app`.
- Settings > Modules no longer leaves Hold available on the right when Workspace occupies the left slot.
- About links are live system links and were verified to open in the default browser.
- No source code changes were needed after commit `ce96dc1`.

## Important Files / Artifacts

- `Sources/FlowlineCore/Models/FlowlineModulePreferences.swift`: declares Hold as left-only at selection-policy level.
- `Sources/FlowlineApp/Stores/AppState.swift`: closes/disables Hold when Workspace claims the left slot and normalizes invalid persisted state.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`: no right-side Hold render path remains.
- `Sources/FlowlineApp/Support/OverlayHoldDropTarget.swift`: no right-side Hold drop target remains.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`: About project rows use SwiftUI `Link("OPEN", destination: url)`.
- `Tests/FlowlineAppTests/SettingsPresentationTests.swift`: guards About URL metadata and `Link` usage.
- `docs/checkpoints/2026-05-23-1627.md`: previous checkpoint, now superseded by this live-QA update.

## Verification

- Command: `git status --short --branch`
  - Result: passed; `## main...origin/main` before editing this checkpoint.
- Command: `rtk pgrep -fl Flowline`
  - Result: passed; running process was `/Users/kyylian/flowline/dist/Flowline.app/Contents/MacOS/Flowline`.
- Command: Accessibility script pressing `MODULES`, toggling Workspace on, dumping module text/toggle state, then restoring Workspace off.
  - Result: passed; after Workspace on, toggles were `On,Unavailable,Off,Off` and Hold showed `SLOT` plus `LEFT ONLY`.
  - Restore result: passed; toggles returned to `Off,Off,Off,Off`.
- Command: Accessibility script dumping About `AXLink` attributes.
  - Result: passed; all 3 expected `AXURL` values were present.
- Command: System Events clicked each About `AXLink`, then `rtk osascript -e 'tell application "Safari" to get URL of every tab of every window'`.
  - Result: passed; Safari showed:
    - `https://github.com/kingkyylian/flowline`
    - `https://github.com/kingkyylian/flowline/releases`
    - `https://github.com/kingkyylian/flowline/blob/main/LICENSE`
- Previous code verification remains current for code commit `ce96dc1`:
  - `rtk swift test`: passed, 279 tests.
  - `rtk git diff --check`: passed.
  - `rtk ./script/secret_scan.sh`: passed.
  - `rtk ./script/build_and_run.sh --verify`: passed.
  - GitHub Actions CI run `26333853762`: passed for `ce96dc1`.
  - GitHub Actions CI run `26333970132`: passed for checkpoint commit `e09eefe`.

## Open Questions / Risks

- Real notarized release candidate remains external-credential dependent:
  - Apple Developer ID signing identity must exist.
  - Notary credentials/secrets must be configured.
- Historical checkpoint files still mention older right-slot Hold work as history; current product behavior is defined by the latest source and this checkpoint.

## Next Steps

1. Commit and push this checkpoint-only update.
2. Confirm CI on the new checkpoint commit.
3. If release work is next, verify repository Apple signing/notary secret availability before attempting a real release-candidate run.

## Project Rules For Resume

- Use `rtk` for noisy shell commands.
- Keep Turkish communication short and concrete.
- Protect dirty worktree state.
- Prefer TDD for behavior changes.
- Do not claim completion without fresh verification.
- No local `AGENTS.md` file exists in the repo; active instructions come from the conversation and user `.codex` files.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -5`, `docs/checkpoints/LATEST.md`, and the latest GitHub Actions run for `main`. The Hold left-only fix is implemented in code commit `ce96dc1`; live Settings Modules and About link QA are verified in this checkpoint. Remaining release-candidate work depends on Apple signing/notary credentials.
