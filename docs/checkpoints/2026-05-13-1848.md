# Project Checkpoint - 2026-05-13 18:48 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with active Flowline notch/HOLD/LIMITS/settings/logo changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: Flowline is moving away from "notch clone" toward a work buffer model: `capture -> hold -> act -> clear`.

## Done This Session

- Fixed false HOLD panel activation while dragging Terminal/windows:
  - Root cause: stale `NSPasteboard(name: .drag)` payload from prior text/screenshot drags was treated as a fresh drag.
  - `OverlayDragActivation` now rejects pasteboard payloads whose change count matches the baseline captured before the drag.
  - `OverlayController` refreshes drag pasteboard baseline on non-drag events and monitor setup.
- Refined HOLD tray actions:
  - Action buttons now use 32 pt dedicated metrics.
  - Right-side HOLD action row uses the full music/utility column width with evenly spaced actions.
  - Tray height stayed stable.
- Improved HOLD auto-capture latency:
  - Clipboard polling cadence moved to low-latency capture.
  - Screenshot directory events now trigger immediate scan plus delayed rescans.
  - Shelf-only snapshot updates avoid unnecessary git refresh.
- Replaced the menubar/status logo path:
  - Added `FlowlineMarkView` and `FlowlineMarkImage`.
  - Current logo shape is the minimal "Broken Y" mark.
  - Menubar icon is now an AppKit template `NSImage`, not a SwiftUI `MenuBarExtra` custom label.
- Fixed menubar Settings not opening:
  - Root cause: after replacing `MenuBarExtra`, the status menu still relied on SwiftUI `showSettingsWindow:`.
  - Added `SettingsWindowController` and wired status menu `Settings` directly to it.
  - Settings window is retained and reopens after close.
- Preserved existing Settings visual design while adding requested structure:
  - Kept the old dark/mono look, uppercase headings, left selection rule, square toggles, and `600x360` window.
  - Sidebar categories are now `General`, `Hold`, `Modules`, `Shortcuts`.
  - `Modules` includes a small same-style `Layout preview`.
  - No broad redesign, native switch conversion, or larger settings window remains.
- Product direction clarified:
  - Notch should remain glance + quick actions.
  - Larger/detail work should move into a future Workbench/popout instead of widening the notch.

## Current State

- Flowline was rebuilt, signed, relaunched, and verified running after the latest settings changes.
- Full Swift test suite passes with 173 tests.
- `rtk git diff --check` passes.
- Worktree remains large and uncommitted with many related feature files and tests.
- No `AGENTS.md` file exists in the repo root, but the session-provided project rules apply.

## Important Files / Artifacts

- `Sources/FlowlineApp/App/FlowlineApp.swift`
  - Uses AppKit `NSStatusItem` for the menubar icon/menu.
  - Settings menu item calls the retained settings window controller.
- `Sources/FlowlineApp/App/SettingsWindowController.swift`
  - AppKit settings window opener; preserves old `600x360` size.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`
  - Old settings visual language retained.
  - Categories split into General/Hold/Modules/Shortcuts.
  - Module layout preview added without changing the theme.
- `Sources/FlowlineApp/Support/SettingsPresentation.swift`
  - Small presentation model for settings categories and module preview items.
- `Sources/FlowlineApp/Views/FlowlineMarkView.swift`
  - SwiftUI Broken Y mark for in-app/collapsed notch usage.
- `Sources/FlowlineApp/Views/FlowlineMarkImage.swift`
  - AppKit template menubar icon image.
- `Sources/FlowlineApp/Support/OverlayDragActivation.swift`
  - Fresh/stale drag pasteboard detection.
- `Sources/FlowlineApp/App/OverlayController.swift`
  - Drag pasteboard baseline tracking.
- `Sources/FlowlineApp/Services/ShelfService.swift`
  - Faster clipboard/screenshot auto-capture behavior.
- `Sources/FlowlineApp/Stores/AppState.swift`
  - Shelf-only snapshot updates can skip git refresh.
- `Tests/FlowlineAppTests/SettingsPresentationTests.swift`
  - Locks focused settings categories and layout preview behavior.
- `Tests/FlowlineAppTests/SettingsWindowControllerTests.swift`
  - Verifies stable settings window construction.
- `Tests/FlowlineAppTests/OverlayDragActivationTests.swift`
  - Covers stale vs fresh global drag pasteboard activation.
- `Tests/FlowlineAppTests/NotchHoldTrayLayoutTests.swift`
  - Covers larger HOLD action metrics and right-side row width.
- `Tests/FlowlineAppTests/ShelfServiceTests.swift`
  - Covers low-latency auto-capture cadence and shelf behavior.

## Verification

- Command: `rtk swift test --filter SettingsPresentationTests`
  - Result: passed; 3 tests.
- Command: `rtk swift test --filter SettingsWindowControllerTests`
  - Result: passed; 1 test.
- Command: `rtk swift test --filter ShelfServiceTests`
  - Result: passed after rerun; earlier full-suite run had one transient clipboard timing failure.
- Command: `rtk swift test`
  - Result: passed; 173 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.

## Open Questions / Risks

- Physical UI verification is still needed on the real notch for:
  - Broken Y menubar/collapsed notch visibility.
  - Settings window opening from status menu.
  - HOLD action row spacing when HOLD is right side.
  - Terminal/window drag no longer opening HOLD.
- Full-suite `ShelfService` clipboard quick-capture test had one transient failure, then passed on targeted and full reruns.
- Worktree is very large; commit should be split by feature group.
- Product direction decision pending:
  - Do not keep widening the notch for detail.
  - Build a future Workbench/popout for expanded HOLD preview, OCR text, AI action, and history.

## Next Steps

1. Physically verify the latest running app:
   - status menu Settings opens
   - Broken Y icon visibility
   - Terminal drag does not open HOLD
   - screenshot/text capture speed feels right
2. If accepted, do a self-review over the large dirty diff and split commits:
   - drag activation bugfix
   - HOLD action layout
   - shelf capture latency
   - logo/status item/settings open fix
   - settings category/preview structure
3. Design the future Workbench/popout scope:
   - HOLD item click opens a wider panel below notch
   - first version: big preview, metadata, Copy/OCR/Delete, Close

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For behavior changes, prefer test-first and keep changes scoped.
- Do not revert unrelated user changes in the dirty worktree.
- Keep Settings visual style stable unless the user explicitly asks for redesign.

## Resume Prompt

Continue from this checkpoint. First read this file and the project instructions, then inspect the listed files before making changes. Preserve the current Settings visual language; focus next on physical verification and then either commit splitting or Workbench/popout planning.
