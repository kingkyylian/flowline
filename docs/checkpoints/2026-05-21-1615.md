# Project Checkpoint - 2026-05-21 16:15 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest implementation commit:
  - `fe057cd Require exact release artifact names`
- Context: Continued autonomous release hardening by making Release Candidate Verify accept only exact release artifact names.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current head/CI were clean.
- Selected the next secret-free release hardening target:
  - `Release Candidate Verify` selected artifacts with `startswith("flowline-release-v")`.
  - That accepted non-release suffixes such as `flowline-release-v1.2.3-debug`.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `releaseCandidateVerifyWorkflowRejectsAmbiguousCandidateArtifacts` now requires exact artifact-name regex filtering and rejects `startswith`.
- Updated `.github/workflows/release-candidate-verify.yml`:
  - Replaced `startswith("flowline-release-v")` with jq `test("^flowline-release-v[0-9]+(\\.[0-9]+){0,2}$")`.
- Pushed implementation commit `fe057cd`.
- GitHub Actions run `26228116026` passed for `fe057cd`.

## Current State

- The release hardening implementation is pushed at `fe057cdf0a240e8d7b8026b98fc63d23b3e4a99f`.
- Working tree was clean before writing this checkpoint.
- Release Candidate Verify now requires candidate artifact names to match the same one-to-three-component version shape as release tags.
- Existing release protections remain in place:
  - release candidate workflow runs publish preflight before release secret validation;
  - Developer ID `.p12` import uses scoped keychain access;
  - signing identity is preflighted before notary credential storage;
  - release candidate verification rejects ambiguous artifacts and archives;
  - publish preflight verifies CI and artifact provenance for the produced archive.

## Important Files / Artifacts

- `.github/workflows/release-candidate-verify.yml`: exact release artifact name filter.
- `.github/workflows/release-candidate.yml`: guarded release candidate build and notarization workflow.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers release workflow security invariants.
- `script/publish_preflight.sh`: repository hygiene, release manifest, CI, and artifact provenance checks.

## Verification

- Command: `rtk swift test --filter AmbiguousCandidateArtifacts`
  - Result: first failed against the old workflow, then passed after replacing `startswith` with exact regex filtering.
- Command: `rtk jq -nr '["flowline-release-v1", "flowline-release-v1.2", "flowline-release-v1.2.3", "flowline-release-v1.2.3-debug"] | map(select(test("^flowline-release-v[0-9]+(\\.[0-9]+){0,2}$"))) | join(",")'`
  - Result: passed; output excludes `flowline-release-v1.2.3-debug`.
- Command: `rtk swift test --filter 'releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 58 tests.
- Command: `rtk ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/ci.yml .github/workflows/release-candidate.yml .github/workflows/release-candidate-verify.yml`
  - Result: passed.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 267 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26228116026 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `fe057cd`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `fe057cd`; Release Candidate Verify now accepts only exact `flowline-release-vX[.Y[.Z]]` artifact names.
