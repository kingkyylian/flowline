# Project Checkpoint - 2026-05-21 14:08 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit before this checkpoint:
  - `1a2ab8f Verify release archive manifests before tagging`
- Context: Continued autonomous release hardening by adding a manifest-backed provenance/version check before release tagging.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed `main` was clean.
- Selected the next checkpoint target:
  - The archive preflight proved archive name and non-empty bytes, but did not prove the archive came from the current source revision or matched release metadata.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - Packaging writes `Flowline-X.Y.Z.manifest` next to the release archive.
  - Publish preflight rejects a missing manifest.
  - Publish preflight rejects manifest version mismatch.
  - Publish preflight rejects manifest git commit mismatch.
  - Publish preflight rejects manifest checksum mismatch.
  - Positive release tag/archive preflight now uses a manifest tied to the fixture `HEAD`.
- Updated `script/package_release.sh`:
  - Writes `Flowline-X.Y.Z.manifest` next to the archive.
  - Manifest includes format version, archive name, app version, build, bundle ID, git commit, SHA-256, size in bytes, and notarization state.
  - Rewrites the manifest after final notarized re-zip.
- Updated `script/publish_preflight.sh`:
  - Requires the manifest when `--tag` and `--archive` are used.
  - Verifies manifest format, archive name, version, SHA-256, size, and git commit against current `HEAD`.
- Updated release docs:
  - `README.md`
  - `docs/RELEASE.md`
- Pushed `1a2ab8f`; GitHub Actions run `26222151141` passed.

## Current State

- Working tree was clean after pushing `1a2ab8f` and before writing this checkpoint.
- `main` and `origin/main` were aligned at `1a2ab8f8784234870d1cd0d308a95090bf284295`.
- GitHub Actions is green on current implementation head:
  - Run: `26222151141`
  - Head: `1a2ab8f8784234870d1cd0d308a95090bf284295`
  - Result: `success`
  - Job: `Swift tests`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.

## Important Files / Artifacts

- `script/package_release.sh`: now emits release archive manifests.
- `script/publish_preflight.sh`: now verifies archive manifest metadata before allowing release tag preflight to pass.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers manifest creation and publish-preflight manifest rejection paths.
- `docs/RELEASE.md`: documents manifest-backed tag preflight.
- `README.md`: mentions the required generated manifest.
- `/private/tmp/Flowline-0.1.0.zip` and `/private/tmp/Flowline-0.1.0.manifest`: temporary files used to verify the real preflight path; not release artifacts.

## Verification

- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: first failed with expected RED issues, then passed with 39 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 248 tests.
- Command: `rtk ./script/publish_preflight.sh --tag v0.1.0 --archive /private/tmp/Flowline-0.1.0.zip`
  - Result: passed with a manifest tied to current `HEAD`.
- Command: `rtk gh run watch 26222151141 --repo kingkyylian/flowline --exit-status`
  - Result: passed.

## Open Questions / Risks

- Historical Google OAuth credential still needs external rotation/revocation if it was real.
- Real release packaging/notarization still requires external Apple credentials:
  - Developer ID Application certificate.
  - `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.
- The real notarization service and Gatekeeper assessment still need one end-to-end run with real credentials before a public release.
- The manifest improves provenance checks, but a determined local actor could still forge both archive and manifest; stronger release assurance would require signed release attestations or verified CI-built artifacts.

## Next Steps

1. Add a local release attestation or CI artifact handoff so public releases can require CI-built artifacts instead of locally generated archives.
2. Run one real notarized release when Apple Developer ID and notary credentials are available.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`, and the latest GitHub Actions run for `main`. CI is green on `1a2ab8f`; package release now writes `Flowline-X.Y.Z.manifest`, and publish preflight requires that manifest to match archive name, version, checksum, size, and current `HEAD` before release tagging. Follow project rules from `AGENTS.md`: use `rtk`, preserve unrelated changes, and verify before claiming completion.
