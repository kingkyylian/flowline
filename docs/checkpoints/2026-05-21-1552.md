# Project Checkpoint - 2026-05-21 15:52 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest implementation commit:
  - `6c36bf9 Run release preflight before secrets`
- Context: Continued autonomous release hardening by making the manual Release Candidate workflow run repository publish preflight before validating or consuming Apple signing/notary secrets.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current head/CI were clean.
- Selected the next secret-free release hardening target:
  - `Release Candidate` already checked tag collisions before secrets.
  - It did not rerun `script/publish_preflight.sh` before secrets, so a manual candidate could consume Apple credentials before repository hygiene checks such as secret scan and origin-head validation.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `releaseCandidateWorkflowRunsPublishPreflightBeforeUsingReleaseSecrets`
- Updated `.github/workflows/release-candidate.yml`:
  - Added `Run publish preflight` after `Preflight release tag`.
  - The new step runs `script/publish_preflight.sh` before `Validate release secrets`.
- Pushed implementation commit `6c36bf9`.
- GitHub Actions run `26226932688` passed for `6c36bf9`.

## Current State

- The release hardening implementation is pushed at `6c36bf9fcc957c933bc4ef519fe8a57534ff22d1`.
- Working tree was clean before writing this checkpoint.
- Release candidate workflow now has three pre-secret gates:
  - release tag format/metadata derivation;
  - local/origin tag collision check;
  - full publish preflight for repository hygiene.
- Existing release protections remain in place:
  - Developer ID `.p12` import uses scoped keychain access;
  - release candidate verification rejects ambiguous artifacts and archives;
  - publish preflight verifies CI and artifact provenance for the produced archive.

## Important Files / Artifacts

- `.github/workflows/release-candidate.yml`: runs publish preflight before release secret validation.
- `.github/workflows/release-candidate-verify.yml`: rejects ambiguous release candidate artifacts and archives.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers release workflow security invariants.
- `script/publish_preflight.sh`: repository hygiene, secret scan, origin reachability, release manifest, CI, and artifact provenance checks.

## Verification

- Command: `rtk swift test --filter PublishPreflightBeforeUsingReleaseSecrets`
  - Result: first failed against the old workflow, then passed after adding the preflight step.
- Command: `rtk swift test --filter 'releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 57 tests.
- Command: `rtk ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/ci.yml .github/workflows/release-candidate.yml .github/workflows/release-candidate-verify.yml`
  - Result: passed.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 266 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26226932688 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `6c36bf9`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `6c36bf9`; release candidate now runs publish preflight before validating or consuming Apple release secrets.
