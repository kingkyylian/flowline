# Project Checkpoint - 2026-05-19 19:47 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, HEAD `f4c9449 Fix clipboard Hold snapshot updates`
- Context: Checkpoint next steps were treated as a serious QA/release-readiness pass for the expanded notch overlay work.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed the source worktree started clean.
- Re-ran the full Swift test suite on current HEAD.
- Rebuilt, signed, and launched `dist/Flowline.app` through the project run script.
- Captured live desktop screenshots to verify the overlay and Settings UI were actually visible, not just process-running:
  - `/private/tmp/flowline-live-qa.png`
  - `/private/tmp/flowline-after-toggle-qa.png`
  - `/private/tmp/flowline-settings-qa.png`
  - `/private/tmp/flowline-settings-modules-qa.png`
- Verified `Option-Space` live overlay expansion with System Events after granting command permission.
- Opened Flowline Settings from the app menu and visually verified:
  - General screen renders with permission rows.
  - Modules screen renders with `Layout preview 2/2 + Limits`.
  - Active preview cards are Hold, Limits, and Music.
  - Workspace and Calendar show slot-limit `MAX` state while Hold/Music occupy the two side slots.
- Checked persisted module defaults after the UI attempt:
  - `module.shelf.enabled = 1`
  - `module.music.enabled = 1`
  - `module.workspace.enabled = 0`
  - `module.calendar.enabled = 0`

## Current State

- `Flowline.app` is running from `dist/Flowline.app`.
- Expanded overlay visually renders across the notch area with Hold, Limits, and Music modules.
- The live Settings window can open and render General/Modules content, but UI checkbox clicking through automation was not reliable enough to claim manual toggle completion.
- `git remote -v` is empty, so there is no configured push target from this repo.
- The keychain has one valid signing identity: `Apple Development: bwib3927@outlook.com (G953CU4G2L)`.
- No Developer ID Application identity is available, so `script/package_release.sh` cannot produce a release archive without `FLOWLINE_DEVELOPER_ID_IDENTITY`.

## Important Files / Artifacts

- `script/build_and_run.sh`: debug build/sign/run path; successfully launches Flowline.
- `script/package_release.sh`: release packaging path; requires a Developer ID Application certificate.
- `Sources/FlowlineApp/App/FlowlineApp.swift`: status menu, Settings opening, and `Option-Space` overlay toggle wiring.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`: live Settings General/Modules UI surface.
- `Sources/FlowlineApp/Stores/AppState.swift`: persisted module state and toggle policy.
- `/private/tmp/flowline-after-toggle-qa.png`: best visual evidence for expanded overlay with Hold/Limits/Music.
- `/private/tmp/flowline-settings-modules-qa.png`: best visual evidence for Modules settings preview and slot-limit state.

## Verification

- Command: `rtk swift test`
  - Result: passed, 202 tests.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; debug app built, signed with Apple Development identity, and Flowline is running.
- Command: `rtk screencapture -x /private/tmp/flowline-live-qa.png`
  - Result: passed after sandbox escalation; visual overlay evidence captured.
- Command: `rtk osascript -e 'tell application "System Events" to key code 49 using option down'`
  - Result: passed after sandbox escalation; `Option-Space` expanded the overlay.
- Command: `rtk codesign --verify --deep --strict --verbose=2 dist/Flowline.app`
  - Result: passed outside sandbox; `dist/Flowline.app` is valid on disk and satisfies its designated requirement.
- Command: `rtk log show --last 10m --predicate 'process == "Flowline" && (eventMessage CONTAINS[c] "error" OR eventMessage CONTAINS[c] "fault" OR eventMessage CONTAINS[c] "crash")' --style compact`
  - Result: no crash/fault; saw framework/system noise including `FSFindFolder failed with error=-43` and FrontBoard/CFNetwork debug entries.

## Open Questions / Risks

- Live checkbox toggles in Settings were not fully completed because coordinate/UI automation did not reliably change the `UserDefaults` state.
- Finder drag/drop into Hold was not manually exercised with real files, screenshots, text, URLs, and file promises.
- Physical notch/display polish still needs human inspection on the target display with windows moved out of the overlay area.
- Release packaging is blocked until a Developer ID Application certificate is available and `FLOWLINE_DEVELOPER_ID_IDENTITY` is set.
- Push is blocked until a git remote is configured.

## Next Steps

1. Manually perform the two remaining physical QA flows: Settings module checkbox toggles and Finder/text/URL drops into Hold.
2. Configure a git remote or confirm the intended repo, then push `main` if the manual QA is acceptable.
3. Install/configure a Developer ID Application certificate and run `script/package_release.sh --archive` or `--notarize`.

## Resume Prompt

Continue from this checkpoint. First read this file and the project `AGENTS.md` instructions from the conversation, then inspect `rtk git status --short`, `rtk git log --oneline -8`, and whether a git remote plus Developer ID Application signing identity are available. Do not claim Settings checkbox toggles or Finder Hold drag/drop are complete until they have been manually exercised or a reliable automation harness is added.
