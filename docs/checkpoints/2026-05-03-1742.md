# Project Checkpoint - 2026-05-03 17:42

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, no commits yet; all project files are currently untracked.
- Context: Flowline is a local-first macOS developer companion overlay with Companion and Notch position modes.

## Done This Session

- Created a new SwiftPM macOS app with `FlowlineCore` and `FlowlineApp` targets.
- Added AppKit `NSPanel` overlay, SwiftUI settings, menu bar extra, and build/run script.
- Implemented core models and services for active app context, git status parsing, calendar next event, meeting URL detection, local shelf, permissions, launch-at-login, and `.codexbar` agent provider discovery.
- Added tests for URL detection, git status parsing, shelf behavior, and permission fallback.
- Iterated UI from floating bar to top-attached companion mode, then added `Companion / Notch` position mode.
- Companion Mode: right/top attached, white glass style.
- Notch Mode: center/top attached, custom black notch surface using `FlowlineNotchShape`, minimal collapsed state, darker expanded panels.
- Latest Notch Mode fix: expanded content now has a top safe inset so text is not clipped by the top screen edge.

## Current State

- App launches from `dist/Flowline.app` via `script/build_and_run.sh`.
- Settings has `Position` segmented control with `Companion` and `Notch`.
- User feedback: Notch Mode still needs UI/UX polish based on upcoming screenshot references. Current issue was text clipping and separate-piece feel; latest change addressed clipping, but visual fit still needs review.
- User asked to use a UI/UX skill and will send images. No separate local skill named `ui`/`ux` was found under `/Users/kyylian/.codex/skills`; use macOS SwiftUI patterns and image reference analysis instead.

## Important Files / Artifacts

- `Sources/FlowlineApp/App/OverlayController.swift`: panel size, screen position, Companion/Notch placement logic.
- `Sources/FlowlineApp/Views/Overlay/OverlayRootView.swift`: root overlay styling, mode-aware background/shape, tap/drop behavior, expanded safe padding.
- `Sources/FlowlineApp/Views/Overlay/FlowlineNotchShape.swift`: custom black top-flush notch shape.
- `Sources/FlowlineApp/Views/Overlay/CollapsedPillView.swift`: collapsed Companion and minimal Notch content.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`: expanded `Now / Agent / Shelf` panels and mode-aware colors.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`: privacy controls, behavior controls, position picker.
- `script/build_and_run.sh`: builds SwiftPM target, stages `.app`, launches and verifies process.

## Verification

- Command: `rtk swift test`
- Result: passed, 10/10 tests.
- Command: `rtk ./script/build_and_run.sh --verify`
- Result: passed, `Flowline is running`.

## Open Questions / Risks

- Notch Mode needs visual tuning against real screenshots; center placement, black surface size, and content safe area may still need adjustment.
- Need decide whether Notch Mode should be interactive by click only or hover-open like boring.notch.
- Current `.git` repo has no commits; commit strategy is still open.
- Launch-at-login may not work correctly from a SwiftPM-staged development bundle; verify later in packaged release flow.

## Next Steps

1. Review the next screenshot reference from the user and identify the exact visual target for Notch Mode.
2. Tune `FlowlineNotchShape`, `OverlayController.currentSize`, and `OverlayRootView.contentInsets` against that reference.
3. Add a UI polish pass for Settings after the main overlay direction is stable.

## Resume Prompt

Continue from this checkpoint. First read this file, then inspect the listed overlay files before making UI changes. Use the user's screenshot reference as the source of truth for the next visual iteration.
