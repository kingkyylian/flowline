# Project Checkpoint - 2026-05-11 15:21 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with HOLD feature changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: Added AppKit drag/drop bridge so dragging macOS floating screenshot thumbnails toward the notch can expand Flowline and drop into HOLD.

## Done This Session

- Added `HoldDropTypes`:
  - accepts file URLs
  - accepts URLs/text
  - accepts raw PNG/TIFF
  - accepts all `NSFilePromiseReceiver.readableDraggedTypes`
- Moved root-level drop handling from SwiftUI `.onDrop` to `OverlayHostingView`.
- Added AppKit drag destination behavior:
  - registers the overlay hosting view for HOLD drop types
  - collapsed notch drag enter expands Flowline
  - expanded mode accepts drop only in the left HOLD frame
  - file URL drops still go through normal HOLD file path
  - file promise drops are received into a temp destination, then moved into Flowline screenshot stash
  - raw PNG/TIFF drops are written as `Dropped Screenshot ...png`, then moved into Flowline screenshot stash
- Added `AppState.addShelfScreenshots(_:)` and `ShelfService.addScreenshotFiles(_:)`.
- Kept normal file drag safe:
  - regular file drops still do not move the user's original file
  - promised/raw screenshot drops are stashed because Flowline owns the received temp file
- Added regression tests:
  - HOLD drop types include file promise types
  - dropped screenshot files are moved into the Flowline screenshot stash

## Current State

- Flowline rebuilt, signed for development, relaunched, and running.
- Worktree is dirty and uncommitted.
- New/important files:
  - `Sources/FlowlineApp/Support/HoldDropTypes.swift`
  - `Tests/FlowlineAppTests/HoldDropTypesTests.swift`
- Important modified files:
  - `Sources/FlowlineApp/App/OverlayHostingView.swift`
  - `Sources/FlowlineApp/Views/Overlay/OverlayRootView.swift`
  - `Sources/FlowlineApp/Stores/AppState.swift`
  - `Sources/FlowlineApp/Services/ShelfService.swift`
  - `Tests/FlowlineAppTests/ShelfServiceTests.swift`

## Verification

- Command: `rtk swift test`
  - Result: passed; 62 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.

## Open Questions / Risks

- Real floating screenshot thumbnail drag/drop cannot be fully automated from tests because the source is macOS SystemUIServer UI.
- Manual test still required:
  - take a screenshot
  - drag the floating thumbnail to the notch
  - verify Flowline expands
  - drop on HOLD
  - verify it appears as a screenshot item with thumbnail/actions
- If the floating thumbnail does not expose `NSFilePromiseReceiver` on this macOS build, inspect live pasteboard drag types next.

## Next Steps

1. Manually test real floating screenshot thumbnail drag into notch/HOLD.
2. If it fails, instrument `draggingEntered` to log `sender.draggingPasteboard.types` and adjust accepted types.
3. If it works, commit the HOLD feature.

## Resume Prompt

Continue from this checkpoint. First manually test dragging the macOS floating screenshot thumbnail to the notch and dropping it on HOLD. If it fails, inspect live dragging pasteboard types from `OverlayHostingView`.
