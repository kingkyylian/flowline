# Project Checkpoint - 2026-05-20 19:11 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commits before this checkpoint:
  - `ac07396 Use Node 24 compatible checkout action`
  - `d41ec2c Validate release metadata before packaging`
- Context: Continued autonomous release/CI hardening after the CI recovery checkpoint.

## Done This Session

- Restored the previous checkpoint and compared it with current git state.
- Selected a credential-free release hardening target: package metadata validation before release build/signing.
- Added a RED regression test proving invalid release metadata reached `swift build`:
  - `FLOWLINE_BUNDLE_ID=dev.kyylian.flowline<bad>`
  - `FLOWLINE_VERSION=1.0<bad>`
  - `FLOWLINE_BUILD=1&bad`
- Updated `script/package_release.sh` to fail fast before icon/build/notary work when:
  - `FLOWLINE_BUNDLE_ID` is not a reverse-DNS identifier.
  - `FLOWLINE_VERSION` is not one to three dot-separated integers.
  - `FLOWLINE_BUILD` is not one to three dot-separated integers.
- Pushed `d41ec2c Validate release metadata before packaging`; CI run `26174637510` passed.
- Picked the next concrete CI target from that run's annotation: `actions/checkout@v4` was using deprecated Node.js 20 runtime.
- Confirmed `actions/checkout` tag `v5` exists.
- Added a RED regression test requiring `actions/checkout@v5` and rejecting `@v4`.
- Updated `.github/workflows/ci.yml` to use `actions/checkout@v5` while preserving `fetch-depth: 0`.
- Pushed `ac07396 Use Node 24 compatible checkout action`; CI run `26174850911` passed.

## Current State

- Working tree was clean after the pushed implementation commits.
- `main` and `origin/main` were aligned at `ac07396` before this checkpoint file was written.
- GitHub Actions is green on current implementation head:
  - Run: `26174850911`
  - Head: `ac07396db44dbbdc90a112c9f4c59007e64caff4`
  - Result: `success`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.
- The previous Node.js 20 checkout annotation did not appear in the `actions/checkout@v5` watch output.
- Local full test suite passes with 230 tests.

## Important Files / Artifacts

- `script/package_release.sh`: validates release metadata before packaging and documents the accepted env var formats.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: now covers release metadata fail-fast behavior and checkout action version policy.
- `.github/workflows/ci.yml`: uses `actions/checkout@v5` with full-history checkout for publish preflight.
- `docs/checkpoints/2026-05-20-1911.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: should match this checkpoint.

## Verification

- Command: `rtk swift test --filter releasePackagingRejectsInvalidMetadataBeforeBuild`
  - Result: first failed correctly, then passed after implementation.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 20 tests.
- Command: `rtk bash -n script/package_release.sh`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 229 tests after release metadata change; passed, 230 tests after checkout policy test.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk ./script/publish_preflight.sh`
  - Result: passed after each push; remote HEAD matched `d41ec2c` and then `ac07396`.
- Command: `rtk gh run watch 26174637510 --repo kingkyylian/flowline --exit-status`
  - Result: passed for `d41ec2c`.
- Command: `rtk gh run watch 26174850911 --repo kingkyylian/flowline --exit-status`
  - Result: passed for `ac07396`.

## Open Questions / Risks

- Historical Google OAuth credential still needs external rotation/revocation if it was real.
- Real release packaging/notarization still requires external Apple credentials:
  - Developer ID Application certificate.
  - `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.
- The package script's real archive/notarize path still cannot be fully exercised locally without those credentials.

## Next Steps

1. Rotate/revoke the exposed Google OAuth credential if it was real.
2. Install/select a Developer ID Application identity and set `FLOWLINE_DEVELOPER_ID_IDENTITY`.
3. Configure `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials, then run `script/package_release.sh --notarize`.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/ci.yml`, `script/package_release.sh`, and the latest GitHub Actions run for `main`. CI is green on `ac07396`; release metadata validation and checkout v5 are now guarded by tests. Real release notarization remains blocked on external Apple credentials.
