# Project Checkpoint - 2026-05-12 20:20 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with active Flowline notch/HOLD/LIMITS/module changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: HOLD side-slot is polished, automatic capture is user-controlled, and screenshot drag-to-open regression has been patched.

## Done This Session

- Made automatic HOLD capture user-controlled:
  - plain clipboard text is no longer auto-captured by default, so `ctrl+c` remains user-controlled
  - screenshot auto-capture remains enabled by default
  - Settings > Behavior has `Hold capture` toggles for `Screenshots` and `Clipboard text`
  - enabling auto-capture marks the current pasteboard as handled so old clipboard contents are not pulled in retroactively
- Fixed the screenshot drag regression reported after the plain-window-drag fix:
  - root cause: `OverlayController` always passed `hasHoldPayload: false` for global collapsed drag expansion, so real screenshot/file drags could hover the small notch without opening the panel
  - added `HoldDropPayloadDetector` to classify real HOLD payloads from drag pasteboards
  - `OverlayController` now reads `NSPasteboard.Name.drag` during mouse-drag events and expands only when it contains a file/text/image/file-promise payload
  - ordinary Terminal/window drags still do not open the panel
  - `OverlayHostingView` now uses the same detector for accept/import decisions
  - legacy `NSFilenamesPboardType` filename drags are registered and imported as file URLs
- Earlier HOLD polish in this batch remains in place:
  - inline text/code hover preview instead of floating peek card
  - larger centered empty HOLD drop arrow beside `Drop here`
  - screenshot primary action copies image data as `IMG`; OCR copy remains explicit `TEXT`
  - screenshot/file drag provider includes Terminal-compatible payloads
  - right-slot Hold renders the real interactive Hold column

## Current State

- Flowline has been rebuilt, signed, relaunched, and verified running after the latest drag fix.
- Worktree is still dirty and uncommitted.
- `docs/checkpoints/LATEST.md` points to this checkpoint.

## Important Files / Artifacts

- `Sources/FlowlineApp/App/OverlayController.swift`
  - collapsed notch hover/drag expansion; now uses drag pasteboard payload detection.
- `Sources/FlowlineApp/App/OverlayHostingView.swift`
  - AppKit drag destination bridge; uses the shared HOLD payload detector for drop acceptance/import.
- `Sources/FlowlineApp/Support/HoldDropPayloadDetector.swift`
  - shared pasteboard classifier for file promises, file URLs, legacy filename drags, web URLs, image data, and non-empty text.
- `Sources/FlowlineApp/Support/HoldDropTypes.swift`
  - registered drop pasteboard types, now including `NSFilenamesPboardType`.
- `Sources/FlowlineApp/Support/OverlayDragActivation.swift`
  - has both boolean and pasteboard-based collapsed expansion entrypoints.
- `Sources/FlowlineApp/Services/ShelfService.swift`
  - owns HOLD source polling, screenshot stash lifecycle, OCR, self-copy guard, and `ShelfAutoCaptureOptions`.
- `Sources/FlowlineApp/Stores/AppState.swift`
  - persists `hold.autoCapture.screenshots` and `hold.autoCapture.clipboardText`.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`
  - `Hold capture` toggles.
- `Tests/FlowlineAppTests/HoldDropTypesTests.swift`
  - covers legacy filename payload detection, empty pasteboard rejection, and file drag pasteboard expansion.
- `Tests/FlowlineAppTests/OverlayDragActivationTests.swift`
  - verifies HOLD payload drags expand and plain window drags do not.
- `Tests/FlowlineAppTests/ShelfServiceTests.swift`
  - covers default no-text clipboard capture, opt-in text capture, disabled screenshot auto-capture, manual drops, OCR, cleanup, and self-copy guard.

## Verification

- Command: `rtk swift test --filter HoldDropTypesTests`
  - Result: failed first as expected before `HoldDropPayloadDetector` and pasteboard expansion existed.
- Command: `rtk swift test --filter HoldDropTypesTests`
  - Result: passed; 5 tests.
- Command: `rtk swift test --filter OverlayDragActivationTests`
  - Result: passed; 4 tests.
- Command: `rtk swift test --filter HoldDropImportPlanTests`
  - Result: passed; 3 tests.
- Command: `rtk swift test`
  - Result: passed; 148 tests.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.
- Command: `rtk git diff --check`
  - Result: passed.

## Open Questions / Risks

- User should physically retest screenshot drag over the small notch with the freshly relaunched app.
- Also retest Terminal/window drag to confirm the panel stays closed for non-payload drags.
- Right-slot HOLD should still be physically tested once with Workspace enabled and Music disabled.
- The worktree is large; before commit, stage related files carefully.

## Next Steps

1. Ask user to retry dragging a screenshot onto the small notch; expected result: panel opens and HOLD accepts the drop.
2. Physically test non-payload Terminal/window drag; expected result: panel does not open.
3. Commit the stabilized HOLD/LIMITS/module batch after physical validation.

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For behavior changes, prefer test-first and keep changes scoped.
- Do not revert unrelated user changes in the dirty worktree.

## Resume Prompt

Continue from this checkpoint. First read this file and the project instructions, then physically validate screenshot drag-to-open and non-payload window dragging before committing.
