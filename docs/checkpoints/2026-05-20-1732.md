# Project Checkpoint - 2026-05-20 17:32 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and tracking `origin/main`
- Latest implementation commit: `3d49e04 Expand publish secret scan patterns`
- Remote: `origin -> https://github.com/kingkyylian/flowline.git`
- Context: Publish is guarded and pushed; the latest target eliminated was the secret scanner only recognizing Google OAuth-style credentials.

## Done This Session

- Restored from the previous checkpoint and verified the current tree was clean.
- Selected the next local security target from the publish pipeline:
  - `script/secret_scan.sh` already included ignored files and avoided echoing matched values.
  - Its pattern coverage was still too narrow for common high-risk developer credentials.
- Added failing regression tests first for additional credential classes:
  - OpenAI API keys.
  - Anthropic API keys.
  - GitHub tokens.
  - Slack tokens.
  - AWS access keys.
- Kept test fixtures split in source so the repository itself does not contain contiguous fake credential patterns.
- Updated `script/secret_scan.sh` with narrow high-signal patterns for those classes.
- Updated `README.md` to describe the expanded scan coverage.
- Committed and pushed the implementation to `origin/main`.

## Current State

- Working tree is clean.
- `main` is synchronized with `origin/main`.
- Publish preflight passes with network access.
- Full test suite passes with 224 tests.
- Release archive/notarization still requires external Apple credentials:
  - Developer ID Application identity.
  - Notary profile or Apple ID notary credentials.

## Important Files / Artifacts

- `script/secret_scan.sh`: now scans for Google OAuth values, AI API keys, GitHub tokens, Slack tokens, and AWS access keys.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers the expanded secret-scan token classes and non-disclosure of matched values.
- `README.md`: documents the expanded scan coverage.
- `docs/checkpoints/2026-05-20-1732.md`: this checkpoint.

## Verification

- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: first failed against old behavior for the new token classes, then passed after implementation.
- Command: `rtk bash -n script/secret_scan.sh script/publish_preflight.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 224 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/publish_preflight.sh`
  - Result: passed after push; secret scan passed and GitHub origin was reachable.
- Command: `rtk git push origin main`
  - Result: passed; implementation commit was pushed to remote `main`.

## Open Questions / Risks

- The historical OAuth credential should still be treated as exposed if it was real. Rotate or revoke it in Google Cloud outside this repo.
- Developer ID Application certificate is still not installed locally, so real archive/notarization remains externally blocked.
- Notarization still needs `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.

## Next Steps

1. Rotate/revoke the exposed Google OAuth credential if it was real.
2. Install/select a Developer ID Application identity and set `FLOWLINE_DEVELOPER_ID_IDENTITY`.
3. Configure `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials, then run `script/package_release.sh --notarize`.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -8`, `docs/checkpoints/LATEST.md`, and `script/secret_scan.sh`. Publish is guarded and pushed; secret scanning includes ignored files, redacts matched values, and covers common AI/GitHub/Slack/AWS token classes. Real release notarization still needs external Apple credentials.
