# Project Checkpoint - 2026-05-24 18:36 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, synchronized with `origin/main`
- Head: `5047b15b413d49e51d71d27fb495456b438b87a3`
- Context: Release-candidate execution is blocked by external Apple Developer ID/notary credentials and missing GitHub Actions secrets. Repo-side helper automation has been added and verified, but the real notarized candidate cannot be produced from the current environment.

## Done This Session

- Rechecked current external state after the release helper work:
  - Required release secret env values are not present in the shell.
  - Local Keychain still has only `Apple Development: bwib3927@outlook.com (G953CU4G2L)`.
  - No local `Developer ID Application` identity is installed.
  - GitHub Actions still lacks all seven required release secrets.
- Confirmed the guarded release runner cannot be used yet because:
  - `script/configure_release_secrets.sh --repo kingkyylian/flowline --check` fails on missing secrets.
  - The working tree is intentionally dirty with helper/test/docs/checkpoint changes, so `script/run_release_candidate.sh` would stop at publish preflight even before dispatch.
- No new source changes were made after the `2026-05-24 18:28 +03` checkpoint except this checkpoint update.

## Current State

- Working tree is dirty with release-helper, release-runner, docs, tests, and checkpoint work:
  - `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`
  - `docs/RELEASE.md`
  - `docs/checkpoints/LATEST.md`
  - `docs/checkpoints/2026-05-24-1810.md`
  - `docs/checkpoints/2026-05-24-1821.md`
  - `docs/checkpoints/2026-05-24-1828.md`
  - `docs/checkpoints/2026-05-24-1836.md`
  - `script/configure_release_secrets.sh`
  - `script/run_release_candidate.sh`
- Source-side release gates from the previous checkpoint are passing:
  - Secret scan passed.
  - Diff check passed.
  - Swift test suite passed with 285 tests.
  - Release build passed.
- A real notarized release candidate cannot be produced until the external credentials are supplied and configured.

## Important Files / Artifacts

- `script/configure_release_secrets.sh`: validates/configures/checks release GitHub Actions secrets.
- `script/run_release_candidate.sh`: guarded release workflow dispatch helper.
- `docs/RELEASE.md`: documents secret setup and guarded dispatch.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: tests release helper and dispatch guard behavior.
- `docs/checkpoints/2026-05-24-1836.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: updated to this checkpoint.

## Verification

- Command: shell presence check for required release env vars
  - Result: missing: `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`, `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PATH`, `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`, `FLOWLINE_DEVELOPER_ID_IDENTITY`, `FLOWLINE_KEYCHAIN_PASSWORD`, `APPLE_ID`, `APPLE_TEAM_ID`, `APPLE_APP_SPECIFIC_PASSWORD`.
- Command: `rtk security find-identity -v -p codesigning`
  - Result: only `Apple Development: bwib3927@outlook.com (G953CU4G2L)` is available; no Developer ID identity.
- Command: `script/configure_release_secrets.sh --repo kingkyylian/flowline --check`
  - Result: failed as expected; all seven required GitHub Actions secrets are missing.
- Command: `git status --short --branch`
  - Result: branch is synchronized with `origin/main`; helper/test/docs/checkpoint files are uncommitted.

## Open Questions / Risks

- The release-candidate workflow still requires these repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- Local notarized packaging still requires an installed `Developer ID Application` identity, or CI must import it from the configured certificate secret.
- Helper/test/docs/checkpoint changes are not committed or pushed.

## Next Steps

1. Commit and push the release helper/test/docs/checkpoint changes if they should become part of the release process.
2. Provide the Developer ID `.p12`, certificate password, Developer ID identity string, Apple ID, Team ID, and app-specific password.
3. Run `script/configure_release_secrets.sh --repo kingkyylian/flowline --dry-run`, then run it without `--dry-run`, then run `--check`.
4. Run `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0`.
5. Let `Release Candidate Verify` complete, then preserve the publish preflight evidence against the uploaded notarized artifact before creating or pushing the public release tag.

## Resume Prompt

Continue from this checkpoint only after either the helper changes should be committed/pushed or the required Apple/GitHub release credentials are available. First inspect `git status --short --branch`, `docs/checkpoints/LATEST.md`, `script/configure_release_secrets.sh`, `script/run_release_candidate.sh`, and GitHub Actions secret state. Do not run the `Release Candidate` workflow until `script/configure_release_secrets.sh --repo kingkyylian/flowline --check` passes and the worktree is clean/pushed.
