# Project Checkpoint - 2026-05-21 14:21 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit before this checkpoint:
  - `ac7afba Require CI provenance for release tags`
- Context: Continued autonomous release hardening by requiring CI provenance for public release tag preflight.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed `main` was clean.
- Selected the next checkpoint target:
  - Manifest-backed archives could still be locally forged; public release preflight needed a way to require CI-run provenance.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - Release manifests record GitHub Actions provenance when `GITHUB_*` environment variables are present.
  - `--require-ci` rejects local-only manifests.
  - `--require-ci` rejects GitHub Actions runs whose head SHA does not match current `HEAD`.
  - `--require-ci` rejects GitHub Actions runs that did not complete successfully.
  - `--require-ci` accepts manifests whose GitHub Actions run succeeded for current `HEAD`.
- Updated `script/package_release.sh`:
  - Manifest now records `github_repository`, `github_run_id`, `github_run_attempt`, `github_workflow`, and `github_server_url`.
- Updated `script/publish_preflight.sh`:
  - Adds `--require-ci`.
  - Reads manifest GitHub Actions provenance.
  - Uses `gh run view` through `rtk` when available.
  - Verifies the manifest repository matches `origin`, the run completed successfully, and the run `headSha` matches current `HEAD`.
- Updated release docs:
  - `README.md`
  - `docs/RELEASE.md`
- Pushed `ac7afba`; GitHub Actions run `26222678138` passed.

## Current State

- Working tree was clean after pushing `ac7afba` and before writing this checkpoint.
- `main` and `origin/main` were aligned at `ac7afbaa0faf0fe8549205aa18012a6ee5ec6343`.
- GitHub Actions is green on current implementation head:
  - Run: `26222678138`
  - Head: `ac7afbaa0faf0fe8549205aa18012a6ee5ec6343`
  - Result: `success`
  - Job: `Swift tests`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.

## Important Files / Artifacts

- `script/package_release.sh`: release manifest now carries optional GitHub Actions provenance.
- `script/publish_preflight.sh`: public tag preflight can require CI provenance with `--require-ci`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers CI provenance manifest and publish-preflight paths.
- `docs/RELEASE.md`: documents public release tag preflight with `--require-ci`.
- `README.md`: documents `--require-ci`.
- `/private/tmp/Flowline-0.1.0.zip` and `/private/tmp/Flowline-0.1.0.manifest`: temporary files used to verify the real `--require-ci` path; not release artifacts.

## Verification

- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: first failed with expected RED issues, then passed with 44 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 253 tests.
- Command: `rtk gh run watch 26222678138 --repo kingkyylian/flowline --exit-status`
  - Result: passed.
- Command: `rtk ./script/publish_preflight.sh --tag v0.1.0 --archive /private/tmp/Flowline-0.1.0.zip --require-ci`
  - Result: passed with a manifest tied to GitHub Actions run `26222678138`.

## Open Questions / Risks

- Historical Google OAuth credential still needs external rotation/revocation if it was real.
- Real release packaging/notarization still requires external Apple credentials:
  - Developer ID Application certificate.
  - `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.
- The real notarization service and Gatekeeper assessment still need one end-to-end run with real credentials before a public release.
- `--require-ci` verifies a successful CI run for the same commit, but it does not yet prove the archive bytes were uploaded by that run; stronger assurance would require CI artifact upload/download or a signed attestation tied to the archive digest.

## Next Steps

1. Add CI artifact upload/download or signed release attestation so the archive digest itself is tied to a GitHub Actions run.
2. Run one real notarized release when Apple Developer ID and notary credentials are available.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`, and the latest GitHub Actions run for `main`. CI is green on `ac7afba`; package release manifests can now record GitHub Actions provenance, and public release preflight uses `--require-ci` to verify the manifest's run succeeded for the same `HEAD`. Follow project rules from `AGENTS.md`: use `rtk`, preserve unrelated changes, and verify before claiming completion.
