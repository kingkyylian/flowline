# Project Checkpoint - 2026-05-21 15:07 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit before this checkpoint:
  - `0a3761f Add notarized release candidate workflows`
- Context: Continued autonomous release hardening by adding the CI path needed to produce and verify a notarized release candidate artifact.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed the next target:
  - `--require-artifact` existed, but there was no GitHub Actions workflow to produce a notarized archive artifact and verify it after the producing run completed.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `Release Candidate` workflow must be manual, main-branch guarded, notarize with Apple credentials, and upload the release zip plus manifest.
  - `Release Candidate Verify` workflow must run after the candidate workflow completes, checkout the candidate commit, download the candidate artifact, and run `--require-ci --require-artifact`.
  - `script/package_release.sh` must also accept `FLOWLINE_GITHUB_ARTIFACT_NAME`; this avoids relying on a `GITHUB_*` variable that GitHub Actions may reject when written through `$GITHUB_ENV`.
- Added `.github/workflows/release-candidate.yml`:
  - Manual `workflow_dispatch` with a release tag input.
  - Runs only from `main`.
  - Imports a Developer ID certificate from repository secrets into a temporary keychain.
  - Stores notary credentials in a temporary notary profile.
  - Runs `script/package_release.sh --notarize`.
  - Uploads `Flowline-X.Y.Z.zip` and `Flowline-X.Y.Z.manifest` with `actions/upload-artifact@v4`.
- Added `.github/workflows/release-candidate-verify.yml`:
  - Runs on completed successful `Release Candidate` workflows from the same repository and `main`.
  - Checks out the exact candidate `head_sha`.
  - Downloads the `flowline-release-vX.Y.Z` artifact from the candidate run.
  - Runs `script/publish_preflight.sh --require-ci --require-artifact` against the downloaded archive.
- Updated `script/package_release.sh`:
  - Manifest artifact name now prefers `FLOWLINE_GITHUB_ARTIFACT_NAME` and falls back to `GITHUB_ARTIFACT_NAME`.
- Updated release docs:
  - `README.md`
  - `docs/RELEASE.md`
- Pushed implementation commit `0a3761f`.
- GitHub Actions run `26224793476` passed for `0a3761f`.
- Confirmed GitHub has all workflows active:
  - `CI`
  - `Release Candidate`
  - `Release Candidate Verify`

## Current State

- Working tree was clean after pushing `0a3761f` and before writing this checkpoint.
- `main` and `origin/main` were aligned at `0a3761f98dc19191ee973dbfa2e585acd3f9834d`.
- GitHub Actions is green on current implementation head:
  - Run: `26224793476`
  - Workflow: `CI`
  - Head: `0a3761f98dc19191ee973dbfa2e585acd3f9834d`
  - Result: `success`
  - Job: `Swift tests`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.

## Important Files / Artifacts

- `.github/workflows/release-candidate.yml`: produces a notarized release candidate artifact from `main`.
- `.github/workflows/release-candidate-verify.yml`: verifies the completed candidate run and artifact provenance.
- `script/package_release.sh`: supports `FLOWLINE_GITHUB_ARTIFACT_NAME`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers the workflow contracts and artifact env fallback.
- `docs/RELEASE.md`: documents the GitHub release candidate flow and required secrets.
- `README.md`: summarizes the CI-backed release candidate flow.

## Verification

- Command: `rtk swift test --filter releaseCandidate`
  - Result: first failed because workflow files were missing, then passed.
- Command: `rtk swift test --filter FlowlineGitHubArtifactName`
  - Result: first failed because `FLOWLINE_GITHUB_ARTIFACT_NAME` was unsupported, then passed.
- Command: `rtk ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/ci.yml .github/workflows/release-candidate.yml .github/workflows/release-candidate-verify.yml`
  - Result: passed.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 52 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 261 tests.
- Command: `rtk gh run watch 26224793476 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `0a3761f`.
- Command: `rtk gh workflow list --repo kingkyylian/flowline --all`
  - Result: `CI`, `Release Candidate`, and `Release Candidate Verify` active.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The new workflows are loaded and locally/CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `0a3761f`; the repo now has active `Release Candidate` and `Release Candidate Verify` workflows. The remaining release step is to configure Apple signing/notary secrets and execute a real candidate run.
