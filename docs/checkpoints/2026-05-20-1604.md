# Project Checkpoint - 2026-05-20 16:04 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, source HEAD `c37353e Cover module toggle persistence`
- Context: Sensitive Hold expiry work is committed and live-verified; Settings module toggle behavior now has persistent regression coverage.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and verified current state before relying on prior context.
- Confirmed external blockers remain unchanged:
  - `git remote -v` is empty, so push has no configured target.
  - Keychain has only `Apple Development: bwib3927@outlook.com (G953CU4G2L)`, so Developer ID release packaging is still blocked.
- Selected the next local, non-blocked target: Settings module checkbox/toggle QA.
- Reviewed `SettingsView`, `AppState`, and `FlowlineModuleSelection` to identify the same behavior path used by live Settings toggles:
  - `SettingsView` module toggles call `state.setModule(module, enabled:)`.
  - `AppState` persists module toggle changes to `UserDefaults`.
  - `AppState` rejects enabling a module when side slots are already full and reports `max`.
- Added regression tests in `Tests/FlowlineAppTests/AppStateHoldDropFeedbackTests.swift`:
  - `appStatePersistsModuleToggleChanges`: toggles Hold off/on, verifies `module.shelf.enabled` persistence, and checks Workspace/Calendar become enableable while a side slot is free.
  - `appStateRejectsModuleToggleWhenSideSlotsAreFull`: verifies Workspace/Calendar cannot be enabled while Music+Hold occupy the two side slots and their status remains `max`.
- Committed the test coverage:
  - `c37353e Cover module toggle persistence`
- Also performed live Settings probing:
  - Opened Settings -> Modules.
  - Captured `/private/tmp/flowline-settings-modules-window-qa.png`.
  - Verified the UI tree exposed Workspace/Calendar as `Unavailable` and Hold/Music as `On`.
  - Toggled Hold off once and observed `module.shelf.enabled = 0`, then restored `module.shelf.enabled = 1`.

## Current State

- Source worktree was clean after `c37353e`.
- This checkpoint update is the only pending doc change at checkpoint-write time.
- `dist/Flowline.app` was rebuilt and is running.
- UserDefaults were restored to the expected working state:
  - `module.workspace.enabled = 0`
  - `module.music.enabled = 1`
  - `module.calendar.enabled = 0`
  - `module.shelf.enabled = 1`
  - `hold.autoCapture.clipboardText = 1`
  - `hold.autoCapture.screenshots` key absent, so it uses fallback `true`.

## Important Files / Artifacts

- `Tests/FlowlineAppTests/AppStateHoldDropFeedbackTests.swift`: now covers Settings module toggle persistence and max-slot rejection through the AppState path used by Settings.
- `/private/tmp/flowline-settings-modules-window-qa.png`: Settings Modules screenshot showing layout preview and module toggle states.
- `/private/tmp/flowline-secret-expanded-window-qa.png`: prior live overlay evidence showing `Sensitive clip SECRET` without countdown.
- `docs/checkpoints/2026-05-20-1604.md`: current checkpoint.

## Verification

- Command: `rtk swift test --filter appStatePersistsModuleToggleChanges`
  - Result: passed, 1 Testing test.
- Command: `rtk swift test --filter appStateRejectsModuleToggleWhenSideSlotsAreFull`
  - Result: passed, 1 Testing test.
- Command: `rtk swift test --filter AppStateHoldDropFeedbackTests`
  - Result: passed, 4 AppState/Hold tests.
- Command: `rtk swift test`
  - Result: passed, 205 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; debug app was built, signed with the Apple Development identity, and Flowline is running.
- Command: `rtk defaults read dev.kyylian.flowline`
  - Result: restored module defaults confirmed after live toggle probing.

## Open Questions / Risks

- Finder/text/URL Hold drag/drop still needs physical live QA.
- Settings UI automation remains somewhat brittle: Accessibility sometimes loses the Settings window reference after a close/toggle, so long-term UI automation should use a dedicated harness if this becomes recurring.
- Push remains blocked until a git remote is configured.
- Release packaging remains blocked until a Developer ID Application certificate is available and `FLOWLINE_DEVELOPER_ID_IDENTITY` is set.

## Next Steps

1. Commit this checkpoint update.
2. Eliminate the next local QA target: live Finder/text/URL Hold drag/drop.
3. Configure git remote and Developer ID release identity when ready to push/package.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status --short`, `rtk git log --oneline -5`, and `docs/checkpoints/LATEST.md`. The best next local target is physical/live Hold drag/drop QA; remote push and Developer ID packaging remain external blockers.
