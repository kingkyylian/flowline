# Project Checkpoint - 2026-05-11 22:08 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with active Flowline notch/HOLD/LIMITS changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: Flowline expanded notch is now centered around fixed LIMITS plus optional Workspace/Music/Hold modules; HOLD is the main active polish area.

## Done This Session

- Continued the HOLD redesign from a simple shelf into a usable side slot:
  - empty HOLD shows a compact `Drop here` tray
  - screenshots, text, code, links, and files can be held through the same surface
  - screenshot/file drag toward collapsed notch can expand Flowline
  - dropping into the HOLD frame routes through AppKit drag destination handling
  - dragging an existing HOLD item back out exports a useful provider payload
- Tightened HOLD actions:
  - action count stays small instead of four-button clutter
  - text/code/link expose copy/delete behavior
  - screenshot can expose OCR copy when text exists
  - action button strokes were removed to keep the visual quieter
- Added screenshot-aware HOLD storage behavior:
  - watched screenshot directories are scanned quickly
  - received screenshot promises/raw images are moved into Flowline's stash
  - deleting, clearing, or expiring HOLD screenshots cleans stashed files
  - user-owned ordinary files are not deleted by cleanup paths
- Added large text/code support:
  - large copied code blocks stay as full `code` values
  - metadata shows line/character count where useful
  - context summaries include HOLD while masking sensitive clips
- Added HOLD item recognition polish:
  - text/code thumbnails render a tiny first-lines preview
  - item title comes from the first meaningful text/code line
  - hover peek shows up to five readable text/code lines
  - sensitive clips never expose their raw value in thumbnail, peek, title, or tooltip
- Added HOLD drop feedback:
  - when drag targets HOLD, the tray gets a subtle recessed/depth treatment
  - when a drop lands, the landing feedback briefly holds then settles
- Reworked module placement:
  - fixed LIMITS is always counted in overlay capacity
  - optional modules are capped at 3 overlay modules total including LIMITS
  - Workspace owns the left slot when enabled
  - Music owns the right slot when enabled
  - Hold moves to the open side slot and is never rendered twice
- Reworked settings module rows:
  - Workspace, Music, Calendar, and Hold show their intended slot labels
  - module count displays as `OVERLAY MODULES n/3`
  - fourth optional module selection is rejected
- Improved LIMITS:
  - Codex, Claude, and Gemini rows can be sourced directly from local providers, not only from CodexBar presence
  - reset labels show compact time until reset when available
  - rows were resized/repositioned to fit the narrow center area better
  - provider names use fixed readable sizing
- Adjusted MUSIC/HOLD/LIMITS positioning:
  - MUSIC header was moved into the upper notch space and nudged right/up through layout constants
  - HOLD header aligns vertically with MUSIC without horizontal drift
  - LIMITS was moved right and scaled to make provider rows/symbols more legible

## Current State

- Flowline was rebuilt, signed, relaunched, and verified running after the latest HOLD preview changes.
- Worktree is still dirty and uncommitted.
- `rtk git diff --stat` currently shows 19 tracked files changed with about 2251 insertions and 441 deletions, plus many new untracked support/test files.
- `docs/checkpoints/LATEST.md` is intentionally updated to point to this checkpoint.

## Important Files / Artifacts

- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`
  - expanded notch layout, HOLD tray, HOLD actions, preview/peek UI, LIMITS rows, MUSIC/HOLD side column rendering.
- `Sources/FlowlineApp/App/OverlayHostingView.swift`
  - AppKit drag destination bridge for expanding collapsed notch and routing drops into HOLD.
- `Sources/FlowlineApp/Stores/AppState.swift`
  - HOLD drop targeted/landing state, direct HOLD insert/copy/OCR/export actions, module state wiring.
- `Sources/FlowlineApp/Services/ShelfService.swift`
  - direct text/link/screenshot insertion, screenshot directory watcher, stash cleanup, OCR update path.
- `Sources/FlowlineApp/Services/AIUsageDirectClients.swift`
  - direct local usage sources for Codex/Claude/Gemini-style limits.
- `Sources/FlowlineApp/Support/HoldPreviewPresentation.swift`
  - safe text/code title, thumbnail, hover peek, and tooltip formatting.
- `Sources/FlowlineApp/Support/HoldActionPresentation.swift`
  - keeps HOLD action buttons constrained and context-aware.
- `Sources/FlowlineApp/Support/NotchHoldTrayLayout.swift`
  - HOLD tray dimensions, empty-state positioning, recessed drop feedback, peek sizing.
- `Sources/FlowlineApp/Support/NotchModuleSlotLayout.swift`
  - deterministic Workspace/Music/Hold side-slot placement.
- `Sources/FlowlineApp/Support/NotchAgentColumnLayout.swift`
  - LIMITS row sizing, reset chip sizing, and action visibility rules.
- `Sources/FlowlineCore/Models/FlowlineModulePreferences.swift`
  - module capacity and toggle validation.
- `Tests/FlowlineAppTests/HoldPreviewPresentationTests.swift`
  - verifies readable text/code preview and sensitive masking.
- `Tests/FlowlineAppTests/NotchHoldTrayLayoutTests.swift`
  - verifies HOLD tray fit, empty alignment, larger preview surface, and no outer/action button strokes.
- `Tests/FlowlineAppTests/NotchModuleSlotLayoutTests.swift`
  - verifies HOLD placement and duplicate prevention.
- `Tests/FlowlineAppTests/AIUsageDirectClientTests.swift`
  - verifies direct provider parsing/auth behavior.

## Verification

- Command: `rtk swift test --filter HoldPreviewPresentationTests`
  - Result: passed; 4 tests.
- Command: `rtk swift test`
  - Result: passed; 131 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.

## Open Questions / Risks

- Real physical smoke is still the decisive check for HOLD feel:
  - drag a fresh macOS floating screenshot thumbnail into the notch
  - drop it on HOLD
  - drag it back out into Terminal and confirm the shell path/file behavior
  - test a 4000-line copied code block into HOLD and inspect title/peek/readability
- OCR is useful but the side slot is narrow; keep it action-based rather than always showing OCR text inline.
- Hover peek should be checked visually against real notch geometry so it does not cover the MUSIC/LIMITS content in a bad way.
- Calendar module is still not designed as a real product surface; only its module accounting/setting behavior is covered.
- Marketing/polish work is still pending after HOLD is locked.

## Next Steps

1. Physically test HOLD with screenshot drag-in, screenshot drag-out to Terminal, text/code drag-in, and hover peek.
2. Tune HOLD preview/peek position only if the physical test shows overlap or insufficient readability.
3. Commit the stabilized HOLD/LIMITS/module batch, then move to remaining small polish and marketing positioning.

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For behavior changes, prefer test-first and keep changes scoped.
- Do not revert unrelated user changes in the dirty worktree.

## Resume Prompt

Continue from this checkpoint. First read this file and `AGENTS.md`, then physically test HOLD with screenshot drag-in/out and a large code block before changing layout again.
