# Project Checkpoint - 2026-05-20 17:18 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and tracking `origin/main`
- Latest implementation commit: `e6702a4 Fail notarization packaging before build without credentials`
- Remote: `origin -> https://github.com/kingkyylian/flowline.git`
- Context: Publish is guarded and pushed; the next release-path target eliminated was late failure in `--notarize` when notary credentials are absent.

## Done This Session

- Restored from the previous checkpoint and verified the current tree was clean.
- Selected the next local target from the remaining release blockers:
  - Developer ID/notary setup is external.
  - The script-side gap was that `script/package_release.sh --notarize` could reach build/sign work before reporting missing notary credentials.
- Added a test-first regression in `PermissionSurfaceTests`:
  - Simulates an installed Developer ID identity.
  - Fakes `swift` with a marker file.
  - Proves `--notarize` exits before build when notary credentials are missing.
- Updated `script/package_release.sh`:
  - Added a notary credential guard.
  - Runs that guard before release build work in `--notarize` mode.
- Updated `docs/RELEASE.md` to state that `--notarize` validates Developer ID and notary credentials before starting the release build.
- Committed and pushed the implementation to `origin/main`.

## Current State

- Working tree is clean.
- `main` is synchronized with `origin/main`.
- Publish preflight passes with network access.
- Full test suite passes with 218 tests.
- Release archive/notarization still requires external Apple credentials:
  - Developer ID Application identity.
  - Notary profile or Apple ID notary credentials.

## Important Files / Artifacts

- `script/package_release.sh`: notarization mode now fails before build if notary credentials are missing.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers the missing-notary-credentials fail-fast behavior.
- `docs/RELEASE.md`: documents the pre-build notary credential validation.
- `docs/checkpoints/2026-05-20-1718.md`: this checkpoint.

## Verification

- Command: `rtk swift test --filter notarizeReleaseFailsBeforeBuildWhenNotaryCredentialsAreMissing`
  - Result: first failed against old behavior, then passed after implementation.
- Command: `rtk bash -n script/package_release.sh script/publish_preflight.sh script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 9 tests.
- Command: `rtk swift test`
  - Result: passed, 218 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk ./script/package_release.sh --help`
  - Result: passed.
- Command: `rtk ./script/publish_preflight.sh`
  - Result: passed after push; secret scan passed and GitHub origin was reachable.
- Command: `rtk git push origin main`
  - Result: passed; remote `main` is at `e6702a4`.

## Open Questions / Risks

- The historical OAuth credential should still be treated as exposed if it was real. Rotate or revoke it in Google Cloud outside this repo.
- Developer ID Application certificate is still not installed locally, so real archive/notarization remains externally blocked.
- Notarization still needs `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.

## Next Steps

1. Rotate/revoke the exposed Google OAuth credential if it was real.
2. Install/select a Developer ID Application identity and set `FLOWLINE_DEVELOPER_ID_IDENTITY`.
3. Configure `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials, then run `script/package_release.sh --notarize`.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -8`, `docs/checkpoints/LATEST.md`, and `script/package_release.sh`. Publish is guarded and pushed; release packaging now fails early for missing Developer ID or notary credentials, but real notarization still needs external Apple credentials.
