# Project Checkpoint - 2026-05-12 21:42 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with active Flowline notch/HOLD/LIMITS/module changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: `LIMITS` is fixed in the center; users can choose up to two optional side modules.

## Done This Session

- Preserved the side-module product model:
  - `LIMITS` stays fixed center and is not removable.
  - Optional modules are side-only.
  - User can enable at most two optional modules for left/right.
- Added and refined Calendar as a side column:
  - Calendar can render in left or right side slots.
  - Right-side Calendar now uses the same horizontal rhythm as Music:
    - same header lift
    - same right-column safe leading inset
    - same fixed content width as the Music timeline
  - Left-side Calendar stays within the narrower context column.
- Fixed Hold header when Hold is on the right side:
  - Root cause: `HOLD` header used left-column padding even when rendered in the right utility column.
  - Right-side Hold now uses Music's safe inset and header x shift, avoiding the physical notch cut.
  - Left-side Hold keeps the previous standard inset.
- Raised Workspace header to match Hold:
  - `WORKSPACE` now uses the same header lift as `HOLD`.
  - Only the header moved; Workspace body content was not changed.
- Preserved previous release-candidate fixes:
  - Screenshot drag into the small notch opens HOLD.
  - Ordinary Terminal/window dragging does not open HOLD.
  - Clipboard text capture is opt-in and faster when enabled.
  - Screenshot capture remains user-controlled.

## Current State

- Flowline was rebuilt, signed, relaunched, and verified running after the latest header alignment fixes.
- Full Swift test suite passes with 160 tests.
- Worktree remains large and uncommitted.
- `docs/checkpoints/LATEST.md` points to this checkpoint.

## Important Files / Artifacts

- `Sources/FlowlineApp/Support/NotchUtilityMusicLayout.swift`
  - Defines shared Music/Calendar/Hold/Workspace notch header layout constants.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`
  - Applies placement-aware Calendar and Hold layout.
  - Applies Workspace header lift.
- `Tests/FlowlineAppTests/NotchUtilityMusicLayoutTests.swift`
  - Covers Music layout, Calendar left/right layout, Hold right-side safe inset, and Workspace/Hold header alignment.
- `Sources/FlowlineApp/Support/NotchModuleSlotLayout.swift`
  - Maps enabled optional modules into left/right slots.
- `Sources/FlowlineCore/Models/FlowlineModulePreferences.swift`
  - Enforces two optional side modules and side placement preferences.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`
  - Settings UI shows `2/2 + LIMITS` module model.
- `docs/checkpoints/2026-05-12-2142.md`
  - This checkpoint.

## Verification

- Command: `rtk swift test --filter NotchUtilityMusicLayoutTests`
  - Result: passed; 10 tests.
- Command: `rtk swift test`
  - Result: passed; 160 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.

## Open Questions / Risks

- User should physically verify the exact visual position on the real notch:
  - Workspace + Hold
  - Workspace + Calendar
  - Music + Hold
  - Music + Calendar
- The worktree contains many related uncommitted changes; stage by feature group before commit.
- No known blocker after the latest physical feedback loop.

## Next Steps

1. Physically test the four key module combinations around the notch.
2. If visual alignment is accepted, do one self-review pass over the large dirty diff.
3. Commit the stabilized HOLD/LIMITS/module/header alignment batch.

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For behavior changes, prefer test-first and keep changes scoped.
- Do not revert unrelated user changes in the dirty worktree.

## Resume Prompt

Continue from this checkpoint. First read this file and the project instructions, then physically validate side module combinations around the physical notch before committing.
