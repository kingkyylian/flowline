# Project Checkpoint - 2026-05-21 15:18 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit before this checkpoint:
  - `2a797bb Scope release certificate keychain access`
- Context: Continued autonomous release hardening by fixing the Developer ID certificate import in the release candidate workflow.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and inspected the new release candidate workflows.
- Identified a concrete real-run risk in `.github/workflows/release-candidate.yml`:
  - `security import` used `-t cert`, which can narrow import behavior toward certificate-only material even though the release workflow needs the private key from the `.p12`.
  - `security import` also used `-A`, granting broad key access to all applications.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `releaseCandidateWorkflowImportsDeveloperIDPrivateKeyWithScopedToolAccess`
  - The test requires `-f pkcs12`, scoped `-T /usr/bin/codesign`, scoped `-T /usr/bin/security`, and rejects `-t cert` plus `-A`.
- Updated `.github/workflows/release-candidate.yml`:
  - Removed `-t cert`.
  - Removed broad `-A`.
  - Kept `-f pkcs12`.
  - Added scoped tool access for `/usr/bin/codesign` and `/usr/bin/security`.
- Pushed implementation commit `2a797bb`.
- GitHub Actions run `26225284120` passed for `2a797bb`.

## Current State

- Working tree was clean after pushing `2a797bb` and before writing this checkpoint.
- `main` and `origin/main` were aligned at `2a797bbd8e03abea4d868b7a8346653cd1fae302`.
- GitHub Actions is green on current implementation head:
  - Run: `26225284120`
  - Workflow: `CI`
  - Head: `2a797bbd8e03abea4d868b7a8346653cd1fae302`
  - Result: `success`
  - Job: `Swift tests`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.

## Important Files / Artifacts

- `.github/workflows/release-candidate.yml`: imports the Developer ID `.p12` without `-t cert` or broad `-A`; access is scoped to codesign/security.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers release workflow signing import semantics.
- `.github/workflows/release-candidate-verify.yml`: unchanged from the previous checkpoint; still verifies the completed candidate artifact with `--require-ci --require-artifact`.

## Verification

- Command: `rtk swift test --filter ScopedToolAccess`
  - Result: first failed against the old workflow, then passed after the fix.
- Command: `rtk swift test --filter 'releaseCandidate|ScopedToolAccess|PermissionSurfaceTests'`
  - Result: passed, 53 tests.
- Command: `rtk ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/ci.yml .github/workflows/release-candidate.yml .github/workflows/release-candidate-verify.yml`
  - Result: passed.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 262 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26225284120 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `2a797bb`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- No live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `2a797bb`; release candidate signing import now uses scoped key access and no `-t cert` private-key risk. The remaining release step is to configure Apple signing/notary secrets and execute a real candidate run.
