# Project Checkpoint - 2026-05-15 17:31

## Project

- Root: `/Users/kyylian/flowline`
- Current directory: `/Users/kyylian/flowline/Sources/FlowlineApp`
- Git: `main`, dirty working tree with broad Flowline app/core/test changes.
- Recent commit: `c2fc8f9 Record notch UI checkpoint`
- Context: Chosen goal was to establish a fresh baseline for the current dirty tree before more product work.

## Done This Session

- Ran full SwiftPM tests from the project root.
- Ran the app build/sign/launch verification script.
- Updated the checkpoint so future resume state reflects the fresh verification baseline.
- No product source code was changed during this baseline pass.

## Current State

- The working tree is still intentionally dirty. Do not reset, checkout, or revert broad files without explicit user direction.
- The broad uncommitted feature set currently compiles, passes tests, signs, launches, and leaves Flowline running.
- Modified tracked files remain concentrated in app lifecycle, overlay hosting/controller, notch overlay rendering, settings UI, AI usage, calendar, shelf/HOLD, FlowlineCore preferences/store support, tests, and packaging scripts.
- Untracked files add new support/presentation/layout/service types and many focused FlowlineApp/FlowlineCore tests.
- Existing checkpoint files from 2026-05-11 onward remain untracked in git.

## Important Files / Artifacts

- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`: major notch module composition changes; routes left/right slots and includes HOLD/calendar/agent/music rendering work.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`: redesigned settings categories for general, HOLD, modules, shortcuts, and about.
- `Sources/FlowlineApp/Stores/AppState.swift`: central state wiring for settings, module, HOLD, shelf, and permission behavior.
- `Sources/FlowlineApp/Services/ShelfService.swift`: large shelf/HOLD service expansion.
- `Sources/FlowlineCore/Services/ShelfStore.swift`: persistence changes for shelf/HOLD data.
- `Sources/FlowlineCore/Models/FlowlineModulePreferences.swift`: new/expanded module preference model.
- `Sources/FlowlineApp/Support/NotchHoldTrayLayout.swift`: untracked layout metrics for HOLD tray and action row.
- `Sources/FlowlineApp/Support/NotchModuleSlotLayout.swift`: untracked layout logic for module placement.
- `Sources/FlowlineApp/Support/SettingsPresentation.swift`: untracked presentation model for settings panels.
- `Sources/FlowlineApp/Services/AIUsageDirectClients.swift`: untracked direct AI usage client support.
- `Sources/FlowlineCore/Services/ScreenshotFileScanner.swift`: untracked screenshot scanner support.
- `Tests/FlowlineAppTests/*`: focused tests for presentation, layout, HOLD, settings, permission, shelf, and AI usage behavior.
- `dist/Flowline.app`: rebuilt, signed, and launched by `rtk ./script/build_and_run.sh --verify`.

## Verification

- Command: `rtk swift test`
  - Result: passed, 186 tests.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; `dist/Flowline.app` was signed and Flowline is running.
- Command: `rtk git diff --check`
  - Result: passed earlier in this checkpoint sequence.

## Project Rules That Affect Resume

- Use `rtk` for shell commands.
- Communicate in Turkish, concise and direct.
- Preserve unrelated dirty worktree changes.
- Prefer `apply_patch` for manual file edits.
- Before claiming completion after code changes, run fresh relevant verification.

## Open Questions / Risks

- The next meaningful gap is not compilation; it is scope control and visual/product QA.
- UI-heavy notch/settings changes still need live visual review for alignment, overflow, and interaction feel.
- The worktree is broad enough that commits should be split by area rather than landed as one blob.
- New checkpoint files and new tests are still untracked; decide what belongs in the final commit set.

## Next Steps

1. Do a live Flowline UI pass: notch expanded/collapsed, HOLD drag/drop, settings panels, module toggles, calendar permission state, AI usage refresh.
2. Review the dirty tree by area and split commit candidates: notch/modules, HOLD/shelf, settings/about, AI usage, screenshot/OCR, tests, and scripts.
3. After visual fixes, rerun `rtk swift test`, `rtk ./script/build_and_run.sh --verify`, and `rtk git diff --check`.

## Resume Prompt

Continue from this checkpoint. First read this file and the active user instructions from the conversation, then inspect current `rtk git status --short` and the key changed files above before making changes. Do not revert unrelated worktree changes.
