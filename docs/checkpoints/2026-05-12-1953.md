# Project Checkpoint - 2026-05-12 19:53 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with active Flowline notch/HOLD/LIMITS/module changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: HOLD is now a polished side-slot surface with user-controlled automatic capture.

## Done This Session

- Reworked HOLD text/code hover behavior:
  - removed the floating peek card that covered `COPY/NEXT/DEL`
  - hover now swaps tray content into an inline 4-line preview inside the tray
  - long native tooltip text was reduced to a compact title help string
- Polished empty HOLD state:
  - enlarged the drop arrow
  - placed the arrow beside `Drop here`
  - centered the arrow/title group over `ss - text - code`
- Changed screenshot copy behavior:
  - screenshot primary action label is now `IMG`
  - clicking `IMG` copies actual image data instead of a shell path
  - ordinary file items still copy URL/path payloads
  - OCR copy is only exposed through explicit `TEXT`
  - copied HOLD payloads are marked handled so HOLD does not re-import its own clipboard write
- Improved screenshot drag-out behavior:
  - screenshot/file drag provider now includes file URL, shell-safe plain text path, legacy string type, and `NSFilenamesPboardType`
  - user physically tested screenshot drag to Terminal and confirmed it works
- Fixed right-slot HOLD:
  - when Workspace owns the left slot and Hold moves right, the app now renders the real interactive `NotchHoldColumn`
  - expanded HOLD drop target follows Hold placement and accepts right-slot drops
  - removed old non-interactive right-side hold summary code
- Prevented plain window drags from opening the collapsed notch:
  - global mouse-drag expansion now requires a HOLD payload signal
  - ordinary Terminal/window dragging no longer opens the panel
  - real file/text/image drops still expand through the AppKit drop pasteboard path
- Made automatic HOLD capture user-controlled:
  - plain clipboard text is no longer auto-captured by default, so `ctrl+c` remains user-controlled
  - screenshot auto-capture remains enabled by default for users who want screenshots to land in HOLD immediately
  - Settings > Behavior now has `Hold capture` toggles for `Screenshots` and `Clipboard text`
  - turning auto-capture on marks the current pasteboard as handled, so old clipboard contents are not pulled in retroactively
  - manual drag/drop and explicit HOLD add flows still work even when automatic capture is disabled

## Current State

- Flowline has been rebuilt, signed, relaunched, and verified running.
- Worktree is still dirty and uncommitted.
- `rtk git status --short` shows the large existing HOLD/LIMITS/module batch plus the latest ShelfService/AppState/Settings/test changes.
- `docs/checkpoints/LATEST.md` points to this checkpoint.

## Important Files / Artifacts

- `Sources/FlowlineApp/Services/ShelfService.swift`
  - owns HOLD source polling, screenshot stash lifecycle, OCR, self-copy guard, and `ShelfAutoCaptureOptions`.
- `Sources/FlowlineApp/Stores/AppState.swift`
  - persists `hold.autoCapture.screenshots` and `hold.autoCapture.clipboardText`, then applies them to `ShelfService`.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`
  - adds the `Hold capture` settings toggles.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`
  - HOLD tray UI, inline preview, empty state, action row, shared left/right Hold rendering.
- `Sources/FlowlineApp/App/OverlayHostingView.swift`
  - AppKit drag destination bridge; expanded drop target uses module preferences.
- `Sources/FlowlineApp/Support/OverlayDragActivation.swift`
  - prevents ordinary mouse/window drags from opening the collapsed notch.
- `Sources/FlowlineApp/Support/HoldItemTransfer.swift`
  - screenshot image clipboard copy and Terminal-compatible drag payloads.
- `Sources/FlowlineApp/Support/HoldActionPresentation.swift`
  - screenshot primary action is `IMG`; OCR copy stays explicit as `TEXT`.
- `Tests/FlowlineAppTests/ShelfServiceTests.swift`
  - covers default no-text clipboard auto-capture, opt-in text capture, disabled screenshot auto-capture, manual drops, OCR, cleanup, and self-copy guard.
- `Tests/FlowlineAppTests/OverlayDragActivationTests.swift`
  - verifies HOLD payload drags expand the notch while plain window drags do not.

## Verification

- Command: `rtk swift test --filter shelfServiceLeavesClipboardTextForUserByDefault`
  - Result: failed first as expected; proved the old auto text capture behavior.
- Command: `rtk swift test --filter ShelfServiceTests`
  - Result: passed; 19 tests.
- Command: `rtk swift test`
  - Result: passed; 145 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.
- Physical validation:
  - user tested screenshot drag/drop/export flow and reported it is working well before the latest auto-capture toggle change.

## Open Questions / Risks

- Physical validation is still useful for the new Settings toggles: confirm `Clipboard text` off ignores `ctrl+c`, on captures only new copied text.
- Right-slot HOLD should still be physically tested once with Workspace enabled and Music disabled.
- The worktree is large; before commit, stage related files carefully and avoid mixing unrelated future polish.

## Next Steps

1. Physically test Settings > Behavior > Hold capture with clipboard text off/on and screenshot capture off/on.
2. Physically test right-slot Hold by enabling Workspace + Hold and disabling Music, then drop a screenshot onto the right-side Hold tray.
3. Commit the stabilized HOLD/LIMITS/module batch once physical validation is complete.

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For behavior changes, prefer test-first and keep changes scoped.
- Do not revert unrelated user changes in the dirty worktree.

## Resume Prompt

Continue from this checkpoint. First read this file and the project instructions, then physically validate the Hold capture toggles and right-slot Hold before committing the batch.
