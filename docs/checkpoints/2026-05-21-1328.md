# Project Checkpoint - 2026-05-21 13:28 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit before this checkpoint:
  - `92bf235 Require pushed HEAD before publishing`
- Context: Continued autonomous publish/release hardening after Apple ID notarization coverage.

## Done This Session

- Restored current state from `docs/checkpoints/LATEST.md` and verified `main` was clean at `5638ae1`.
- Selected the next credential-free target in publish safety:
  - `script/publish_preflight.sh` checked clean worktree, secret scan, GitHub origin shape, and reachability.
  - It did not prove the local `HEAD` was actually pushed to `origin`.
  - This could allow a local-only commit to report publish preflight success.
- Added RED regression coverage:
  - `publishPreflightFailsWhenLocalHeadIsNotPushedToOrigin`
  - Builds a clean temporary repo with two commits.
  - Uses a fake `rtk git ls-remote --exit-code origin HEAD` returning the first commit as remote `HEAD`.
  - Confirmed current behavior failed the test because preflight exited `0`.
- Updated `script/publish_preflight.sh`:
  - Added `origin_head_sha`.
  - Captures `origin HEAD` via `rtk git ls-remote --exit-code origin HEAD` when `rtk` is available.
  - Falls back to `git ls-remote --exit-code origin HEAD`.
  - Compares `git rev-parse HEAD` against the remote `HEAD`.
  - Fails with `local HEAD is not pushed to origin` when they differ.
- Pushed `92bf235`; GitHub Actions run `26220316784` passed.

## Current State

- Working tree was clean after pushing `92bf235` and before writing this checkpoint.
- `main` and `origin/main` were aligned at `92bf235dc1516048e8cc2ee28b82ffc2cab309e3`.
- GitHub Actions is green on current implementation head:
  - Run: `26220316784`
  - Head: `92bf235dc1516048e8cc2ee28b82ffc2cab309e3`
  - Result: `success`
  - Job: `Swift tests`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.
- Local full test suite passes with 236 tests.

## Important Files / Artifacts

- `script/publish_preflight.sh`: now requires local `HEAD` to match `origin HEAD`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers the unpushed-local-HEAD preflight failure.
- `docs/checkpoints/2026-05-21-1328.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: should match this checkpoint.

## Verification

- Command: `rtk swift test --filter publishPreflightFailsWhenLocalHeadIsNotPushedToOrigin`
  - Result: first failed correctly because preflight exited `0`; passed after implementation.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 27 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 236 tests.
- Command: `rtk ./script/publish_preflight.sh`
  - Result: passed after push; local `HEAD` matched `origin HEAD`.
- Command: `rtk gh run watch 26220316784 --repo kingkyylian/flowline --exit-status`
  - Result: passed for `92bf235`.

## Open Questions / Risks

- Historical Google OAuth credential still needs external rotation/revocation if it was real.
- Real release packaging/notarization still requires external Apple credentials:
  - Developer ID Application certificate.
  - `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.
- The real notarization service and Gatekeeper assessment still need one end-to-end run with real credentials before a public release.
- Publish preflight now proves a clean tree, secret scan, reachable GitHub origin, and pushed `HEAD`; it does not validate tags or release artifact contents.

## Next Steps

1. Add a release-tag preflight guard before tagging/publishing a public release.
2. Run one real notarized release when Apple Developer ID and notary credentials are available.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `script/publish_preflight.sh`, `script/package_release.sh`, `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`, and the latest GitHub Actions run for `main`. CI is green on `92bf235`; publish preflight now fails if local `HEAD` is not pushed to `origin`. Follow project rules from `AGENTS.md`: use `rtk`, preserve unrelated changes, and verify before claiming completion.
