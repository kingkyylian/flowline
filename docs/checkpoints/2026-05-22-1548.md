# Project Checkpoint - 2026-05-22 15:48 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit:
  - `aebd205 Harden release dispatch tag handling`
- Context: Continued autonomous release hardening by removing direct shell interpolation of the manual release tag input.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed the previous checkpoint CI was green.
- Selected the next secret-free release hardening target in `.github/workflows/release-candidate.yml`:
  - `workflow_dispatch` tag input was assigned inside a shell `run:` block as `RELEASE_TAG="${{ inputs.tag }}"`.
  - The regex validation was present, but shell parsing happens before that validation.
  - This made the manual dispatch input unnecessarily exposed to shell interpolation behavior.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `releaseCandidateWorkflowDoesNotInterpolateDispatchTagInsideShell`
  - The test first failed because the workflow lacked `RELEASE_TAG: ${{ inputs.tag }}` in step `env:` and still contained direct shell assignment from `${{ inputs.tag }}`.
- Updated `.github/workflows/release-candidate.yml`:
  - Moved the dispatch tag into the `Determine release metadata` step `env:`.
  - Kept the existing bash regex validation on `$RELEASE_TAG`.
  - Removed `RELEASE_TAG="${{ inputs.tag }}"` from the shell script.
- Pushed implementation commit `aebd205`.
- GitHub Actions run `26288540698` passed for `aebd205`.

## Current State

- The release hardening implementation is pushed at `aebd2059839c4b6ad46b5e950731d504baf4b466`.
- Working tree was clean before writing this checkpoint.
- Release identity/provenance checks now cover:
  - manual release tag input is passed through step environment before shell validation;
  - exact candidate artifact names;
  - artifact version to archive/tag binding;
  - manifest GitHub artifact name to release tag binding;
  - manifest workflow binding to `Release Candidate`;
  - actual GitHub run workflow binding to `Release Candidate`;
  - manifest GitHub run attempt binding to the actual run attempt;
  - manifest notarization binding to `notarized=true`;
  - downloaded artifact ZIP hash matching the local archive;
  - downloaded artifact manifest content matching the local manifest;
  - ambiguous downloaded archives and manifests;
  - CI run head/status matching;
  - release manifest archive name, version, checksum, size, and git commit.

## Important Files / Artifacts

- `.github/workflows/release-candidate.yml`: now routes manual `inputs.tag` through step `env:` instead of shell interpolation.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers the dispatch input handling invariant.
- `script/publish_preflight.sh`: unchanged this session; still enforces release manifest, CI, and artifact provenance.
- `.github/workflows/release-candidate-verify.yml`: unchanged this session; still runs `--require-ci --require-artifact` after candidate completion.

## Verification

- Command: `rtk swift test --filter DispatchTag`
  - Result: first failed against the old workflow, then passed after moving the tag input to step `env:`.
- Command: `rtk swift test --filter 'releaseCandidate|publishPreflight'`
  - Result: passed, 41 tests.
- Command: `rtk ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/ci.yml .github/workflows/release-candidate.yml .github/workflows/release-candidate-verify.yml`
  - Result: passed.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 277 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26288540698 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `aebd205`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.
- No local `AGENTS.md` file is present; session project rules still require `rtk` command prefix, TDD before behavior changes, and verification before completion claims.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `aebd205`; release candidate dispatch tag input now flows through step `env:` before shell validation instead of being interpolated directly inside the shell script.
