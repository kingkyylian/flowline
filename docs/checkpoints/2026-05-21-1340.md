# Project Checkpoint - 2026-05-21 13:40 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit before this checkpoint:
  - `682ba00 Guard release tags before publishing`
- Context: Continued autonomous publish/release hardening after pushed-HEAD preflight coverage.

## Done This Session

- Restored current state from `docs/checkpoints/LATEST.md` and verified `main` was clean at `c573acb`.
- Selected the next credential-free target from the checkpoint:
  - `script/publish_preflight.sh` proved clean tree, secret scan, reachable GitHub origin, and pushed `HEAD`.
  - It did not validate release tag safety before public tagging.
- Added RED/GREEN coverage for release tag preflight:
  - `publishPreflightFailsWhenReleaseTagAlreadyExistsOnOrigin`
  - `publishPreflightRejectsInvalidReleaseTag`
  - `publishPreflightFailsWhenReleaseTagAlreadyExistsLocally`
- Updated `script/publish_preflight.sh`:
  - Accepts optional `--tag vX.Y.Z`.
  - Rejects invalid tag names such as `1.2.3`.
  - Rejects tags that already exist locally in `refs/tags/<tag>`.
  - Rejects tags that already exist on `origin`.
- Updated docs:
  - `README.md` now shows `script/publish_preflight.sh --tag v0.1.0` before release tagging.
  - `docs/RELEASE.md` includes tag preflight in pre-tag checks.
- Pushed `682ba00`; GitHub Actions run `26220893320` passed.

## Current State

- Working tree was clean after pushing `682ba00` and before writing this checkpoint.
- `main` and `origin/main` were aligned at `682ba0023b86a125e8fcd2c8d32a20c5ea55de47`.
- GitHub Actions is green on current implementation head:
  - Run: `26220893320`
  - Head: `682ba0023b86a125e8fcd2c8d32a20c5ea55de47`
  - Result: `success`
  - Job: `Swift tests`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.
- Local full test suite passes with 239 tests.

## Important Files / Artifacts

- `script/publish_preflight.sh`: now guards optional release tags before publishing/tagging.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers invalid, local-existing, and origin-existing release tags.
- `docs/RELEASE.md`: documents tag preflight in pre-tag checks.
- `README.md`: documents tag-aware publish preflight.
- `docs/checkpoints/2026-05-21-1340.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: should match this checkpoint.

## Verification

- Command: `rtk swift test --filter publishPreflightFailsWhenReleaseTagAlreadyExistsOnOrigin`
  - Result: first failed because `--tag` only printed usage; passed after implementation.
- Command: `rtk swift test --filter publishPreflightRejectsInvalidReleaseTag`
  - Result: first failed because invalid tag `1.2.3` passed; passed after implementation.
- Command: `rtk swift test --filter publishPreflightFailsWhenReleaseTagAlreadyExistsLocally`
  - Result: first failed because an existing local tag passed; passed after implementation.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 30 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 239 tests.
- Command: `rtk ./script/publish_preflight.sh --tag v0.1.0`
  - Result: passed after push; local/remote tag checks did not find `v0.1.0`.
- Command: `rtk gh run watch 26220893320 --repo kingkyylian/flowline --exit-status`
  - Result: passed for `682ba00`.

## Open Questions / Risks

- Historical Google OAuth credential still needs external rotation/revocation if it was real.
- Real release packaging/notarization still requires external Apple credentials:
  - Developer ID Application certificate.
  - `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.
- The real notarization service and Gatekeeper assessment still need one end-to-end run with real credentials before a public release.
- Tag preflight prevents invalid/reused tags, but there is still no automated check that a release archive exists and corresponds to the tag version.

## Next Steps

1. Add a release artifact/version preflight guard so a tag cannot be created without a matching archive/version.
2. Run one real notarized release when Apple Developer ID and notary credentials are available.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`, and the latest GitHub Actions run for `main`. CI is green on `682ba00`; publish preflight now supports `--tag vX.Y.Z` and rejects invalid, local-existing, and origin-existing release tags. Follow project rules from `AGENTS.md`: use `rtk`, preserve unrelated changes, and verify before claiming completion.
