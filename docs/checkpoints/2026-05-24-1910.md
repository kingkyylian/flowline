# Project Checkpoint - 2026-05-24 19:10 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, synchronized with `origin/main`
- Head: `5047b15b413d49e51d71d27fb495456b438b87a3`
- Context: Release-candidate execution is still blocked by missing external Apple/GitHub credentials, but the repo-side release helpers are now guarded and freshly verified.

## Done This Session

- Re-audited the release helper and guarded release-runner flow.
- Hardened `script/configure_release_secrets.sh`:
  - Verifies explicit `--repo` matches the local GitHub `origin`.
  - Uses paginated GitHub Actions secret listing, so `--check` does not silently miss secrets beyond the first API page.
  - Rejects conflicting `--dry-run --check` mode combinations instead of silently preferring `--check`.
  - Keeps secret values out of command arguments and output.
- Kept `script/run_release_candidate.sh` as the guarded dispatch path:
  - Requires `main`.
  - Rejects reused local or remote tags.
  - Runs `script/publish_preflight.sh`.
  - Requires `script/configure_release_secrets.sh --check`.
  - Dispatches `release-candidate.yml` only after those gates pass.
- Expanded `Tests/FlowlineAppTests/PermissionSurfaceTests.swift` release coverage:
  - Secret helper now has 6 focused tests.
  - Release runner still has 3 focused tests.
  - Full suite now passes with 288 tests.

## Current State

- Working tree is dirty with release-helper, release-runner, docs, tests, and checkpoint work:
  - `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`
  - `docs/RELEASE.md`
  - `docs/checkpoints/LATEST.md`
  - `docs/checkpoints/2026-05-24-1810.md`
  - `docs/checkpoints/2026-05-24-1821.md`
  - `docs/checkpoints/2026-05-24-1828.md`
  - `docs/checkpoints/2026-05-24-1836.md`
  - `docs/checkpoints/2026-05-24-1900.md`
  - `docs/checkpoints/2026-05-24-1910.md`
  - `script/configure_release_secrets.sh`
  - `script/run_release_candidate.sh`
- Source-side release gates are passing.
- No release-candidate workflow has been dispatched from this dirty/unpushed worktree.
- No notarized release artifact exists yet from this helper flow.

## Important Files / Artifacts

- `script/configure_release_secrets.sh`: validates/configures/checks release GitHub Actions secrets; now rejects repo mismatch, paginates secret-name checks, and rejects conflicting modes.
- `script/run_release_candidate.sh`: guarded workflow dispatch helper for `release-candidate.yml`.
- `docs/RELEASE.md`: documents secret setup, guarded dispatch, candidate verification, and publish preflight.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: release helper, release runner, release workflow, package, preflight, and secret-scan test coverage.
- `docs/checkpoints/2026-05-24-1910.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: updated to this checkpoint.

## Verification

- Command: `rtk swift test --filter releaseSecretSetup`
  - Result: passed; 6 focused secret-helper tests passed.
- Command: `script/configure_release_secrets.sh --repo kingkyylian/flowline --dry-run`
  - Result: expected failure; all required local secret inputs are missing from the environment.
- Command: `script/configure_release_secrets.sh --repo kingkyylian/flowline --check`
  - Result: expected failure; all 7 required GitHub Actions release secret names are missing.
- Command: `script/configure_release_secrets.sh --repo kingkyylian/flowline --dry-run --check`
  - Result: expected failure; conflicting modes are rejected.
- Command: `script/configure_release_secrets.sh --repo example/other --dry-run`
  - Result: expected failure; explicit repo mismatch with `origin` is rejected before secret work.
- Command: `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0 --dry-run`
  - Result: expected failure; stopped because the worktree has uncommitted changes.
- Command: `script/secret_scan.sh`
  - Result: passed.
- Command: `git diff --check`
  - Result: passed.
- Command: `bash -n script/configure_release_secrets.sh script/run_release_candidate.sh`
  - Result: passed.
- Command: `rg -n '[[:blank:]]+$' <touched files>`
  - Result: passed; no trailing whitespace matches.
- Command: `rtk swift test`
  - Result: passed; `Test run with 288 tests in 0 suites passed after 14.302 seconds`.
- Command: `rtk swift build -c release`
  - Result: passed; release build complete.
- Command: `git status --short --branch`
  - Result: branch is synchronized with `origin/main`; helper/test/docs/checkpoint files are uncommitted.

## Open Questions / Risks

- The release-candidate workflow still requires these repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- Local notarized packaging still requires an installed `Developer ID Application` identity, or CI must import it from the configured certificate secret.
- Helper/test/docs/checkpoint changes are not committed or pushed.
- A real release candidate still requires a clean pushed commit, configured secrets, successful `Release Candidate`, successful `Release Candidate Verify`, and publish preflight evidence against the uploaded notarized artifact.

## Next Steps

1. Commit and push the release helper/test/docs/checkpoint changes after explicit approval.
2. Provide the Developer ID `.p12`, certificate password, Developer ID identity string, Apple ID, Team ID, and app-specific password.
3. Run `script/configure_release_secrets.sh --repo kingkyylian/flowline --dry-run`, then run it without `--dry-run`, then run `--check`.
4. Run `script/run_release_candidate.sh --repo kingkyylian/flowline --tag v0.1.0`.
5. Let `Release Candidate Verify` complete, then preserve the publish preflight evidence against the uploaded notarized artifact before creating or pushing the public release tag.

## Project Rules For Resume

- Use `rtk` for noisy shell commands.
- Keep Turkish communication short and concrete.
- Protect dirty worktree state.
- Do not commit or push without explicit user approval.
- Do not claim release completion without fresh verification against current files, GitHub state, and produced artifacts.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short --branch`, `docs/checkpoints/LATEST.md`, `script/configure_release_secrets.sh`, `script/run_release_candidate.sh`, `docs/RELEASE.md`, and GitHub Actions secret state. Do not run the `Release Candidate` workflow until `script/configure_release_secrets.sh --repo kingkyylian/flowline --check` passes and the helper changes are committed/pushed.
