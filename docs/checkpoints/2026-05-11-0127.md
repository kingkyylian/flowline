# Project Checkpoint - 2026-05-11 01:27 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`
- Worktree: dirty; HOLD feature changes are not committed.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: Replaced the weak `ACTIONS` notch area with a real HOLD clipboard/screenshot stash workflow.

## Done This Session

- Reworked the left notch column from `ACTIONS` to `HOLD`.
  - Empty state: `Watching` / `Copy text or link`.
  - Active state: clip count and current held item detail.
  - Actions: `COPY`, `NEXT`, `CLEAR`.
- Added HOLD behavior on top of the existing shelf infrastructure:
  - text and links are captured from the pasteboard
  - regular files still work
  - sensitive-looking text is masked as `Sensitive clip`
  - sensitive clips expire after 60 seconds
  - normal clips expire after 15 minutes
  - duplicate held items are deduped
  - `NEXT` cycles held items without losing them
- Added screenshot support:
  - screenshot files are a distinct `.screenshot` kind
  - normal photos/images are ignored
  - macOS screenshot filenames are detected in English and Turkish:
    - `Screen Shot`
    - `Screenshot`
    - `Ekran Resmi`
    - `screen capture`
    - `screencapture`
- Added automatic screenshot file capture:
  - scans Desktop
  - also scans custom macOS screenshot save location from `com.apple.screencapture.location`
  - moves saved screenshots into Flowline stash instead of leaving them on Desktop
  - stash path: `~/Library/Application Support/Flowline/Hold/Screenshots`
  - avoids adding the same screenshot twice
- Added clipboard screenshot capture:
  - image data from pasteboard is written as `Clipboard Screenshot yyyy-MM-dd HH.mm.ss.png`
  - clipboard image wins over fallback text when pasteboard has both image and string representations
  - the resulting stash file is shown as a HOLD screenshot item
- Updated Settings label:
  - `Shelf` -> `Hold`
  - detail: `Clipboard clips and dropped files`
- Updated context summary:
  - `Shelf:` -> `Hold:`
  - sensitive values are not emitted, only masked titles.
- Preserved critical actions after removing `ACTIONS`:
  - meeting / permission actions still surface under `LIMITS` when Workspace is disabled
  - `clearShelf` action title became `Clear Hold`.

## Current State

- Flowline is running from the latest dev build.
- Worktree has modified and untracked files from HOLD work.
- `rtk git status --short` before checkpoint:
  - modified:
    - `Sources/FlowlineApp/Services/ShelfService.swift`
    - `Sources/FlowlineApp/Stores/AppState.swift`
    - `Sources/FlowlineApp/Support/ActionBuilder.swift`
    - `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`
    - `Sources/FlowlineApp/Views/Settings/SettingsView.swift`
    - `Sources/FlowlineCore/Models/ShelfItem.swift`
    - `Sources/FlowlineCore/Services/ShelfStore.swift`
    - `Sources/FlowlineCore/Support/ContextSummaryBuilder.swift`
    - `Tests/FlowlineCoreTests/ContextSummaryBuilderTests.swift`
    - `Tests/FlowlineCoreTests/ShelfStoreTests.swift`
  - untracked:
    - `Sources/FlowlineCore/Services/ScreenshotFileScanner.swift`
    - `Tests/FlowlineAppTests/ShelfServiceTests.swift`
    - `Tests/FlowlineCoreTests/ScreenshotFileScannerTests.swift`

## Important Files / Artifacts

- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`
  - replaces default notch left column with HOLD UI
  - adds `NotchHoldColumn`
  - keeps contextual actions visible under `LIMITS`
  - displays screenshot kind with camera icon and `ss ...` detail
- `Sources/FlowlineApp/Services/ShelfService.swift`
  - central HOLD service behavior
  - polls pasteboard
  - scans screenshot directories
  - moves screenshot files into stash
  - writes clipboard images to PNG stash files
- `Sources/FlowlineCore/Services/ShelfStore.swift`
  - HOLD store semantics:
    - classification
    - TTL pruning
    - sensitive detection
    - screenshot/photo filtering
    - cycle/dedupe behavior
- `Sources/FlowlineCore/Services/ScreenshotFileScanner.swift`
  - scans screenshot directories for new screenshot files
  - ignores old files and repeated discoveries
- `Sources/FlowlineCore/Models/ShelfItem.swift`
  - added `.sensitive`, `.screenshot`, and `expiresAt`
- `Sources/FlowlineApp/Stores/AppState.swift`
  - added copy/cycle HOLD actions
  - copies file/screenshot URLs back to pasteboard
- `Tests/FlowlineCoreTests/ShelfStoreTests.swift`
  - covers classification, TTL, sensitive masking, screenshot filtering, cycling
- `Tests/FlowlineCoreTests/ScreenshotFileScannerTests.swift`
  - covers screenshot discovery and duplicate suppression
- `Tests/FlowlineAppTests/ShelfServiceTests.swift`
  - covers screenshot move-to-stash and clipboard image stash behavior

## Verification

- Command: `rtk swift test`
- Result: passed; 54 tests after clipboard image priority fix.
- Command: `rtk git diff --check`
- Result: passed; rerun fresh before writing checkpoint.
- Command: `rtk ./script/build_and_run.sh --verify`
- Result: passed; Flowline rebuilt, signed for development, relaunched, and running.

## Open Questions / Risks

- Manual QA still needed on the real macOS screenshot UI:
  - `Cmd-Shift-4` / save-to-desktop screenshot should move into stash and appear in HOLD.
  - `Ctrl-Cmd-Shift-4` / clipboard screenshot should appear in HOLD as `Clipboard Screenshot ...`.
  - If macOS screenshot UI puts a text representation into pasteboard alongside image, image should now win.
- Current screenshot file scanning is polling-based at 1 second, not FSEvents.
  - This is simpler and likely enough for release, but could miss unusual race cases if file creation is delayed.
- Stashed screenshot files currently live on disk until cleaned by user/app cleanup.
  - HOLD item expires in memory, but stash file cleanup policy has not been implemented yet.
- Sensitive text is heuristic-based.
  - It masks common token/password shapes but is not a formal secret detector.
- `git diff --stat` shown by `rtk` did not include untracked files; rely on `git status --short` for full changed-file scope.

## Next Steps

1. Manually test real screenshot flows:
   - Desktop save screenshot
   - clipboard screenshot
   - normal photo drag/drop should not enter HOLD
2. Add cleanup policy for old screenshot stash files, probably delete expired HOLD screenshot files after item expiry or on app launch.
3. Do a release readiness pass:
   - inspect UI width/overflow in notch
   - rerun `rtk swift test`
   - rerun `rtk ./script/build_and_run.sh --verify`
   - commit HOLD feature changes.

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For bug fixes, reproduce/root-cause first; for behavior changes, prefer test-first.
- Do not revert unrelated user changes.

## Resume Prompt

Continue from this checkpoint. First run `rtk git status --short`, then manually test the real macOS screenshot flows for HOLD. If screenshot capture works, implement stash cleanup before release; if it does not, inspect current pasteboard types and screenshot file movement before changing behavior.
