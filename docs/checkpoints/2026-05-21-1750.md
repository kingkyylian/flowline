# Project Checkpoint - 2026-05-21 17:50 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest implementation commit:
  - `388c66a Bind release artifact name to tag`
- Context: Continued autonomous release hardening by binding the manifest's GitHub Actions artifact name to the release tag.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current head/CI were clean.
- Selected the next secret-free release hardening target:
  - `Release Candidate` uploads artifacts as `flowline-release-vX.Y.Z`.
  - `script/publish_preflight.sh --require-artifact` checked that a manifest named an artifact, but did not require the artifact name to match the requested release tag.
  - A manifest for tag `v1.2.3` could therefore point at `flowline-release-v1.2.4` and still pass if the downloaded archive hash matched.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `publishPreflightRejectsCIManifestWhenArtifactNameDoesNotMatchTag` first failed because preflight passed a mismatched artifact name.
  - Existing positive artifact-provenance fixtures were aligned to the real workflow artifact naming shape: `flowline-release-v1.2.3`.
- Updated `script/publish_preflight.sh`:
  - Computes `expected_github_artifact_name="flowline-release-$release_tag"`.
  - Fails if `github_artifact_name` from the manifest does not match the release tag.
- Updated `README.md` and `docs/RELEASE.md` to document that `--require-artifact` requires the artifact name to match `flowline-release-vX.Y.Z`.
- Pushed implementation commit `388c66a`.
- GitHub Actions run `26233420235` passed for `388c66a`.

## Current State

- The release hardening implementation is pushed at `388c66a11ce8bec0027efc51e3d4cb885c9977b8`.
- Working tree was clean before writing this checkpoint.
- Release identity/provenance checks now cover:
  - exact candidate artifact names;
  - artifact version to archive/tag binding;
  - manifest GitHub artifact name to release tag binding;
  - ambiguous downloaded archives in both the verify workflow and publish preflight;
  - CI run head/status matching;
  - release manifest archive name, version, checksum, size, and git commit.

## Important Files / Artifacts

- `script/publish_preflight.sh`: now requires manifest `github_artifact_name` to equal `flowline-release-$release_tag`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers the new artifact-name/tag invariant and release provenance tests.
- `README.md`: documents the stricter artifact gate.
- `docs/RELEASE.md`: documents the stricter release artifact gate.
- `.github/workflows/release-candidate.yml`: produces the expected `flowline-release-vX.Y.Z` artifact name.
- `.github/workflows/release-candidate-verify.yml`: runs publish preflight with `--require-ci --require-artifact`.

## Verification

- Command: `rtk swift test --filter ArtifactNameDoesNotMatchTag`
  - Result: first failed against the old script because preflight passed the mismatched artifact name, then passed after enforcing the artifact-name/tag match.
- Command: `rtk swift test --filter 'publishPreflight|releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 61 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 270 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26233420235 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `388c66a`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `388c66a`; publish preflight now requires the manifest's GitHub Actions artifact name to match the release tag.
