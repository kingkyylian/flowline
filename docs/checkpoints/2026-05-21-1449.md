# Project Checkpoint - 2026-05-21 14:49 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit before this checkpoint:
  - `8960f3d Require release artifact provenance`
- Context: Continued autonomous release hardening by closing the remaining gap between a release manifest's CI provenance and the actual archive bytes.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed the previous target:
  - `--require-ci` proved a successful GitHub Actions run for `HEAD`, but did not prove the archive bytes came from that run.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - Release manifests record `github_artifact_name` when `GITHUB_ARTIFACT_NAME` is set.
  - `--require-artifact` rejects CI manifests that do not identify an artifact.
  - `--require-artifact` rejects downloaded artifacts that do not contain the release archive.
  - `--require-artifact` rejects downloaded artifacts whose `Flowline-X.Y.Z.zip` hash differs from the archive being tagged.
  - `--require-artifact` accepts a CI manifest when the downloaded artifact archive hash matches.
- Updated `script/package_release.sh`:
  - Manifest now records `github_artifact_name`.
- Updated `script/publish_preflight.sh`:
  - Adds `--require-artifact`.
  - Requires `--require-ci` when artifact verification is requested.
  - Downloads the named GitHub Actions artifact from the manifest's run with `gh run download`.
  - Finds the expected `Flowline-X.Y.Z.zip` in the downloaded artifact and compares its SHA-256 to the local archive.
- Updated release docs:
  - `README.md`
  - `docs/RELEASE.md`
- Pushed implementation commit `8960f3d`.
- GitHub Actions run `26223992844` passed for `8960f3d`.

## Current State

- Working tree was clean after pushing `8960f3d` and before writing this checkpoint.
- `main` and `origin/main` were aligned at `8960f3dee55d2279e1ca0bb4232ee32dd02e94b7`.
- GitHub Actions is green on current implementation head:
  - Run: `26223992844`
  - Workflow: `CI`
  - Head: `8960f3dee55d2279e1ca0bb4232ee32dd02e94b7`
  - Result: `success`
  - Job: `Swift tests`
  - Steps passed: checkout, Swift version, ripgrep install, publish preflight, build, test.

## Important Files / Artifacts

- `script/package_release.sh`: release manifest includes `github_artifact_name`.
- `script/publish_preflight.sh`: `--require-artifact` downloads the run artifact and verifies archive hash equality.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers the artifact provenance gate with fake `rtk gh run download`.
- `docs/RELEASE.md`: documents `GITHUB_ARTIFACT_NAME` and `--require-artifact` for CI-uploaded release archives.
- `README.md`: summarizes the stricter artifact gate for release tags.

## Verification

- Command: `rtk swift test --filter Artifact`
  - Result: passed, 6 tests.
- Command: `rtk swift test --filter PermissionSurfaceTests`
  - Result: passed, 49 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 258 tests.
- Command: `rtk gh run watch 26223992844 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `8960f3d`.

## Open Questions / Risks

- Historical Google OAuth credential still needs external rotation/revocation if it was real.
- Real release packaging/notarization still requires external Apple credentials:
  - Developer ID Application certificate.
  - `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.
- `--require-artifact` is implemented and test-covered, but no live notarized release archive artifact was available in this session. The next real release workflow should set `GITHUB_ARTIFACT_NAME`, upload the archive, then run `--require-ci --require-artifact`.

## Next Steps

1. Add or run a real release workflow that packages/notarizes with Apple credentials, uploads the release archive artifact, and runs `--require-ci --require-artifact` against that artifact.
2. Run one real notarized release when Apple Developer ID and notary credentials are available.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `8960f3d`; `--require-ci` verifies successful run provenance, and `--require-artifact` now downloads the manifest's GitHub Actions artifact and verifies the downloaded `Flowline-X.Y.Z.zip` hash matches the archive being tagged. Follow project rules from `AGENTS.md`: use `rtk`, preserve unrelated changes, and verify before claiming completion.
