# Project Checkpoint - 2026-05-20 18:04 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest commit: `9731678 Relax music control wait in CI`
- Remote: `origin -> https://github.com/kingkyylian/flowline.git`
- Context: CI/release readiness was the active target; GitHub Actions is now green on `main`.

## Done This Session

- Restored from the stale `17:38` checkpoint and inspected the actual current repo state.
- Traced the failing GitHub Actions sequence across multiple `main` pushes:
  - `67257a7`: first CI workflow existed but preflight failed before build.
  - `3021ebc`: publish preflight failed because `rg` was unavailable.
  - `0a6997b`: `ripgrep` install fixed preflight, then build failed on `IsolatedDeinit`.
  - `86bd143`: frontend flags still failed with production Swift 6.1.2.
  - `1d11176`: build passed after removing the experimental deinit flag path.
  - `c6834f3`: test compile passed after `NSDraggingInfo` conformance moved to `@preconcurrency`.
  - `9731678`: CI passed after relaxing the async music-control wait window.
- Added/kept regression coverage for CI-specific compiler risks:
  - CI must not use `enable-experimental-feature IsolatedDeinit`.
  - CI-sensitive source must not contain `isolated deinit`.
  - The `NSDraggingInfo` test double must use `@preconcurrency NSDraggingInfo`.
- Preserved `script/publish_preflight.sh` in CI with full-history checkout and `ripgrep` installation.
- Pushed all fixes to remote `main`.

## Current State

- Working tree is clean.
- `main` is synchronized with `origin/main`.
- Latest successful GitHub Actions run:
  - Run: `26171151814`
  - Head: `97316786835358c685f0440ffd809a972ea7fe3f`
  - Result: `success`
  - Steps passed: checkout, install ripgrep, publish preflight, build, test.
- Local publish preflight passes against the pushed remote HEAD.
- Local full test suite passes with 228 tests.

## Important Files / Artifacts

- `.github/workflows/ci.yml`: CI now installs `ripgrep`, runs publish preflight, then plain `swift build` and `swift test`.
- `Sources/FlowlineApp/**`: `isolated deinit` syntax has been replaced with `MainActor.assumeIsolated` cleanup in main-actor classes.
- `Tests/FlowlineAppTests/OverlayHostingViewHoldDropTests.swift`: `NSDraggingInfo` test double uses `@preconcurrency` conformance for Swift 6.1.2 CI compatibility.
- `Tests/FlowlineAppTests/MusicPlaybackRefreshWorkerTests.swift`: async wait window relaxed for CI scheduling variance.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: CI regression tests cover preflight, deinit flag avoidance, and dragging info conformance.

## Verification

- Command: `rtk swift test --filter ciAvoidsProductionCompilerRejectedIsolatedDeinitFlags`
  - Result: first failed against the old CI/source state, then passed after implementation.
- Command: `rtk swift test --filter musicControlTogglesCurrentPlayerStateWithoutTrustingStaleSnapshot`
  - Result: passed.
- Command: `rtk swift build`
  - Result: passed with no experimental deinit flags.
- Command: `rtk swift test`
  - Result: passed, 228 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk ./script/publish_preflight.sh`
  - Result: passed after push; secret scan passed and remote HEAD matched `9731678`.
- Command: `rtk gh run view 26171151814 --repo kingkyylian/flowline --json jobs,conclusion,status,headSha,url`
  - Result: completed successfully for `97316786835358c685f0440ffd809a972ea7fe3f`.

## Open Questions / Risks

- Historical OAuth credential still needs external rotation/revocation if it was real.
- Real release packaging/notarization still requires external Apple credentials:
  - Developer ID Application certificate.
  - `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials.
- `MainActor.assumeIsolated` assumes these main-actor services are destroyed on the main actor, matching current app/test ownership.

## Next Steps

1. Rotate/revoke the exposed Google OAuth credential if it was real.
2. Install/select a Developer ID Application identity and set `FLOWLINE_DEVELOPER_ID_IDENTITY`.
3. Configure `FLOWLINE_NOTARY_PROFILE` or Apple ID notary credentials, then run `script/package_release.sh --notarize`.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/ci.yml`, and the latest GitHub Actions run for `main`. CI is green on `9731678`; publish preflight, build, and test are now guarded in GitHub Actions. Real release notarization remains blocked on external Apple credentials.
