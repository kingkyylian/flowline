# Project Checkpoint - 2026-05-24 18:21 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, synchronized with `origin/main`
- Head: `5047b15b413d49e51d71d27fb495456b438b87a3`
- Context: Release-candidate execution is still blocked by missing external Apple/GitHub credentials, but the repo now has a tested helper to validate and configure the required GitHub Actions release secrets once the credentials are available.

## Done This Session

- Reconfirmed the previous release blocker:
  - GitHub Actions release secrets are still missing.
  - Local shell has none of the required release secret env values.
  - No local `Developer ID Application` identity is installed.
- Added `script/configure_release_secrets.sh`:
  - Reads required release secrets from env.
  - Supports `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PATH` as a safer alternative to manually precomputing `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`.
  - Validates nonblank required inputs, Developer ID identity prefix, Apple Team ID format, and certificate base64.
  - Uses `gh secret set` via stdin so secret values are not passed as CLI arguments.
  - Supports `--dry-run` for local input validation.
  - Supports `--check` to verify required secret names already exist in GitHub Actions.
  - Verifies configured secret names after writing unless `--no-verify` is passed.
- Updated `docs/RELEASE.md` with helper usage, dry-run, and check commands.
- Added release-secret helper coverage to `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`.
- Proved live repo state with the new helper:
  - `--dry-run` without env reports all seven missing local inputs.
  - `--check` with GitHub API access reports all seven required GitHub Actions secrets missing.
- Did not trigger `Release Candidate`; current `--check` proves it would fail at secret validation.

## Current State

- Source branch is still synchronized with `origin/main`.
- Working tree is intentionally dirty with release-helper and checkpoint work:
  - `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`
  - `docs/RELEASE.md`
  - `docs/checkpoints/LATEST.md`
  - `docs/checkpoints/2026-05-24-1810.md`
  - `docs/checkpoints/2026-05-24-1821.md`
  - `script/configure_release_secrets.sh`
- Source-side release gates are passing.
- A real notarized release candidate still cannot be produced until the external credentials are supplied and configured.

## Important Files / Artifacts

- `script/configure_release_secrets.sh`: new helper for validating/configuring/checking release GitHub Actions secrets.
- `docs/RELEASE.md`: now documents the helper-based release secret setup.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: added four tests covering helper secret list alignment, dry-run redaction, missing-input failure, and fake `gh secret set` behavior.
- `.github/workflows/release-candidate.yml`: unchanged release candidate workflow that consumes the configured secrets.
- `.github/workflows/release-candidate-verify.yml`: unchanged artifact provenance verifier.
- `docs/checkpoints/2026-05-24-1821.md`: this checkpoint.
- `docs/checkpoints/LATEST.md`: updated to this checkpoint.

## Verification

- Command: `rtk swift test --filter releaseSecretSetup`
  - Result: passed; 4 focused release-secret helper tests passed.
- Command: `script/configure_release_secrets.sh --repo kingkyylian/flowline --dry-run`
  - Result: expected failure; all seven required local inputs are missing.
- Command: `script/configure_release_secrets.sh --repo kingkyylian/flowline --check`
  - Result: sandbox run failed on GitHub API connectivity; network-approved rerun reached GitHub and failed as expected, listing all seven missing configured secrets.
- Command: `script/secret_scan.sh`
  - Result: passed.
- Command: `git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed; `Test run with 283 tests in 0 suites passed after 12.804 seconds`.
- Command: `rtk swift build -c release`
  - Result: passed; release build complete.
- Command: `git status --short --branch`
  - Result: branch is synchronized with `origin/main`; release-helper/checkpoint files are uncommitted.

## Open Questions / Risks

- The release-candidate workflow still requires these repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- Local notarized packaging still requires an installed `Developer ID Application` identity, or CI must import it from the configured certificate secret.
- Helper/test/docs/checkpoint changes are not committed or pushed.

## Next Steps

1. Provide the Developer ID `.p12`, certificate password, Developer ID identity string, Apple ID, Team ID, and app-specific password.
2. Run the helper with those values, starting with `--dry-run`, then without `--dry-run`, then `--check`.
3. Run `Release Candidate` from `main` with the intended tag, for example `v0.1.0`.
4. Let `Release Candidate Verify` complete, then preserve the publish preflight evidence against the uploaded notarized artifact before creating or pushing the public release tag.

## Project Rules For Resume

- Use `rtk` for noisy shell commands.
- Keep Turkish communication short and concrete.
- Protect dirty worktree state.
- Prefer TDD for behavior changes.
- Do not claim release completion without fresh verification.
- No local `AGENTS.md` file exists in the repo; active instructions come from the conversation and user `.codex` files.

## Resume Prompt

Continue from this checkpoint. First inspect `git status --short --branch`, `docs/checkpoints/LATEST.md`, `script/configure_release_secrets.sh`, `docs/RELEASE.md`, and GitHub Actions secret state. Do not run the `Release Candidate` workflow until `script/configure_release_secrets.sh --repo kingkyylian/flowline --check` passes. Once credentials are configured, run the workflow for the intended tag and verify the uploaded notarized artifact with `Release Candidate Verify` / `script/publish_preflight.sh --require-ci --require-artifact`.
