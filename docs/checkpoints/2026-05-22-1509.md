# Project Checkpoint - 2026-05-22 15:09 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit:
  - `19c0531 Verify release artifact manifest contents`
- Context: Continued autonomous release hardening by requiring the downloaded CI artifact manifest to match the local release manifest before tag publication.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current `main` and CI were clean.
- Selected the next secret-free release hardening target:
  - `script/publish_preflight.sh --require-artifact` already downloaded the GitHub Actions artifact and checked the release ZIP hash.
  - It did not require the artifact's `Flowline-X.Y.Z.manifest` to exist or match the local manifest being used for publication.
  - That left a provenance gap where a local manifest could diverge from the manifest actually uploaded with the release candidate artifact.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `publishPreflightRejectsCIManifestWhenDownloadedArtifactIsMissingManifest`
  - `publishPreflightRejectsCIManifestWhenDownloadedArtifactManifestDoesNotMatch`
  - The mismatch test first failed because the old script accepted a matching ZIP even when the downloaded manifest differed.
- Updated the fake GitHub artifact fixture to emit release manifests alongside downloaded archives, while still allowing missing or mismatched manifest cases.
- Updated `script/publish_preflight.sh`:
  - Finds exactly one downloaded `Flowline-X.Y.Z.manifest` in the GitHub artifact.
  - Rejects missing or ambiguous downloaded manifests.
  - Compares the downloaded manifest byte-for-byte against the local manifest.
- Updated `README.md` and `docs/RELEASE.md` to document that `--require-artifact` verifies both archive hash and manifest content.
- Pushed implementation commit `19c0531`.
- GitHub Actions run `26286817944` passed for `19c0531`.

## Current State

- The release hardening implementation is pushed at `19c05319da2b4e161de260653f822651439c50a7`.
- Working tree was clean before writing this checkpoint.
- Release identity/provenance checks now cover:
  - exact candidate artifact names;
  - artifact version to archive/tag binding;
  - manifest GitHub artifact name to release tag binding;
  - manifest workflow binding to `Release Candidate` for artifact provenance;
  - manifest notarization binding to `notarized=true` for artifact provenance;
  - downloaded artifact ZIP hash matching the local archive;
  - downloaded artifact manifest content matching the local manifest;
  - ambiguous downloaded archives and manifests;
  - CI run head/status matching;
  - release manifest archive name, version, checksum, size, and git commit.

## Important Files / Artifacts

- `script/publish_preflight.sh`: now verifies the downloaded release manifest from the GitHub artifact.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers missing and mismatched downloaded artifact manifests.
- `README.md`: documents artifact archive and manifest verification.
- `docs/RELEASE.md`: documents artifact archive and manifest verification.
- `.github/workflows/release-candidate.yml`: uploads both `Flowline-X.Y.Z.zip` and `Flowline-X.Y.Z.manifest`.
- `.github/workflows/release-candidate-verify.yml`: runs publish preflight with `--require-ci --require-artifact`.

## Verification

- Command: `rtk swift test --filter DownloadedArtifactManifest`
  - Result: first failed against the old script because a mismatched downloaded manifest passed, then passed after enforcing manifest comparison.
- Command: `rtk swift test --filter MissingManifest`
  - Result: passed.
- Command: `rtk swift test --filter 'publishPreflight|releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 65 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 274 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26286817944 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `19c0531`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `19c0531`; publish preflight now requires the downloaded GitHub artifact to contain both a matching archive and a matching manifest.
