# Project Checkpoint - 2026-05-23 16:27 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, synchronized with `origin/main` after pushing code commit `ce96dc1`
- Latest code commit:
  - `ce96dc1 Keep Hold left only`
- Context: Hold is now a true left-only module; stale right-slot Hold render/drop/layout support was removed.

## Done This Session

- Fixed the Settings Modules behavior behind the `Left only` copy:
  - `Hold` no longer supports `.right` placement in `FlowlineModuleSelection`.
  - When `Workspace` takes the left slot while `Hold` is enabled, `AppState` closes `Hold` instead of moving it right.
  - When `Workspace` already owns the left slot, `Hold` remains disabled with `slot` status.
- Removed stale right-slot Hold implementation paths:
  - `ExpandedBarView` no longer renders a right-side `NotchHoldColumn`.
  - `OverlayHoldDropTarget` no longer accepts a right-side Hold drop frame.
  - `NotchHoldTrayLayout` and `NotchHoldHeaderLayout` no longer carry right-Hold sizing/inset helpers.
- Updated tests to cover the new product rule:
  - Hold supported placements are `[.left]`.
  - Workspace + Hold is invalid at the placement layer.
  - Notch layout does not place Hold on the right.
  - Expanded Hold drop target rejects the right side when Workspace uses the left slot.
  - AppState normalizes old Workspace+Hold persisted state by turning Hold off.
- Pushed `ce96dc1` to `origin/main`.
- Verified GitHub Actions CI for `ce96dc1` passed:
  - Run `26333853762`
  - Workflow `CI`
  - Conclusion `success`

## Current State

- `main` is synchronized with `origin/main` before writing this checkpoint.
- Latest code commit is `ce96dc1a755d363f39924df65a78d12c616a7861`.
- Running local app bundle was rebuilt, signed, relaunched, and verified running.
- Settings window was opened from the Flowline status item menu through Accessibility automation.
- Sidebar click automation for Settings Modules was unreliable, so final live visual QA is still a manual follow-up.

## Important Files / Artifacts

- `Sources/FlowlineCore/Models/FlowlineModulePreferences.swift`: declares Hold as left-only at selection-policy level.
- `Sources/FlowlineApp/Stores/AppState.swift`: closes Hold when Workspace claims the left slot and normalizes invalid persisted state.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`: removed right-side Hold rendering path.
- `Sources/FlowlineApp/Support/OverlayHoldDropTarget.swift`: removed right-side Hold drop target frame.
- `Sources/FlowlineApp/Support/NotchHoldTrayLayout.swift`: Hold tray metrics are now left-column only.
- `Sources/FlowlineApp/Support/NotchUtilityMusicLayout.swift`: Hold header metrics are now left-column only.
- `Tests/FlowlineCoreTests/ModulePreferencesTests.swift`: covers Hold supported placements and invalid Workspace+Hold placement.
- `Tests/FlowlineAppTests/AppStateHoldDropFeedbackTests.swift`: covers Workspace enabling closing Hold and startup normalization.
- `Tests/FlowlineAppTests/NotchModuleSlotLayoutTests.swift`: covers no right-slot Hold layout.
- `Tests/FlowlineAppTests/OverlayHoldDropTargetTests.swift`: covers right-side Hold drop rejection.
- `Tests/FlowlineAppTests/NotchHoldTrayLayoutTests.swift`: removed right-Hold tray metric expectations.
- `Tests/FlowlineAppTests/NotchUtilityMusicLayoutTests.swift`: removed right-Hold header expectations.
- `dist/Flowline.app`: local runnable app bundle rebuilt after the code change.

## Verification

- Command: `rg -n "rightHoldFrame|Hold.*right|right.*Hold|right-side Hold|right-slot Hold|Hold moves right|Left first|NotchHoldTrayLayout\\.actionRowWidth\\(|NotchHoldTrayLayout\\.actionRowLeadingOffset\\(|NotchHoldHeaderLayout\\.(leadingInset|headerShiftX)\\(" Sources Tests`
  - Result: passed by returning no matches.
- Command: `rtk swift test --filter NotchHoldTrayLayoutTests`
  - Result: passed, 23 tests.
- Command: `rtk swift test --filter NotchUtilityMusicLayoutTests`
  - Result: passed, 10 tests.
- Command: `rtk swift test --filter OverlayHoldDropTargetTests`
  - Result: passed, 4 tests.
- Command: `rtk swift test --filter musicControlTogglesCurrentPlayerStateWithoutTrustingStaleSnapshot`
  - Result: passed after an earlier full-suite timeout isolated to this flaky unrelated music test.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed on the final run, 279 tests.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; `Flowline is running`.
- Command: `rtk gh run watch 26333853762 --repo kingkyylian/flowline --exit-status`
  - Result: passed for code commit `ce96dc1`.

## Open Questions / Risks

- Manual live Settings Modules QA remains:
  - Open Settings > Modules.
  - Confirm Workspace checked makes Hold unchecked/disabled instead of right-positioned.
  - Confirm Hold row still reads `Left only`.
- About page `OPEN` controls still need a final manual click in the live app if browser behavior is in question.
- Release candidate workflow still requires real repository Apple signing/notary secrets for a notarized candidate run.
- Historical checkpoint files still document older right-slot Hold work as history; do not treat those old files as current product behavior.

## Next Steps

1. Commit and push this checkpoint-only update.
2. Confirm CI on the checkpoint commit is green.
3. Manually verify Settings > Modules and About links in the live app.

## Project Rules For Resume

- Use `rtk` for noisy shell commands.
- Keep Turkish communication short and concrete.
- Protect dirty worktree state.
- Prefer TDD for behavior changes.
- Do not claim completion without fresh verification.
- No local `AGENTS.md` file exists in the repo; active instructions come from the conversation and user `.codex` files.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -5`, `docs/checkpoints/LATEST.md`, `Sources/FlowlineCore/Models/FlowlineModulePreferences.swift`, `Sources/FlowlineApp/Stores/AppState.swift`, `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`, and the CI run for the latest checkpoint commit. The latest code commit is `ce96dc1`, and CI run `26333853762` is green for that code.
