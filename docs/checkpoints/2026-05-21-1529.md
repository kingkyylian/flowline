# Project Checkpoint - 2026-05-21 15:29 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest implementation commit:
  - `3b94b82 Preflight release candidate tags before secrets`
- Context: Continued autonomous release hardening by preventing the manual release candidate workflow from touching Apple signing/notary secrets when the intended release tag already exists.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and found that it was behind current head.
- Confirmed current head is `3b94b82ff375ac2b79958c77718655f4e24e0f1c`.
- Confirmed working tree is clean.
- Confirmed GitHub Actions CI run `26225846121` passed for `3b94b82`.
- Recorded the newer release tag preflight work into this checkpoint.

## Current State

- `.github/workflows/release-candidate.yml` now runs `Preflight release tag` before `Validate release secrets`.
- The preflight rejects an existing local tag:
  - `git rev-parse -q --verify "refs/tags/$RELEASE_TAG"`
- The preflight rejects an existing origin tag:
  - `git ls-remote --exit-code --tags origin "refs/tags/$RELEASE_TAG"`
- This avoids consuming Developer ID, keychain, or notary secrets for a candidate tag that cannot be safely produced.

## Important Files / Artifacts

- `.github/workflows/release-candidate.yml`: contains the pre-secret release tag collision check and the scoped Developer ID `.p12` import.
- `.github/workflows/release-candidate-verify.yml`: verifies completed candidate artifacts with `--require-ci --require-artifact`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers release workflow security and release tag preflight invariants.
- `script/publish_preflight.sh`: verifies CI provenance and, when required, matching artifact provenance before publish.

## Verification

- Command: `rtk swift test --filter ExistingTagBeforeUsingReleaseSecrets`
  - Result: first failed against the old workflow, then passed after adding the preflight step.
- Command: `rtk swift test --filter 'releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 54 tests.
- Command: `rtk ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/ci.yml .github/workflows/release-candidate.yml .github/workflows/release-candidate-verify.yml`
  - Result: passed.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 263 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26225846121 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `3b94b82`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Inspect the release candidate verify path for artifact ambiguity or stale-run hazards.
2. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
3. Wait for `Release Candidate Verify` to pass, then create/push the release tag.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `3b94b82`; release candidate signing import uses scoped key access, and the candidate workflow now rejects existing tags before validating or consuming release secrets.
