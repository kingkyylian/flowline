# Project Checkpoint - 2026-05-07 14:08

## Project

- Root: `/Users/kyylian/flowline`
- Git: `main`, no commits yet; project files are currently untracked.
- Context: Flowline is a local-first macOS developer workflow layer that should feel like it emerges from the physical MacBook notch.

## Done This Session

- Performed whole-project audit for dead code, lifecycle gaps, and performance risks.
- Implemented the first cleanup/optimization pass across UI geometry, services, and core utilities.
- Added centralized notch sizing in `Sources/FlowlineApp/Models/NotchMetrics.swift`.
- Reworked collapsed notch sizing so the NSPanel no longer keeps a 600px invisible hit area while collapsed.
- Changed hover affordance so the collapsed notch only opens narrow side wings with `%72` / `%28` style numeric labels.
- Removed dead UI code:
  - `NextPanel` from `ExpandedBarView.swift`
  - `expandDelay` from `AppState.swift`
  - unreachable notch-specific branches inside companion pill rendering
- Added lifecycle cleanup:
  - `ActiveAppMonitor.stop()`
  - `CalendarService.stop()`
  - `ShelfService.stop()`
  - `isolated deinit` cleanup for timer/observer owners
  - `OverlayController` screen observer cleanup
- Reduced unnecessary refresh work:
  - `ActiveAppMonitor` no longer republishes identical `ActiveAppContext`.
  - `AppState.bindServices()` is idempotent.
  - `GitContextService` caches same context/repo status for 5 seconds.
  - `GitContextService` no longer falls back to `Documents` when no working directory can be inferred.
- Optimized small core utility paths:
  - `CalendarService` uses `min` instead of sorting every event.
  - `URLDetectors` reuses one `NSDataDetector`.

## Current State

- App builds and runs.
- Swift tests pass.
- `Flowline is running` after verify script.
- All files remain uncommitted/untracked because the repository has no initial commit yet.
- The physical notch illusion is the top product/design priority.

## Important Files / Artifacts

- `Sources/FlowlineApp/Models/NotchMetrics.swift`: single source of truth for collapsed, hover, and expanded notch dimensions.
- `Sources/FlowlineApp/App/OverlayController.swift`: NSPanel sizing, top-edge frame, duplicate resize cleanup, observer cleanup.
- `Sources/FlowlineApp/Views/Overlay/OverlayRootView.swift`: notch root width/height, expansion animation, collapsed hit area behavior.
- `Sources/FlowlineApp/Views/Overlay/CollapsedPillView.swift`: collapsed notch dots/icon and side percentage labels.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`: notch cockpit sizing and removed dead `NextPanel`.
- `Sources/FlowlineApp/Stores/AppState.swift`: idempotent service binding and service shutdown.
- `Sources/FlowlineApp/Services/ActiveAppMonitor.swift`: duplicate context publish prevention and lifecycle cleanup.
- `Sources/FlowlineApp/Services/GitContextService.swift`: short cache and no `Documents` fallback.
- `Sources/FlowlineApp/Services/CalendarService.swift`: lifecycle cleanup and cheaper next-event selection.
- `Sources/FlowlineApp/Services/ShelfService.swift`: lifecycle cleanup.
- `Sources/FlowlineCore/Support/URLDetectors.swift`: cached link detector.

## Verification

- Command: `rtk swift build`
- Result: passed

- Command: `rtk swift test`
- Result: passed, 10 tests

- Command: `rtk ./script/build_and_run.sh --verify`
- Result: passed, `Flowline is running`

## Open Questions / Risks

- The `%72` / `%28` side labels are still placeholder/static signals, not real provider usage or quota data.
- Need visual screenshot verification after the latest collapsed-width change to confirm exact physical notch alignment.
- Git status cache can be stale for up to 5 seconds by design.
- Repo has no initial commit; future sessions must be careful not to lose the all-untracked baseline.

## Next Steps

1. Inspect a fresh screenshot of collapsed hover and expanded notch after this checkpoint.
2. If the notch is still a few pixels off, restore/use auxiliary top-area based physical notch center logic or add a tiny calibration constant only after measuring.
3. Replace static Codex/Claude percentages with real session/quota data or remove them from the product surface.
4. Add focused tests for `NotchMetrics` and `GitContextService` cache/path behavior.
5. Consider splitting `ExpandedBarView.swift` into notch cockpit, companion panels, and shared controls after visual behavior stabilizes.

## Project Rules For Resume

- Commands should use `rtk`.
- Keep communication Turkish, short, and concrete.
- Do not revert user/unrelated changes.
- Before claiming done, run fresh verification.

## Resume Prompt

Continue from this checkpoint. First read this file and the project `AGENTS.md`, then inspect the listed UI and service files before making changes. Prioritize the physical notch illusion: centered on the real notch, black continuous body, narrow hover wings, no invisible 600px collapsed hit area.
