# Project Checkpoint - 2026-05-12 21:17 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`; dirty worktree with active Flowline notch/HOLD/LIMITS/module changes.
- Latest commit: `c2fc8f9 Record notch UI checkpoint`
- Context: `LIMITS` is now the fixed center surface; users choose up to two side modules for left/right.

## Done This Session

- Reworked module policy around the product model:
  - `LIMITS` is no longer treated as a removable/selectable module
  - `LIMITS` stays fixed in the center
  - optional modules are side modules only
  - users can enable at most 2 optional modules, matching the left/right slots
- Moved Calendar out of the center:
  - Calendar now places into left/right like Workspace, Music, and Hold
  - placement uses side-slot preferences:
    - Workspace and Hold prefer left, then right
    - Music and Calendar prefer right, then left
  - examples covered by tests:
    - Workspace + Calendar => Workspace left, Calendar right
    - Music + Calendar => Calendar left, Music right
    - Workspace + Hold => Workspace left, Hold right
- Added a real side Calendar column:
  - header `CALENDAR`
  - permission state: `Needs access` with an allow button
  - idle state: `No event` / `Next 24h clear`
  - event state: event title, next time, and Join button when a meeting URL exists
- Updated Settings module count:
  - now shows `0/2 + Limits`, `1/2 + Limits`, or `2/2 + Limits`
  - third optional module toggle is rejected as `max`
- Preserved previous fixes:
  - screenshot drag-to-open works
  - ordinary Terminal/window drag does not open panel
  - clipboard text capture is opt-in and fast
  - screenshot auto-capture remains user-controlled

## Current State

- Flowline has been rebuilt, signed, relaunched, and verified running after the module-policy change.
- Worktree is still dirty and uncommitted.
- `docs/checkpoints/LATEST.md` points to this checkpoint.

## Important Files / Artifacts

- `Sources/FlowlineCore/Models/FlowlineModulePreferences.swift`
  - optional module cap is now 2; placement is side-only.
- `Sources/FlowlineApp/Support/NotchModuleSlotLayout.swift`
  - maps enabled optional modules into left/right slots, including Calendar.
- `Sources/FlowlineApp/Views/Overlay/ExpandedBarView.swift`
  - center `LIMITS` no longer receives Calendar status; Calendar renders as side column.
- `Sources/FlowlineApp/Support/OverlayModuleCountPresentation.swift`
  - settings count label now reflects side module capacity plus fixed Limits.
- `Sources/FlowlineApp/Views/Settings/SettingsView.swift`
  - module UI reflects the new `2/2 + Limits` model.
- `Sources/FlowlineApp/Support/CalendarModulePresentation.swift`
  - Calendar detail/status presentation.
- `Tests/FlowlineCoreTests/ModulePreferencesTests.swift`
  - covers 2-module cap and side-only Calendar placement.
- `Tests/FlowlineAppTests/NotchModuleSlotLayoutTests.swift`
  - covers Calendar left/right slot placement.
- `Tests/FlowlineAppTests/OverlayModuleCountPresentationTests.swift`
  - covers `2/2 + Limits` labels.

## Verification

- Command: `rtk swift test --filter ModulePreferencesTests`
  - Result: passed; 4 tests.
- Command: `rtk swift test --filter NotchModuleSlotLayoutTests`
  - Result: passed; 5 tests.
- Command: `rtk swift test --filter OverlayModuleCountPresentationTests`
  - Result: passed; 3 tests.
- Command: `rtk swift test --filter CalendarModulePresentationTests`
  - Result: passed; 4 tests.
- Command: `rtk swift test`
  - Result: passed; 155 tests.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk ./script/build_and_run.sh --verify`
  - Result: passed; Flowline built, signed, relaunched, and process verified running.

## Open Questions / Risks

- User should physically verify Settings module toggles: only two side modules can be enabled at once.
- User should physically verify Calendar side placement with combinations:
  - Workspace + Calendar
  - Music + Calendar
  - Hold + Calendar
- The worktree is large; stage related files carefully before commit.

## Next Steps

1. Physically test module combinations in Settings and confirm `LIMITS` stays center.
2. Physically test Calendar side column in at least one left-slot and one right-slot combination.
3. Commit the stabilized HOLD/LIMITS/module batch after physical validation.

## Project Rules For Resume

- Use `rtk` prefix for shell commands.
- Keep Turkish communication short and concrete.
- Do not claim completion without fresh verification.
- For behavior changes, prefer test-first and keep changes scoped.
- Do not revert unrelated user changes in the dirty worktree.

## Resume Prompt

Continue from this checkpoint. First read this file and the project instructions, then physically validate module combinations: `LIMITS` fixed center plus exactly two optional side modules.
