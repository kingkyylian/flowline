# Project Checkpoint - 2026-05-22 14:35 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main` before writing this checkpoint
- Latest implementation commit:
  - `6ef206a Require notarized release artifacts`
- Context: Continued autonomous release hardening by requiring CI release artifacts to prove they were notarized before tag publication.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed the previous checkpoint was clean on `main`.
- Selected the next secret-free release hardening target:
  - `script/package_release.sh --notarize` already records `notarized=true` in the release manifest.
  - `script/publish_preflight.sh --require-artifact` already checked repository, run ID, run status, run head, workflow name, artifact name, archive name, tag version, size, and hash.
  - It did not enforce the manifest's notarization flag, so a non-notarized artifact from the correct workflow could pass if the archive hash matched.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `publishPreflightRejectsCIManifestWhenArtifactIsNotNotarized` first failed because `notarized=false` passed the artifact gate.
  - Artifact-gate fixtures that are supposed to reach downloaded archive checks now explicitly write `notarized=true`.
- Updated `script/publish_preflight.sh`:
  - Reads `notarized` from the release manifest.
  - Requires `notarized=true` when `--require-artifact` is used.
  - Keeps generic `--require-ci` behavior unchanged for non-artifact CI provenance checks.
- Updated `README.md` and `docs/RELEASE.md` to document the stricter artifact gate.
- Pushed implementation commit `6ef206a`.
- GitHub Actions run `26285287424` passed for `6ef206a`.

## Current State

- The release hardening implementation is pushed at `6ef206a8d0bed482195a8ae887fa697a50d82234`.
- Working tree was clean before writing this checkpoint.
- Release identity/provenance checks now cover:
  - exact candidate artifact names;
  - artifact version to archive/tag binding;
  - manifest GitHub artifact name to release tag binding;
  - manifest workflow binding to `Release Candidate` for artifact provenance;
  - manifest notarization binding to `notarized=true` for artifact provenance;
  - ambiguous downloaded archives in both the verify workflow and publish preflight;
  - CI run head/status matching;
  - release manifest archive name, version, checksum, size, and git commit.

## Important Files / Artifacts

- `script/publish_preflight.sh`: now requires `notarized=true` for `--require-artifact`.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers the not-notarized rejection and artifact-provenance fixtures.
- `README.md`: documents the notarized artifact gate.
- `docs/RELEASE.md`: documents the notarized artifact gate.
- `.github/workflows/release-candidate.yml`: packages notarized release archives and writes the manifest.
- `.github/workflows/release-candidate-verify.yml`: runs publish preflight with `--require-ci --require-artifact`.

## Verification

- Command: `rtk swift test --filter ArtifactIsNotNotarized`
  - Result: first failed against the old script because `notarized=false` passed the artifact gate, then passed after enforcing notarization.
- Command: `rtk swift test --filter 'publishPreflight|releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 63 tests.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 272 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26285287424 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `6ef206a`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `6ef206a`; publish preflight now requires manifest `notarized=true` when `--require-artifact` is used.
