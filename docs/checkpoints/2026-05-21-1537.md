# Project Checkpoint - 2026-05-21 15:37 +03

## Project

- Root: `/Users/kyylian/flowline`
- Git: branch `main`, clean and synchronized with `origin/main`
- Latest implementation commit:
  - `7f75c9f Reject ambiguous release candidate artifacts`
- Context: Continued autonomous release hardening by making the release candidate verify workflow reject ambiguous artifacts and ambiguous downloaded archives instead of silently picking the first match.

## Done This Session

- Restored from `docs/checkpoints/LATEST.md` and confirmed current work had advanced past the older checkpoint.
- Confirmed checkpoint commit `58f9743` is pushed and CI run `26226050775` passed.
- Selected the next release hardening target in `.github/workflows/release-candidate-verify.yml`.
- Added RED/GREEN coverage in `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`:
  - `releaseCandidateVerifyWorkflowRejectsAmbiguousCandidateArtifacts`
  - `releaseCandidateVerifyWorkflowRejectsAmbiguousDownloadedArchives`
- Updated `.github/workflows/release-candidate-verify.yml`:
  - Replaced `head -n 1` artifact selection with an exact count check.
  - Replaced `find ... -print -quit` archive selection with an exact count check.
  - Emits the ambiguous artifact/archive names before failing.
- Pushed implementation commit `7f75c9f`.
- GitHub Actions run `26226236921` passed for `7f75c9f`.

## Current State

- The release hardening implementation is pushed at `7f75c9fb3e90ee0529b5bd2f62d695549f317f02`.
- Working tree was clean before writing this checkpoint.
- Release candidate verification now requires exactly one candidate artifact from the triggering run and exactly one `Flowline-*.zip` inside the downloaded artifact.
- Existing release protections remain in place:
  - candidate workflow rejects existing tags before reading release secrets;
  - Developer ID `.p12` import uses scoped keychain access;
  - publish preflight verifies CI and artifact provenance.

## Important Files / Artifacts

- `.github/workflows/release-candidate-verify.yml`: rejects ambiguous release candidate artifacts and archives.
- `.github/workflows/release-candidate.yml`: builds notarized release candidates and rejects existing tags before secret validation.
- `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`: covers release workflow security invariants.
- `script/publish_preflight.sh`: verifies release tag, manifest, CI, and artifact provenance.

## Verification

- Command: `rtk swift test --filter AmbiguousCandidateArtifacts`
  - Result: first failed against the old workflow, then passed after the artifact count check.
- Command: `rtk swift test --filter AmbiguousDownloadedArchives`
  - Result: first failed against the old workflow, then passed after the archive count check.
- Command: `rtk swift test --filter 'AmbiguousCandidateArtifacts|AmbiguousDownloadedArchives'`
  - Result: passed, 2 tests.
- Command: `rtk swift test --filter 'releaseCandidate|PermissionSurfaceTests'`
  - Result: passed, 56 tests.
- Command: `rtk ruby -e 'require "yaml"; ARGV.each { |path| YAML.load_file(path); puts path }' .github/workflows/ci.yml .github/workflows/release-candidate.yml .github/workflows/release-candidate-verify.yml`
  - Result: passed.
- Command: `rtk bash -n script/publish_preflight.sh script/secret_scan.sh script/package_release.sh`
  - Result: passed.
- Command: `rtk git diff --check`
  - Result: passed.
- Command: `rtk swift test`
  - Result: passed, 265 tests.
- Command: `rtk ./script/secret_scan.sh`
  - Result: passed.
- Command: `rtk gh run watch 26226236921 --repo kingkyylian/flowline --exit-status`
  - Result: passed for implementation commit `7f75c9f`.

## Open Questions / Risks

- Real release candidate workflow execution still requires repository secrets:
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_BASE64`
  - `FLOWLINE_DEVELOPER_ID_CERTIFICATE_PASSWORD`
  - `FLOWLINE_DEVELOPER_ID_IDENTITY`
  - `FLOWLINE_KEYCHAIN_PASSWORD`
  - `APPLE_ID`
  - `APPLE_TEAM_ID`
  - `APPLE_APP_SPECIFIC_PASSWORD`
- The workflows are locally and CI validated, but no live notarized candidate run was executed because Apple credentials are external.
- Historical Google OAuth credential still needs external rotation/revocation if it was real.

## Next Steps

1. Configure the required GitHub repository secrets and run `Release Candidate` from `main` with the intended version tag.
2. Wait for `Release Candidate Verify` to pass, then create/push the release tag.
3. Rotate/revoke the exposed Google OAuth credential if it was real.

## Resume Prompt

Continue from this checkpoint. First inspect `rtk git status -sb`, `rtk git log --oneline -10`, `docs/checkpoints/LATEST.md`, `.github/workflows/release-candidate.yml`, `.github/workflows/release-candidate-verify.yml`, `script/package_release.sh`, `script/publish_preflight.sh`, `docs/RELEASE.md`, `README.md`, and `Tests/FlowlineAppTests/PermissionSurfaceTests.swift`. CI is green on `7f75c9f`; release candidate verification now rejects ambiguous artifacts and archives instead of silently selecting the first match.
