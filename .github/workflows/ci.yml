name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  swift-test:
    name: Swift tests
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Show Swift version
        run: swift --version

      - name: Publish preflight
        run: script/publish_preflight.sh

      - name: Build
        run: swift build

      - name: Test
        run: swift test
